# SaveForge 2.0 endpoint documentation

This directory holds the technical documentation of the public SaveForge 2.0
backend API. Each document describes one public endpoint: what it returns or
mutates, which input it accepts, how it validates that input, which errors it
produces, and how to verify it locally.

## Naming convention

```
docs/endpoints/<domain>/<endpoint_id>.md
```

The file name is the endpoint's `EndpointID` and the directory structure mirrors
`backend/endpoints`, so `backend/endpoints/catalog/get_catalog_info.go` is
documented in `docs/endpoints/catalog/get_catalog_info.md`.

Every public endpoint has its own endpoint file in `backend/endpoints`. That
file is either `contract-only`, meaning it defines the contract without a
runtime handler, or `implemented`, meaning the handler lives there too. A
technical document is written once the runtime handler is added, so a
contract-only endpoint has an endpoint file but no document yet.

Endpoints are never grouped into a shared document, and a document is never
created for a group of endpoints.

## Getters and mutations

The public API separates reads from writes:

- **Getters** only read state. They never modify a save, the GameCatalog, or any
  application data.
- **Mutations** (setters and other mutating endpoints) perform exactly one
  explicitly named operation. A mutation may return a typed result describing
  the resulting state, but it never doubles as a getter.

A single endpoint never switches between reading and writing based on a
parameter such as `action` or `mode`.

## What this documentation is

These documents describe the backend code as it exists today. They are a
technical description, not a specification: they do not replace the endpoint
contracts in `backend/endpoints` and they do not replace the implementation.
When code and documentation disagree, the code and its tests are authoritative
and the document must be corrected.

Documents describe implemented behaviour only. They do not design future
architecture, transports, or contracts.

## Endpoint statuses

Each document reports two independent statuses.

Implementation status:

| Status | Meaning |
|---|---|
| `contract-only` | The endpoint file defines the contract (`EndpointID`, `Definition`) but no runtime handler exists yet. |
| `implemented` | A runtime handler exists in the endpoint file and is covered by tests. |

Transport status:

| Status | Meaning |
|---|---|
| `not exposed` | The endpoint is callable only as a Go function. No Wails binding, HTTP route, or CLI command reaches it. |
| `transport-exposed` | The endpoint is reachable through at least one transport (Wails, HTTP, or CLI), named in the document. |

## Shared error contract

Every failure of every endpoint is reported in one public model, owned by
`backend/apperror`. An endpoint document describes only the failures specific to
its own rules and never repeats this section.

The model carries:

| Member | Meaning |
|---|---|
| `code` | Stable classification. A consumer branches on this and never on the wording of `message`. |
| `message` | Safe English fallback. The frontend owns the final localized text and falls back to this only for a code it does not know. |
| `params` | Safe parameters of the message, keyed by name. |
| `severity` | `error` or `warning`. |
| `stage` | `request`, `session`, `mutation` or `internal`. |
| `retryable` | Whether repeating the identical call can succeed. |
| `fieldErrors` | Rejected request fields, each with its own field name, stable code and safe fallback. |
| `currentRevision` | Present only on `revision_conflict`: the session's current `saveRevision`. |
| `diagnosticID` | Unpredictable identifier correlating the response with the backend log entry holding the private cause. |

The implemented codes are `invalid_request`, `invalid_revision`,
`revision_conflict`, `unknown_save_session`, `operation_failed` and
`internal_error`. A domain failure with no confirmed finer classification is
reported as `operation_failed`: the taxonomy is extended by adding a code, never
by inspecting error text.

A raw Go error, a stack trace, a private path and an unredacted parameter never
reach a response. `operation_failed` and `internal_error` therefore report a
fixed safe sentence, and the original cause is written to the backend log beside
the same `diagnosticID`.

Transports carry the same model:

- the local HTTP explorer answers with `{"error": <model>}` and derives the
  status from the code — `400` for `invalid_request` and `invalid_revision`,
  `404` for `unknown_save_session`, `409` for `revision_conflict` and `500` for
  `internal_error`;
- the Wails bridge carries the model as JSON behind the `saveforge-error:`
  marker, because Wails 2 transports only the string of a returned error. The
  frontend validates that envelope strictly and reduces anything else to its own
  `bridge_call_failed` code.

## Documented endpoints

| Name | EndpointID | Kind | Domain | Implementation status | Transport status | Document |
|---|---|---|---|---|---|---|
| `GetApplicationInfo` | `get_application_info` | Getter | `application` | implemented | transport-exposed — Wails bridge method `GetApplicationInfo`, and `GET /api/v1/application/info` of the local explorer | [application/get_application_info.md](application/get_application_info.md) |
| `GetSafetyProfile` | `get_safety_profile` | Getter | `application` | implemented (global host Safety Profile and its closed vocabulary) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [application/get_safety_profile.md](application/get_safety_profile.md) |
| `SetSafetyProfile` | `set_safety_profile` | Mutation | `application` | implemented (stores the global host Safety Profile; advances no save revision) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [application/set_safety_profile.md](application/set_safety_profile.md) |
| `GetHostSettings` | `get_host_settings` | Getter | `application` | implemented (the two Save behavior preferences and the two openable host directories; advances no save revision) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [application/get_host_settings.md](application/get_host_settings.md) |
| `SetHostSettings` | `set_host_settings` | Mutation | `application` | implemented (stores the complete host settings value; advances no save revision) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [application/set_host_settings.md](application/set_host_settings.md) |
| `ExportDiagnosticReport` | `export_diagnostic_report` | Getter | `application` | implemented (writes a redacted report carrying the diagnostic mode state and a bounded slice of safe records, and no save data, host path or key material) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [application/export_diagnostic_report.md](application/export_diagnostic_report.md) |
| `GetProjectLinks` | `get_project_links` | Getter | `application` | implemented (the closed allowlist of approved project addresses) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [application/get_project_links.md](application/get_project_links.md) |
| `CheckForUpdates` | `check_for_updates` | Getter | `application` | implemented (one manual, anonymous, bounded check of the official stable releases; downloads and installs nothing) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [application/check_for_updates.md](application/check_for_updates.md) |
| `GetCatalogInfo` | `get_catalog_info` | Getter | `catalog` | implemented | transport-exposed — `GET /api/v1/catalog/info` of the local explorer | [catalog/get_catalog_info.md](catalog/get_catalog_info.md) |
| `GetResource` | `get_resource` | Getter | `catalog` | implemented | transport-exposed — `GET /api/v1/catalog/resource` of the local explorer | [catalog/get_resource.md](catalog/get_resource.md) |
| `GetItemVariants` | `get_item_variants` | Getter | `catalog` | implemented | transport-exposed — `GET /api/v1/catalog/item-variants` of the local explorer | [catalog/get_item_variants.md](catalog/get_item_variants.md) |
| `GetResourceRelations` | `get_resource_relations` | Getter | `catalog` | implemented | transport-exposed — `GET /api/v1/catalog/resource-relations` of the local explorer | [catalog/get_resource_relations.md](catalog/get_resource_relations.md) |
| `GetResources` | `get_resources` | Getter | `catalog` | implemented | transport-exposed — `GET /api/v1/catalog/resources` of the local explorer | [catalog/get_resources.md](catalog/get_resources.md) |
| `GetItemDatabase` | `get_item_database` | Getter | `catalog` | implemented (authoritative Item Database page under the active Safety Profile) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [catalog/get_item_database.md](catalog/get_item_database.md) |
| `GetResourcePresentationSummaries` | `get_resource_presentation_summaries` | Getter | `catalog` | implemented | transport-exposed — Wails bridge method `GetResourcePresentationSummaries`, and `POST /api/v1/catalog/resource-presentation-summaries` of the local explorer | [catalog/get_resource_presentation_summaries.md](catalog/get_resource_presentation_summaries.md) |
| `GetNetworkPresets` | `get_network_presets` | Getter | `network` | implemented | transport-exposed — `GET /api/v1/network/presets` of the local explorer | [network/get_network_presets.md](network/get_network_presets.md) |
| `GetNetworkSettings` | `get_network_settings` | Getter | `network` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/network-settings` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [network/get_network_settings.md](network/get_network_settings.md) |
| `SetNetworkSettings` | `set_network_settings` | Mutation | `network` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/network-settings` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [network/set_network_settings.md](network/set_network_settings.md) |
| `ApplyNetworkPreset` | `apply_network_preset` | Mutation | `network` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/network-settings/preset` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [network/apply_network_preset.md](network/apply_network_preset.md) |
| `GetAppearancePresets` | `get_appearance_presets` | Getter | `appearance` | implemented | transport-exposed — `GET /api/v1/appearance/presets` of the local explorer | [appearance/get_appearance_presets.md](appearance/get_appearance_presets.md) |
| `ApplyAppearancePreset` | `apply_appearance_preset` | Mutation | `appearance` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/appearance/preset` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [appearance/apply_appearance_preset.md](appearance/apply_appearance_preset.md) |
| `LoadSave` | `load_save` | Mutation | `savesession` | implemented | transport-exposed — Wails bridge method `LoadSave`, and `POST /api/v1/save-sessions` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [savesession/load_save.md](savesession/load_save.md) |
| `GetLoadedSave` | `get_loaded_save` | Getter | `savesession` | implemented | transport-exposed — Wails bridge method `GetLoadedSave`, and `GET /api/v1/save-sessions/{saveSessionID}` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [savesession/get_loaded_save.md](savesession/get_loaded_save.md) |
| `WriteSave` | `write_save` | Mutation | `savesession` | implemented | transport-exposed — `POST /api/v1/save-sessions/{saveSessionID}/write` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [savesession/write_save.md](savesession/write_save.md) |
| `GetOperationHistory` | `get_operation_history` | Getter | `savesession` | implemented | transport-exposed — Wails bridge method `GetOperationHistory` | [savesession/get_operation_history.md](savesession/get_operation_history.md) |
| `UndoLastOperation` | `undo_last_operation` | Mutation | `savesession` | implemented | transport-exposed — Wails bridge method `UndoLastOperation` | [savesession/undo_last_operation.md](savesession/undo_last_operation.md) |
| `RedoLastOperation` | `redo_last_operation` | Mutation | `savesession` | implemented | transport-exposed — Wails bridge method `RedoLastOperation` | [savesession/redo_last_operation.md](savesession/redo_last_operation.md) |
| `RevertOperation` | `revert_operation` | Mutation | `savesession` | implemented | transport-exposed — Wails bridge method `RevertOperation` | [savesession/revert_operation.md](savesession/revert_operation.md) |
| `DiscardChanges` | `discard_changes` | Mutation | `savesession` | implemented | transport-exposed — Wails bridge method `DiscardChanges` | [savesession/discard_changes.md](savesession/discard_changes.md) |
| `ValidateReviewChanges` | `validate_review_changes` | Mutation | `savesession` | implemented | transport-exposed — Wails bridge method `ValidateReviewChanges` | [savesession/validate_review_changes.md](savesession/validate_review_changes.md) |
| `Save` | `save` | Mutation | `savesession` | implemented | transport-exposed — Wails bridge method `Save` | [savesession/save.md](savesession/save.md) |
| `SaveAs` | `save_as` | Mutation | `savesession` | implemented | transport-exposed — Wails bridge method `SaveAs` | [savesession/save_as.md](savesession/save_as.md) |
| `GetSaveLifecycleSettings` | `get_save_lifecycle_settings` | Getter | `savesession` | implemented | transport-exposed — Wails bridge method `GetSaveLifecycleSettings` | [savesession/get_save_lifecycle_settings.md](savesession/get_save_lifecycle_settings.md) |
| `SetSaveLifecycleSettings` | `set_save_lifecycle_settings` | Mutation | `savesession` | implemented | transport-exposed — Wails bridge method `SetSaveLifecycleSettings` | [savesession/set_save_lifecycle_settings.md](savesession/set_save_lifecycle_settings.md) |
| `GetRecentFiles` | `get_recent_files` | Getter | `savesession` | implemented | transport-exposed — Wails bridge method `GetRecentFiles` | [savesession/get_recent_files.md](savesession/get_recent_files.md) |
| `RecordRecentFile` | `record_recent_file` | Mutation | `savesession` | implemented | transport-exposed — Wails bridge method `RecordRecentFile` | [savesession/record_recent_file.md](savesession/record_recent_file.md) |
| `RemoveRecentFile` | `remove_recent_file` | Mutation | `savesession` | implemented | transport-exposed — Wails bridge method `RemoveRecentFile` | [savesession/remove_recent_file.md](savesession/remove_recent_file.md) |
| `ClearRecentFiles` | `clear_recent_files` | Mutation | `savesession` | implemented | transport-exposed — Wails bridge method `ClearRecentFiles` | [savesession/clear_recent_files.md](savesession/clear_recent_files.md) |
| `GetRecoveryJournals` | `get_recovery_journals` | Getter | `savesession` | implemented | transport-exposed — Wails bridge method `GetRecoveryJournals` | [savesession/get_recovery_journals.md](savesession/get_recovery_journals.md) |
| `GetRecoveryJournal` | `get_recovery_journal` | Getter | `savesession` | implemented | transport-exposed — Wails bridge method `GetRecoveryJournal` | [savesession/get_recovery_journal.md](savesession/get_recovery_journal.md) |
| `RestoreRecoveryJournal` | `restore_recovery_journal` | Mutation | `savesession` | implemented | transport-exposed — Wails bridge method `RestoreRecoveryJournal` | [savesession/restore_recovery_journal.md](savesession/restore_recovery_journal.md) |
| `DiscardRecoveryJournal` | `discard_recovery_journal` | Mutation | `savesession` | implemented | transport-exposed — Wails bridge method `DiscardRecoveryJournal` | [savesession/discard_recovery_journal.md](savesession/discard_recovery_journal.md) |
| `ExportRecoveryJournal` | `export_recovery_journal` | Mutation | `savesession` | implemented | transport-exposed — Wails bridge method `ExportRecoveryJournal` | [savesession/export_recovery_journal.md](savesession/export_recovery_journal.md) |
| `SetSaveAccountID` | `set_save_account_id` | Mutation | `savesession` | implemented (global and every active slot copy of the PC owner identifier) | transport-exposed — `PATCH /api/v1/save-sessions/{saveSessionID}/account-id` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [savesession/set_save_account_id.md](savesession/set_save_account_id.md) |
| `CloseSave` | `close_save` | Mutation | `savesession` | implemented | transport-exposed — `DELETE /api/v1/save-sessions/{saveSessionID}` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [savesession/close_save.md](savesession/close_save.md) |
| `GetSaveCharacters` | `get_save_characters` | Getter | `character` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/get_save_characters.md](character/get_save_characters.md) |
| `GetCharacterProfile` | `get_character_profile` | Getter | `character` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/profile` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/get_character_profile.md](character/get_character_profile.md) |
| `CloneCharacter` | `clone_character` | Mutation | `character` | implemented | transport-exposed — `POST /api/v1/save-sessions/{saveSessionID}/characters/{sourceCharacterID}/clone` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/clone_character.md](character/clone_character.md) |
| `DeleteCharacter` | `delete_character` | Mutation | `character` | implemented | transport-exposed — `DELETE /api/v1/save-sessions/{saveSessionID}/characters/{characterID}` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/delete_character.md](character/delete_character.md) |
| `SetCharacterActive` | `set_character_active` | Mutation | `character` | implemented | transport-exposed — `PATCH /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/active` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/set_character_active.md](character/set_character_active.md) |
| `SetCharacterName` | `set_character_name` | Mutation | `character` | implemented | transport-exposed — `PATCH /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/name` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/set_character_name.md](character/set_character_name.md) |
| `SetCharacterGender` | `set_character_gender` | Mutation | `character` | implemented | transport-exposed — `PATCH /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/gender` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/set_character_gender.md](character/set_character_gender.md) |
| `SetCharacterRunes` | `set_character_runes` | Mutation | `character` | implemented | transport-exposed — `PATCH /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/runes` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/set_character_runes.md](character/set_character_runes.md) |
| `GetCharacterStats` | `get_character_stats` | Getter | `character` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/stats` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/get_character_stats.md](character/get_character_stats.md) |
| `SetCharacterStats` | `set_character_stats` | Mutation | `character` | implemented (eight attributes, recalculated level, SoulMemory floor) | transport-exposed — `PATCH /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/stats` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/set_character_stats.md](character/set_character_stats.md) |
| `SetCharacterStartingClass` | `set_character_starting_class` | Mutation | `character` | implemented (confirmed destructive class reset: exact class base attributes and base level, preserved SoulMemory and held runes, synchronised class and level copies) | transport-exposed — `PATCH /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/starting-class` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/set_character_starting_class.md](character/set_character_starting_class.md) |
| `GetCharacterAppearance` | `get_character_appearance` | Getter | `character` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/appearance` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/get_character_appearance.md](character/get_character_appearance.md) |
| `SetCharacterAppearance` | `set_character_appearance` | Mutation | `character` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/appearance` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/set_character_appearance.md](character/set_character_appearance.md) |
| `GetUndoState` | `get_undo_state` | Getter | `character` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/undo` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/get_undo_state.md](character/get_undo_state.md) |
| `UndoCharacterChanges` | `undo_character_changes` | Mutation | `character` | implemented (one undo point per session, restoring the slot data, activity flag and profile summary) | transport-exposed — `POST /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/undo` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/undo_character_changes.md](character/undo_character_changes.md) |
| `GetEquipment` | `get_equipment` | Getter | `equipment` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/equipment` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/get_equipment.md](equipment/get_equipment.md) |
| `GetCharacterLoadout` | `get_character_loadout` | Getter | `equipment` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/loadout` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/get_character_loadout.md](equipment/get_character_loadout.md) |
| `SetEquippedArmaments` | `set_equipped_armaments` | Mutation | `equipment` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/equipped-armaments` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/set_equipped_armaments.md](equipment/set_equipped_armaments.md) |
| `SetEquippedArmor` | `set_equipped_armor` | Mutation | `equipment` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/equipped-armor` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/set_equipped_armor.md](equipment/set_equipped_armor.md) |
| `GetQuickItems` | `get_quick_items` | Getter | `equipment` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/quick-items` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/get_quick_items.md](equipment/get_quick_items.md) |
| `SetQuickItems` | `set_quick_items` | Mutation | `equipment` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/quick-items` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/set_quick_items.md](equipment/set_quick_items.md) |
| `GetPouchItems` | `get_pouch_items` | Getter | `equipment` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/pouch-items` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/get_pouch_items.md](equipment/get_pouch_items.md) |
| `SetPouchItems` | `set_pouch_items` | Mutation | `equipment` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/pouch-items` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/set_pouch_items.md](equipment/set_pouch_items.md) |
| `GetPhysickMixture` | `get_physick_mixture` | Getter | `equipment` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/physick-mixture` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/get_physick_mixture.md](equipment/get_physick_mixture.md) |
| `SetPhysickMixture` | `set_physick_mixture` | Mutation | `equipment` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/physick-mixture` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/set_physick_mixture.md](equipment/set_physick_mixture.md) |
| `GetEquippedSpells` | `get_equipped_spells` | Getter | `equipment` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/equipped-spells` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/get_equipped_spells.md](equipment/get_equipped_spells.md) |
| `SetEquippedSpells` | `set_equipped_spells` | Mutation | `equipment` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/equipped-spells` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/set_equipped_spells.md](equipment/set_equipped_spells.md) |
| `SetEquippedTalismans` | `set_equipped_talismans` | Mutation | `equipment` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/equipped-talismans` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/set_equipped_talismans.md](equipment/set_equipped_talismans.md) |
| `GetEquipmentCandidates` | `get_equipment_candidates` | Getter | `equipment` | implemented | transport-exposed — Wails bridge method `GetEquipmentCandidates`; no local explorer route, because no explorer surface consumes it | [equipment/get_equipment_candidates.md](equipment/get_equipment_candidates.md) |
| `GetInventory` | `get_inventory` | Getter | `inventory` | implemented (resolved ItemDocument identities) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/inventory` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/get_inventory.md](inventory/get_inventory.md) |
| `GetStorage` | `get_storage` | Getter | `inventory` | implemented (resolved ItemDocument identities) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/storage` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/get_storage.md](inventory/get_storage.md) |
| `GetItemCapacity` | `get_item_capacity` | Getter | `inventory` | implemented (non-reserving common-container and GaItemData preflight) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/item-capacity` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/get_item_capacity.md](inventory/get_item_capacity.md) |
| `GetOwnedItem` | `get_owned_item` | Getter | `inventory` | implemented (resolved ItemDocument identity of one owned instance) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/get_owned_item.md](inventory/get_owned_item.md) |
| `GetOwnedItems` | `get_owned_items` | Getter | `inventory` | implemented (authoritative container page: catalog-resolved rows, profile limits and per-record allowed mutations) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [inventory/get_owned_items.md](inventory/get_owned_items.md) |
| `AddItemToInventory` | `add_item_to_inventory` | Mutation | `inventory` | implemented (adds one goods or talisman resource to the common section, in the session's private snapshot) | transport-exposed — `POST /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/inventory/items` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/add_item_to_inventory.md](inventory/add_item_to_inventory.md) |
| `AddItemToStorage` | `add_item_to_storage` | Mutation | `inventory` | implemented (adds one depositable goods or talisman resource to common Storage, in the session's private snapshot) | transport-exposed — `POST /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/storage/items` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/add_item_to_storage.md](inventory/add_item_to_storage.md) |
| `AddItemsToContainers` | `add_items_to_containers` | Mutation | `inventory` | implemented (atomic batch add into common Inventory, common Storage or both) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [inventory/add_items_to_containers.md](inventory/add_items_to_containers.md) |
| `SetOwnedItemQuantity` | `set_owned_item_quantity` | Mutation | `inventory` | implemented (quantity of one owned instance, in the session's private snapshot) | transport-exposed — `PATCH /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}/quantity` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/set_owned_item_quantity.md](inventory/set_owned_item_quantity.md) |
| `SetInventoryOrder` | `set_inventory_order` | Mutation | `inventory` | implemented (complete supported common Inventory order, without moving physical rows) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/inventory/order` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/set_inventory_order.md](inventory/set_inventory_order.md) |
| `ReorderInventoryItems` | `reorder_inventory_items` | Mutation | `inventory` | implemented (anchored group move inside the supported Inventory order) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [inventory/reorder_inventory_items.md](inventory/reorder_inventory_items.md) |
| `SetStorageOrder` | `set_storage_order` | Mutation | `inventory` | implemented (complete supported common Storage order, without moving physical rows) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/storage/order` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/set_storage_order.md](inventory/set_storage_order.md) |
| `MoveOwnedItemToInventory` | `move_owned_item_to_inventory` | Mutation | `inventory` | implemented (moves one common Storage record into logical common Inventory order) | transport-exposed — `POST /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}/move-to-inventory` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/move_owned_item_to_inventory.md](inventory/move_owned_item_to_inventory.md) |
| `MoveOwnedItemToStorage` | `move_owned_item_to_storage` | Mutation | `inventory` | implemented (moves one common Inventory record to common Storage) | transport-exposed — `POST /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}/move-to-storage` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/move_owned_item_to_storage.md](inventory/move_owned_item_to_storage.md) |
| `MoveOwnedItemsToInventory` | `move_owned_items_to_inventory` | Mutation | `inventory` | implemented (atomic batch move of common Storage records into common Inventory) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [inventory/move_owned_items_to_inventory.md](inventory/move_owned_items_to_inventory.md) |
| `MoveOwnedItemsToStorage` | `move_owned_items_to_storage` | Mutation | `inventory` | implemented (atomic batch move of common Inventory records into common Storage) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [inventory/move_owned_items_to_storage.md](inventory/move_owned_items_to_storage.md) |
| `SetWeaponUpgradeLevel` | `set_weapon_upgrade_level` | Mutation | `inventory` | implemented (upgrade level of one owned weapon, preserving its base and affinity) | transport-exposed — `PATCH /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}/upgrade-level` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/set_weapon_upgrade_level.md](inventory/set_weapon_upgrade_level.md) |
| `SetSpiritAshUpgradeLevel` | `set_spirit_ash_upgrade_level` | Mutation | `inventory` | implemented (upgrade level of one owned Spirit Ash) | transport-exposed — `PATCH /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}/spirit-ash-upgrade-level` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/set_spirit_ash_upgrade_level.md](inventory/set_spirit_ash_upgrade_level.md) |
| `SetWeaponInfusion` | `set_weapon_infusion` | Mutation | `inventory` | implemented (affinity of one owned weapon, preserving its upgrade level and Ash of War) | transport-exposed — `PATCH /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}/infusion` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/set_weapon_infusion.md](inventory/set_weapon_infusion.md) |
| `SetWeaponAshOfWar` | `set_weapon_ash_of_war` | Mutation | `inventory` | implemented (mounts an existing free Ash of War copy or removes the custom Ash without allocation or repacking) | transport-exposed — `PATCH /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}/ash-of-war` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/set_weapon_ash_of_war.md](inventory/set_weapon_ash_of_war.md) |
| `RemoveOwnedItem` | `remove_owned_item` | Mutation | `inventory` | implemented (removes one owned instance from the session's private snapshot) | transport-exposed — `DELETE /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/remove_owned_item.md](inventory/remove_owned_item.md) |
| `RemoveOwnedItems` | `remove_owned_items` | Mutation | `inventory` | implemented (atomic batch removal of several owned instances) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [inventory/remove_owned_items.md](inventory/remove_owned_items.md) |
| `GetGestures` | `get_gestures` | Getter | `world` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/gestures` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/get_gestures.md](world/get_gestures.md) |
| `SetGestureUnlocked` | `set_gesture_unlocked` | Mutation | `world` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/gestures/unlock` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/set_gesture_unlocked.md](world/set_gesture_unlocked.md) |
| `GetCookbooks` | `get_cookbooks` | Getter | `world` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/cookbooks` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/get_cookbooks.md](world/get_cookbooks.md) |
| `GetBellBearings` | `get_bell_bearings` | Getter | `world` | implemented (acquisition event flag) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/bell-bearings` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/get_bell_bearings.md](world/get_bell_bearings.md) |
| `GetWhetblades` | `get_whetblades` | Getter | `world` | implemented (event flag or owned goods item) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/whetblades` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/get_whetblades.md](world/get_whetblades.md) |
| `SetCookbookUnlocked` | `set_cookbook_unlocked` | Mutation | `world` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/cookbooks/unlock` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/set_cookbook_unlocked.md](world/set_cookbook_unlocked.md) |
| `SetBellBearingUnlocked` | `set_bell_bearing_unlocked` | Mutation | `world` | implemented (acquisition flag plus Bell Bearing consumption) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/bell-bearings/unlock` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/set_bell_bearing_unlocked.md](world/set_bell_bearing_unlocked.md) |
| `GetColosseums` | `get_colosseums` | Getter | `world` | implemented (activation event flag) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/colosseums` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/get_colosseums.md](world/get_colosseums.md) |
| `GetRegions` | `get_regions` | Getter | `world` | implemented (`UnlockedRegions` membership) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/regions` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/get_regions.md](world/get_regions.md) |
| `GetSummoningPools` | `get_summoning_pools` | Getter | `world` | implemented (activation event flag, block 670) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/summoning-pools` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/get_summoning_pools.md](world/get_summoning_pools.md) |
| `GetGraces` | `get_graces` | Getter | `world` | implemented (visit event flag, blocks 71-74 and 76) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/graces` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/get_graces.md](world/get_graces.md) |
| `GetBosses` | `get_bosses` | Getter | `world` | implemented (synchronized defeat event flag, block 9) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/bosses` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/get_bosses.md](world/get_bosses.md) |
| `GetMapRegions` | `get_map_regions` | Getter | `world` | implemented (safe map visibility event flags, block 62) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/map-regions` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/get_map_regions.md](world/get_map_regions.md) |
| `GetTutorials` | `get_tutorials` | Getter | `world` | implemented (`TutorialData` membership) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/tutorials` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/get_tutorials.md](world/get_tutorials.md) |
| `SetBossDefeated` | `set_boss_defeated` | Mutation | `world` | implemented (synchronized defeat event flag, block 9) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/bosses/defeat` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/set_boss_defeated.md](world/set_boss_defeated.md) |
| `SetGraceVisited` | `set_grace_visited` | Mutation | `world` | implemented (visit event flag, blocks 71-74 and 76, plus the private door flag and the confirmed Gatefront companion set) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/graces/visit` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/set_grace_visited.md](world/set_grace_visited.md) |
| `SetColosseumUnlocked` | `set_colosseum_unlocked` | Mutation | `world` | implemented (confirmed four-flag arena set, blocks 6, 60, 62, 69 and 710, plus the three global SET-only flags) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/colosseums/unlock` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/set_colosseum_unlocked.md](world/set_colosseum_unlocked.md) |
| `SetRegionUnlocked` | `set_region_unlocked` | Mutation | `world` | implemented (`UnlockedRegions` membership and slot rebuild) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/regions/unlock` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/set_region_unlocked.md](world/set_region_unlocked.md) |
| `SetMapRegionRevealed` | `set_map_region_revealed` | Mutation | `world` | implemented (safe visibility flag plus the catalog-linked Map Fragment in InventoryHeld) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/map-regions/reveal` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/set_map_region_revealed.md](world/set_map_region_revealed.md) |
| `SetFogOfWarRemoved` | `set_fog_of_war_removed` | Mutation | `world` | implemented (`removed: true` only — fills the confirmed 2099-byte global Fog of War bitfield with 0xFF) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/fog-of-war` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/set_fog_of_war_removed.md](world/set_fog_of_war_removed.md) |
| `SetSummoningPoolActivated` | `set_summoning_pool_activated` | Mutation | `world` | implemented (activation event flag, block 670) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/summoning-pools/activate` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/set_summoning_pool_activated.md](world/set_summoning_pool_activated.md) |
| `SetWhetbladeUnlocked` | `set_whetblade_unlocked` | Mutation | `world` | implemented (Inventory item, main and affinity flags, shared AoW menu flag) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/whetblades/unlock` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/set_whetblade_unlocked.md](world/set_whetblade_unlocked.md) |
| `GetWorldMutationCapabilities` | `get_world_mutation_capabilities` | Getter | `world` | implemented (static list of the 15 World mutations with the SaveEngine risk contract of each) | transport-exposed — `GET /api/v1/world/mutation-capabilities` of the local explorer; it names no save session, so it is registered in every mode | [world/get_world_mutation_capabilities.md](world/get_world_mutation_capabilities.md) |
| `GetSpectralSteedAttires` | `get_spectral_steed_attires` | Getter | `world` | implemented (mutually exclusive appearance event flags plus Inventory ownership) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/spectral-steed-attires` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/get_spectral_steed_attires.md](world/get_spectral_steed_attires.md) |
| `SetSpectralSteedAttire` | `set_spectral_steed_attire` | Mutation | `world` | implemented (atomic mutually exclusive appearance event flags; requires the attire item, never adds it) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/spectral-steed-attires/select` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/set_spectral_steed_attire.md](world/set_spectral_steed_attire.md) |
| `LockAllSpectralSteedAttires` | `lock_all_spectral_steed_attires` | Mutation | `world` | implemented (atomic removal of the three attire Inventory records plus the default appearance flag) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/spectral-steed-attires/lock-all` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/lock_all_spectral_steed_attires.md](world/lock_all_spectral_steed_attires.md) |
| `GetQuests` | `get_quests` | Getter | `world` | implemented (canonical event flag plan match per supported step; no current step and no transition graph) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/quests` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/get_quests.md](world/get_quests.md) |
| `SetQuestStep` | `set_quest_step` | Mutation | `world` | implemented (curated quest resource, 908 supported steps, atomic canonical event flags plan) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/quests/step` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/set_quest_step.md](world/set_quest_step.md) |
| `SetTutorialUnlocked` | `set_tutorial_unlocked` | Mutation | `world` | implemented (`TutorialData` membership, in-place write of count and ID array) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/tutorials/unlock` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/set_tutorial_unlocked.md](world/set_tutorial_unlocked.md) |
| `GetFavoritePresets` | `get_favorite_presets` | Getter | `favorites` | implemented (occupancy state of the 15 global Mirror Favorites slots in UserData10) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/favorite-presets` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [favorites/get_favorite_presets.md](favorites/get_favorite_presets.md) |
| `SetFavoritePreset` | `set_favorite_preset` | Mutation | `favorites` | implemented (writes one 0x130-byte Mirror Favorites slot in UserData10 from character appearance) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/favorite-presets/{favoriteSlotID}` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [favorites/set_favorite_preset.md](favorites/set_favorite_preset.md) |
| `DeleteFavoritePreset` | `delete_favorite_preset` | Mutation | `favorites` | implemented (clears one active 0x130-byte Mirror Favorites slot in UserData10) | transport-exposed — `DELETE /api/v1/save-sessions/{saveSessionID}/favorite-presets/{favoriteSlotID}` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [favorites/delete_favorite_preset.md](favorites/delete_favorite_preset.md) |
| `ApplyFavoritePreset` | `apply_favorite_preset` | Mutation | `favorites` | implemented (applies one active Mirror Favorites preset in UserData10 to an active character) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/appearance/favorite-preset` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [favorites/apply_favorite_preset.md](favorites/apply_favorite_preset.md) |
| `GetBuildTemplates` | `get_build_templates` | Getter | `templates` | implemented (lightweight library index metadata) | transport-exposed — `GET /api/v1/build-templates` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [templates/get_build_templates.md](templates/get_build_templates.md) |
| `GetBuildTemplate` | `get_build_template` | Getter | `templates` | implemented (one complete build template) | transport-exposed — `GET /api/v1/build-templates/{templateID}` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [templates/get_build_template.md](templates/get_build_template.md) |
| `DeleteBuildTemplate` | `delete_build_template` | Mutation | `templates` | implemented (revision-checked removal of one library entry, then its payload) | transport-exposed — `DELETE /api/v1/build-templates/{templateID}` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [templates/delete_build_template.md](templates/delete_build_template.md) |
| `UpdateBuildTemplate` | `update_build_template` | Mutation | `templates` | implemented (revision-checked update of metadata and/or content) | transport-exposed — `PUT /api/v1/build-templates/{templateID}` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [templates/update_build_template.md](templates/update_build_template.md) |
| `CreateBuildTemplate` | `create_build_template` | Mutation | `templates` | implemented (constructs schema v2 template from character data selection and writes to library) | transport-exposed — `POST /api/v1/build-templates` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [templates/create_build_template.md](templates/create_build_template.md) |
| `GetBuildTemplatePreview` | `get_build_template_preview` | Getter | `templates` | implemented (builds non-mutating preview plan of applying a template to a character) | transport-exposed — `POST /api/v1/build-templates/{templateID}/preview` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [templates/get_build_template_preview.md](templates/get_build_template_preview.md) |
| `ApplyBuildTemplate` | `apply_build_template` | Mutation | `templates` | implemented (atomically applies a validated build template plan to a character) | transport-exposed — `POST /api/v1/build-templates/{templateID}/apply` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [templates/apply_build_template.md](templates/apply_build_template.md) |
| `ImportBuildTemplate` | `import_build_template` | Mutation | `templates` | implemented (validates and stores a locally picked Build Template document; no URL and no network) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [templates/import_build_template.md](templates/import_build_template.md) |
| `GetDeploymentTargets` | `get_deployment_targets` | Getter | `deployment` | implemented (configured targets with their transfer capability and SSH host key trust state) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [deployment/get_deployment_targets.md](deployment/get_deployment_targets.md) |
| `CreateDeploymentTarget` | `create_deployment_target` | Mutation | `deployment` | implemented (validates and stores one target; the store owns the identifier) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [deployment/create_deployment_target.md](deployment/create_deployment_target.md) |
| `UpdateDeploymentTarget` | `update_deployment_target` | Mutation | `deployment` | implemented (replaces one target; moving an SSH address drops its approved host key) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [deployment/update_deployment_target.md](deployment/update_deployment_target.md) |
| `DeleteDeploymentTarget` | `delete_deployment_target` | Mutation | `deployment` | implemented (removes the configuration entry only; touches no file on the target) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [deployment/delete_deployment_target.md](deployment/delete_deployment_target.md) |
| `TestDeploymentTarget` | `test_deployment_target` | Getter | `deployment` | implemented (non-mutating reachability, writability and game-status check) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [deployment/test_deployment_target.md](deployment/test_deployment_target.md) |
| `TrustDeploymentHostKey` | `trust_deployment_host_key` | Mutation | `deployment` | implemented | transport-exposed — Wails bridge method `TrustDeploymentHostKey`; deliberately absent from OpenAPI and Scalar | [deployment/trust_deployment_host_key.md](deployment/trust_deployment_host_key.md) |
| `ForgetDeploymentHostKey` | `forget_deployment_host_key` | Mutation | `deployment` | implemented (drops the approved fingerprint so the next connection asks again) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [deployment/forget_deployment_host_key.md](deployment/forget_deployment_host_key.md) |
| `GetDeploymentGameStatus` | `get_deployment_game_status` | Getter | `deployment` | implemented (running, stopped or unknown; never guessed from a heuristic) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [deployment/get_deployment_game_status.md](deployment/get_deployment_game_status.md) |
| `LaunchTargetGame` | `launch_target_game` | Mutation | `deployment` | implemented (runs the configured start command verbatim and reports its real outcome) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [deployment/launch_target_game.md](deployment/launch_target_game.md) |
| `CloseTargetGame` | `close_target_game` | Mutation | `deployment` | implemented (runs the configured stop command verbatim; no process found is an outcome, not a failure) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [deployment/close_target_game.md](deployment/close_target_game.md) |
| `DeployToTarget` | `deploy_to_target` | Mutation | `deployment` | implemented (shared save preparation, mandatory target backup and atomic replacement; writes no local file and never marks the session clean) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [deployment/deploy_to_target.md](deployment/deploy_to_target.md) |
| `DownloadFromTarget` | `download_from_target` | Mutation | `deployment` | implemented (waits for the target save to stabilise, then stages it locally as a temporary session source) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [deployment/download_from_target.md](deployment/download_from_target.md) |
| `GetTargetBackups` | `get_target_backups` | Getter | `deployment` | implemented (the backup library Deployment and Save Manager share; carries no size) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [deployment/get_target_backups.md](deployment/get_target_backups.md) |
| `CreateTargetBackup` | `create_target_backup` | Mutation | `deployment` | implemented (manual backup of the current target save; exempt from automatic retention) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [deployment/create_target_backup.md](deployment/create_target_backup.md) |
| `ActivateTargetBackup` | `activate_target_backup` | Mutation | `deployment` | implemented (backs up the current target save, then atomically replaces it with the selected backup) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [deployment/activate_target_backup.md](deployment/activate_target_backup.md) |
| `ClearActiveTargetBackup` | `clear_active_target_backup` | Mutation | `deployment` | implemented (metadata only; touches no file on the target) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [deployment/clear_active_target_backup.md](deployment/clear_active_target_backup.md) |
| `UpdateTargetBackup` | `update_target_backup` | Mutation | `deployment` | implemented (metadata only; replaces the tags and the description) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [deployment/update_target_backup.md](deployment/update_target_backup.md) |
| `DeleteTargetBackup` | `delete_target_backup` | Mutation | `deployment` | implemented (removes the file on the target, then forgets its record) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [deployment/delete_target_backup.md](deployment/delete_target_backup.md) |
| `DownloadTargetBackup` | `download_target_backup` | Mutation | `deployment` | implemented (copies one backup to the path the native Save As dialog returned; derives no destination) | desktop bridge only — a Wails method of `desktop.Bridge`; deliberately not an HTTP route of the local explorer and therefore absent from OpenAPI and Scalar | [deployment/download_target_backup.md](deployment/download_target_backup.md) |
| `GetSaveValidationReport` | `get_save_validation_report` | Getter | `diagnostics` | implemented (inventory, storage, stats, equipment and spell scopes with per-scope coverage; reports defects only, proposes no repair) | transport-exposed — Wails bridge method `GetSaveValidationReport`, and `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/validation-report` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [diagnostics/get_save_validation_report.md](diagnostics/get_save_validation_report.md) |
| `GetRepairPlan` | `get_repair_plan` | Getter | `diagnostics` | implemented (plans four confirmed repairs from selected validation findings, refuses every policy-dependent and unresolvable one with a reason; sealed with a derived plan token, mutates nothing) | transport-exposed — `POST /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/repair-plan` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [diagnostics/get_repair_plan.md](diagnostics/get_repair_plan.md) |
| `ApplyRepairs` | `apply_repairs` | Mutation | `diagnostics` | implemented (re-derives selected token-sealed actions and atomically applies only the confirmed repairs) | transport-exposed — `POST /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/repairs/apply` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [diagnostics/apply_repairs.md](diagnostics/apply_repairs.md) |
| `GetDiagnosticLog` | `get_diagnostic_log` | Getter | `diagnostics` | implemented (returns a safe portion of the session's structured diagnostic log, mutates nothing) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/diagnostic-log` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [diagnostics/get_diagnostic_log.md](diagnostics/get_diagnostic_log.md) |
| `GetDiagnosticEvents` | `get_diagnostic_events` | Getter | `diagnostics` | implemented (returns a safe portion of the instance-wide diagnostic event stream, needs no save session, mutates nothing) | desktop bridge only — a Wails method of `desktop.Bridge`; the stream belongs to one running desktop instance and is therefore absent from OpenAPI and Scalar | [diagnostics/get_diagnostic_events.md](diagnostics/get_diagnostic_events.md) |
| `GetDiagnosticMode` | `get_diagnostic_mode` | Getter | `diagnostics` | implemented (reports whether extended diagnostics are enabled and whether local diagnostic logging works) | desktop bridge only — a Wails method of `desktop.Bridge`; it describes one running desktop instance and is therefore absent from OpenAPI and Scalar | [diagnostics/get_diagnostic_mode.md](diagnostics/get_diagnostic_mode.md) |
| `SetDiagnosticMode` | `set_diagnostic_mode` | Mutation | `diagnostics` | implemented (turns extended diagnostics on or off for this instance; advances no save revision and produces no mutation receipt) | desktop bridge only — a Wails method of `desktop.Bridge`; the flag governs one running desktop instance and is therefore absent from OpenAPI and Scalar | [diagnostics/set_diagnostic_mode.md](diagnostics/set_diagnostic_mode.md) |

Only implemented endpoints are documented. The remaining contract-only endpoints
defined in `backend/endpoints` get a document when their runtime handler lands.
