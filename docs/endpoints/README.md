# SaveForge 2.0 endpoint documentation

This directory holds the technical documentation of the public SaveForge 2.0
backend API. Each document describes one public endpoint: what it returns or
mutates, which input it accepts, how it validates that input, which errors it
produces, and how to verify it locally.

## Naming convention

```
docs/endpoints/<domain>/<endpoint_id>.md
```

The file name is the endpoint's `EndpointID` and the directory structure mirrors
`backend/endpoints`, so `backend/endpoints/catalog/get_catalog_info.go` is
documented in `docs/endpoints/catalog/get_catalog_info.md`.

Every public endpoint has its own endpoint file in `backend/endpoints`. That
file is either `contract-only`, meaning it defines the contract without a
runtime handler, or `implemented`, meaning the handler lives there too. A
technical document is written once the runtime handler is added, so a
contract-only endpoint has an endpoint file but no document yet.

Endpoints are never grouped into a shared document, and a document is never
created for a group of endpoints.

## Getters and mutations

The public API separates reads from writes:

- **Getters** only read state. They never modify a save, the GameCatalog, or any
  application data.
- **Mutations** (setters and other mutating endpoints) perform exactly one
  explicitly named operation. A mutation may return a typed result describing
  the resulting state, but it never doubles as a getter.

A single endpoint never switches between reading and writing based on a
parameter such as `action` or `mode`.

## What this documentation is

These documents describe the backend code as it exists today. They are a
technical description, not a specification: they do not replace the endpoint
contracts in `backend/endpoints` and they do not replace the implementation.
When code and documentation disagree, the code and its tests are authoritative
and the document must be corrected.

Documents describe implemented behaviour only. They do not design future
architecture, transports, or contracts.

## Endpoint statuses

Each document reports two independent statuses.

Implementation status:

| Status | Meaning |
|---|---|
| `contract-only` | The endpoint file defines the contract (`EndpointID`, `Definition`) but no runtime handler exists yet. |
| `implemented` | A runtime handler exists in the endpoint file and is covered by tests. |

Transport status:

| Status | Meaning |
|---|---|
| `not exposed` | The endpoint is callable only as a Go function. No Wails binding, HTTP route, or CLI command reaches it. |
| `transport-exposed` | The endpoint is reachable through at least one transport (Wails, HTTP, or CLI), named in the document. |

## Documented endpoints

| Name | EndpointID | Kind | Domain | Implementation status | Transport status | Document |
|---|---|---|---|---|---|---|
| `GetApplicationInfo` | `get_application_info` | Getter | `application` | implemented | transport-exposed — `GET /api/v1/application/info` of the local explorer | [application/get_application_info.md](application/get_application_info.md) |
| `GetCatalogInfo` | `get_catalog_info` | Getter | `catalog` | implemented | transport-exposed — `GET /api/v1/catalog/info` of the local explorer | [catalog/get_catalog_info.md](catalog/get_catalog_info.md) |
| `GetResource` | `get_resource` | Getter | `catalog` | implemented | transport-exposed — `GET /api/v1/catalog/resource` of the local explorer | [catalog/get_resource.md](catalog/get_resource.md) |
| `GetItemVariants` | `get_item_variants` | Getter | `catalog` | implemented | transport-exposed — `GET /api/v1/catalog/item-variants` of the local explorer | [catalog/get_item_variants.md](catalog/get_item_variants.md) |
| `GetResourceRelations` | `get_resource_relations` | Getter | `catalog` | implemented | transport-exposed — `GET /api/v1/catalog/resource-relations` of the local explorer | [catalog/get_resource_relations.md](catalog/get_resource_relations.md) |
| `GetResources` | `get_resources` | Getter | `catalog` | implemented | transport-exposed — `GET /api/v1/catalog/resources` of the local explorer | [catalog/get_resources.md](catalog/get_resources.md) |
| `GetNetworkPresets` | `get_network_presets` | Getter | `network` | implemented | transport-exposed — `GET /api/v1/network/presets` of the local explorer | [network/get_network_presets.md](network/get_network_presets.md) |
| `GetNetworkSettings` | `get_network_settings` | Getter | `network` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/network-settings` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [network/get_network_settings.md](network/get_network_settings.md) |
| `SetNetworkSettings` | `set_network_settings` | Mutation | `network` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/network-settings` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [network/set_network_settings.md](network/set_network_settings.md) |
| `ApplyNetworkPreset` | `apply_network_preset` | Mutation | `network` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/network-settings/preset` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [network/apply_network_preset.md](network/apply_network_preset.md) |
| `GetAppearancePresets` | `get_appearance_presets` | Getter | `appearance` | implemented | transport-exposed — `GET /api/v1/appearance/presets` of the local explorer | [appearance/get_appearance_presets.md](appearance/get_appearance_presets.md) |
| `ApplyAppearancePreset` | `apply_appearance_preset` | Mutation | `appearance` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/appearance/preset` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [appearance/apply_appearance_preset.md](appearance/apply_appearance_preset.md) |
| `LoadSave` | `load_save` | Mutation | `savesession` | implemented | transport-exposed — `POST /api/v1/save-sessions` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [savesession/load_save.md](savesession/load_save.md) |
| `GetLoadedSave` | `get_loaded_save` | Getter | `savesession` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [savesession/get_loaded_save.md](savesession/get_loaded_save.md) |
| `WriteSave` | `write_save` | Mutation | `savesession` | implemented | transport-exposed — `POST /api/v1/save-sessions/{saveSessionID}/write` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [savesession/write_save.md](savesession/write_save.md) |
| `SetSaveAccountID` | `set_save_account_id` | Mutation | `savesession` | implemented (global and every active slot copy of the PC owner identifier) | transport-exposed — `PATCH /api/v1/save-sessions/{saveSessionID}/account-id` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [savesession/set_save_account_id.md](savesession/set_save_account_id.md) |
| `CloseSave` | `close_save` | Mutation | `savesession` | implemented | transport-exposed — `DELETE /api/v1/save-sessions/{saveSessionID}` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [savesession/close_save.md](savesession/close_save.md) |
| `GetSaveCharacters` | `get_save_characters` | Getter | `character` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/get_save_characters.md](character/get_save_characters.md) |
| `GetCharacterProfile` | `get_character_profile` | Getter | `character` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/profile` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/get_character_profile.md](character/get_character_profile.md) |
| `CloneCharacter` | `clone_character` | Mutation | `character` | implemented | transport-exposed — `POST /api/v1/save-sessions/{saveSessionID}/characters/{sourceCharacterID}/clone` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/clone_character.md](character/clone_character.md) |
| `DeleteCharacter` | `delete_character` | Mutation | `character` | implemented | transport-exposed — `DELETE /api/v1/save-sessions/{saveSessionID}/characters/{characterID}` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/delete_character.md](character/delete_character.md) |
| `SetCharacterActive` | `set_character_active` | Mutation | `character` | implemented | transport-exposed — `PATCH /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/active` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/set_character_active.md](character/set_character_active.md) |
| `SetCharacterName` | `set_character_name` | Mutation | `character` | implemented | transport-exposed — `PATCH /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/name` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/set_character_name.md](character/set_character_name.md) |
| `SetCharacterGender` | `set_character_gender` | Mutation | `character` | implemented | transport-exposed — `PATCH /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/gender` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/set_character_gender.md](character/set_character_gender.md) |
| `SetCharacterRunes` | `set_character_runes` | Mutation | `character` | implemented | transport-exposed — `PATCH /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/runes` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/set_character_runes.md](character/set_character_runes.md) |
| `GetCharacterStats` | `get_character_stats` | Getter | `character` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/stats` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/get_character_stats.md](character/get_character_stats.md) |
| `SetCharacterStats` | `set_character_stats` | Mutation | `character` | implemented (eight attributes, recalculated level, SoulMemory floor) | transport-exposed — `PATCH /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/stats` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/set_character_stats.md](character/set_character_stats.md) |
| `GetCharacterAppearance` | `get_character_appearance` | Getter | `character` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/appearance` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/get_character_appearance.md](character/get_character_appearance.md) |
| `SetCharacterAppearance` | `set_character_appearance` | Mutation | `character` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/appearance` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/set_character_appearance.md](character/set_character_appearance.md) |
| `GetUndoState` | `get_undo_state` | Getter | `character` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/undo` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/get_undo_state.md](character/get_undo_state.md) |
| `UndoCharacterChanges` | `undo_character_changes` | Mutation | `character` | implemented (one undo point per session, restoring the slot data, activity flag and profile summary) | transport-exposed — `POST /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/undo` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [character/undo_character_changes.md](character/undo_character_changes.md) |
| `GetEquipment` | `get_equipment` | Getter | `equipment` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/equipment` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/get_equipment.md](equipment/get_equipment.md) |
| `SetEquippedArmaments` | `set_equipped_armaments` | Mutation | `equipment` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/equipped-armaments` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/set_equipped_armaments.md](equipment/set_equipped_armaments.md) |
| `SetEquippedArmor` | `set_equipped_armor` | Mutation | `equipment` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/equipped-armor` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/set_equipped_armor.md](equipment/set_equipped_armor.md) |
| `GetQuickItems` | `get_quick_items` | Getter | `equipment` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/quick-items` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/get_quick_items.md](equipment/get_quick_items.md) |
| `SetQuickItems` | `set_quick_items` | Mutation | `equipment` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/quick-items` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/set_quick_items.md](equipment/set_quick_items.md) |
| `GetPouchItems` | `get_pouch_items` | Getter | `equipment` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/pouch-items` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/get_pouch_items.md](equipment/get_pouch_items.md) |
| `SetPouchItems` | `set_pouch_items` | Mutation | `equipment` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/pouch-items` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/set_pouch_items.md](equipment/set_pouch_items.md) |
| `GetPhysickMixture` | `get_physick_mixture` | Getter | `equipment` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/physick-mixture` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/get_physick_mixture.md](equipment/get_physick_mixture.md) |
| `SetPhysickMixture` | `set_physick_mixture` | Mutation | `equipment` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/physick-mixture` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/set_physick_mixture.md](equipment/set_physick_mixture.md) |
| `GetEquippedSpells` | `get_equipped_spells` | Getter | `equipment` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/equipped-spells` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/get_equipped_spells.md](equipment/get_equipped_spells.md) |
| `SetEquippedSpells` | `set_equipped_spells` | Mutation | `equipment` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/equipped-spells` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/set_equipped_spells.md](equipment/set_equipped_spells.md) |
| `SetEquippedTalismans` | `set_equipped_talismans` | Mutation | `equipment` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/equipped-talismans` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [equipment/set_equipped_talismans.md](equipment/set_equipped_talismans.md) |
| `GetInventory` | `get_inventory` | Getter | `inventory` | implemented (resolved ItemDocument identities) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/inventory` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/get_inventory.md](inventory/get_inventory.md) |
| `GetStorage` | `get_storage` | Getter | `inventory` | implemented (resolved ItemDocument identities) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/storage` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/get_storage.md](inventory/get_storage.md) |
| `GetItemCapacity` | `get_item_capacity` | Getter | `inventory` | implemented (non-reserving common-container and GaItemData preflight) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/item-capacity` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/get_item_capacity.md](inventory/get_item_capacity.md) |
| `GetOwnedItem` | `get_owned_item` | Getter | `inventory` | implemented (resolved ItemDocument identity of one owned instance) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/get_owned_item.md](inventory/get_owned_item.md) |
| `AddItemToInventory` | `add_item_to_inventory` | Mutation | `inventory` | implemented (adds one goods or talisman resource to the common section, in the session's private snapshot) | transport-exposed — `POST /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/inventory/items` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/add_item_to_inventory.md](inventory/add_item_to_inventory.md) |
| `AddItemToStorage` | `add_item_to_storage` | Mutation | `inventory` | implemented (adds one depositable goods or talisman resource to common Storage, in the session's private snapshot) | transport-exposed — `POST /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/storage/items` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/add_item_to_storage.md](inventory/add_item_to_storage.md) |
| `SetOwnedItemQuantity` | `set_owned_item_quantity` | Mutation | `inventory` | implemented (quantity of one owned instance, in the session's private snapshot) | transport-exposed — `PATCH /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}/quantity` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/set_owned_item_quantity.md](inventory/set_owned_item_quantity.md) |
| `SetInventoryOrder` | `set_inventory_order` | Mutation | `inventory` | implemented (complete supported common Inventory order, without moving physical rows) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/inventory/order` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/set_inventory_order.md](inventory/set_inventory_order.md) |
| `SetStorageOrder` | `set_storage_order` | Mutation | `inventory` | implemented (complete supported common Storage order, without moving physical rows) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/storage/order` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/set_storage_order.md](inventory/set_storage_order.md) |
| `MoveOwnedItemToInventory` | `move_owned_item_to_inventory` | Mutation | `inventory` | implemented (moves one common Storage record into logical common Inventory order) | transport-exposed — `POST /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}/move-to-inventory` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/move_owned_item_to_inventory.md](inventory/move_owned_item_to_inventory.md) |
| `MoveOwnedItemToStorage` | `move_owned_item_to_storage` | Mutation | `inventory` | implemented (moves one common Inventory record to common Storage) | transport-exposed — `POST /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}/move-to-storage` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/move_owned_item_to_storage.md](inventory/move_owned_item_to_storage.md) |
| `SetWeaponUpgradeLevel` | `set_weapon_upgrade_level` | Mutation | `inventory` | implemented (upgrade level of one owned weapon, preserving its base and affinity) | transport-exposed — `PATCH /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}/upgrade-level` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/set_weapon_upgrade_level.md](inventory/set_weapon_upgrade_level.md) |
| `SetSpiritAshUpgradeLevel` | `set_spirit_ash_upgrade_level` | Mutation | `inventory` | implemented (upgrade level of one owned Spirit Ash) | transport-exposed — `PATCH /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}/spirit-ash-upgrade-level` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/set_spirit_ash_upgrade_level.md](inventory/set_spirit_ash_upgrade_level.md) |
| `SetWeaponInfusion` | `set_weapon_infusion` | Mutation | `inventory` | implemented (affinity of one owned weapon, preserving its upgrade level and Ash of War) | transport-exposed — `PATCH /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}/infusion` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/set_weapon_infusion.md](inventory/set_weapon_infusion.md) |
| `SetWeaponAshOfWar` | `set_weapon_ash_of_war` | Mutation | `inventory` | implemented (mounts an existing free Ash of War copy or removes the custom Ash without allocation or repacking) | transport-exposed — `PATCH /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}/ash-of-war` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/set_weapon_ash_of_war.md](inventory/set_weapon_ash_of_war.md) |
| `RemoveOwnedItem` | `remove_owned_item` | Mutation | `inventory` | implemented (removes one owned instance from the session's private snapshot) | transport-exposed — `DELETE /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [inventory/remove_owned_item.md](inventory/remove_owned_item.md) |
| `GetGestures` | `get_gestures` | Getter | `world` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/gestures` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/get_gestures.md](world/get_gestures.md) |
| `SetGestureUnlocked` | `set_gesture_unlocked` | Mutation | `world` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/gestures/unlock` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/set_gesture_unlocked.md](world/set_gesture_unlocked.md) |
| `GetCookbooks` | `get_cookbooks` | Getter | `world` | implemented | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/cookbooks` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/get_cookbooks.md](world/get_cookbooks.md) |
| `GetBellBearings` | `get_bell_bearings` | Getter | `world` | implemented (acquisition event flag) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/bell-bearings` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/get_bell_bearings.md](world/get_bell_bearings.md) |
| `GetWhetblades` | `get_whetblades` | Getter | `world` | implemented (event flag or owned goods item) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/whetblades` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/get_whetblades.md](world/get_whetblades.md) |
| `SetCookbookUnlocked` | `set_cookbook_unlocked` | Mutation | `world` | implemented | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/cookbooks/unlock` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/set_cookbook_unlocked.md](world/set_cookbook_unlocked.md) |
| `SetBellBearingUnlocked` | `set_bell_bearing_unlocked` | Mutation | `world` | implemented (acquisition flag plus Bell Bearing consumption) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/bell-bearings/unlock` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/set_bell_bearing_unlocked.md](world/set_bell_bearing_unlocked.md) |
| `GetColosseums` | `get_colosseums` | Getter | `world` | implemented (activation event flag) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/colosseums` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/get_colosseums.md](world/get_colosseums.md) |
| `GetRegions` | `get_regions` | Getter | `world` | implemented (`UnlockedRegions` membership) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/regions` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/get_regions.md](world/get_regions.md) |
| `GetSummoningPools` | `get_summoning_pools` | Getter | `world` | implemented (activation event flag, block 670) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/summoning-pools` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/get_summoning_pools.md](world/get_summoning_pools.md) |
| `GetGraces` | `get_graces` | Getter | `world` | implemented (visit event flag, blocks 71-74 and 76) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/graces` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/get_graces.md](world/get_graces.md) |
| `GetBosses` | `get_bosses` | Getter | `world` | implemented (synchronized defeat event flag, block 9) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/bosses` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/get_bosses.md](world/get_bosses.md) |
| `GetMapRegions` | `get_map_regions` | Getter | `world` | implemented (safe map visibility event flags, block 62) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/map-regions` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/get_map_regions.md](world/get_map_regions.md) |
| `GetTutorials` | `get_tutorials` | Getter | `world` | implemented (`TutorialData` membership) | transport-exposed — `GET /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/tutorials` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/get_tutorials.md](world/get_tutorials.md) |
| `SetBossDefeated` | `set_boss_defeated` | Mutation | `world` | implemented (synchronized defeat event flag, block 9) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/bosses/defeat` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/set_boss_defeated.md](world/set_boss_defeated.md) |
| `SetGraceVisited` | `set_grace_visited` | Mutation | `world` | implemented (visit event flag, blocks 71-74 and 76, plus the private door flag and the confirmed Gatefront companion set) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/graces/visit` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/set_grace_visited.md](world/set_grace_visited.md) |
| `SetColosseumUnlocked` | `set_colosseum_unlocked` | Mutation | `world` | implemented (confirmed four-flag arena set, blocks 6, 60, 62, 69 and 710, plus the three global SET-only flags) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/colosseums/unlock` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/set_colosseum_unlocked.md](world/set_colosseum_unlocked.md) |
| `SetMapRegionRevealed` | `set_map_region_revealed` | Mutation | `world` | implemented (safe visibility flag plus the catalog-linked Map Fragment in InventoryHeld) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/map-regions/reveal` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/set_map_region_revealed.md](world/set_map_region_revealed.md) |
| `SetFogOfWarRemoved` | `set_fog_of_war_removed` | Mutation | `world` | implemented (`removed: true` only — fills the confirmed 2099-byte global Fog of War bitfield with 0xFF) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/fog-of-war` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/set_fog_of_war_removed.md](world/set_fog_of_war_removed.md) |
| `SetSummoningPoolActivated` | `set_summoning_pool_activated` | Mutation | `world` | implemented (activation event flag, block 670) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/summoning-pools/activate` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/set_summoning_pool_activated.md](world/set_summoning_pool_activated.md) |
| `SetWhetbladeUnlocked` | `set_whetblade_unlocked` | Mutation | `world` | implemented (Inventory item, main and affinity flags, shared AoW menu flag) | transport-exposed — `PUT /api/v1/save-sessions/{saveSessionID}/characters/{characterID}/whetblades/unlock` of the local explorer, registered only when the explorer runs without `-allow-external-bind` | [world/set_whetblade_unlocked.md](world/set_whetblade_unlocked.md) |

Only implemented endpoints are documented. The remaining contract-only endpoints
defined in `backend/endpoints` get a document when their runtime handler lands.
