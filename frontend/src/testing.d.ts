// jest-dom must augment Vitest's `Assertion` interface, not the global jest
// namespace. Importing the `vitest` entry keeps a single matcher augmentation.
/// <reference types="@testing-library/jest-dom/vitest" />
