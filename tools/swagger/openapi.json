{
  "openapi": "3.0.3",
  "info": {
    "title": "SaveForge 2.0 GameCatalog getters",
    "version": "1.0.0",
    "description": "Local, read-only HTTP surface over the currently implemented GameCatalog getters. Every operation calls exactly one Go getter and serialises its typed result. Getters whose contract file declares 'contract definition only' are not exposed."
  },
  "servers": [
    {
      "url": "http://127.0.0.1:8788",
      "description": "Loopback address of the local API host. Every client, including the Scalar documentation portal on another origin, calls this absolute address; a relative server would resolve against the portal, which serves no API."
    }
  ],
  "tags": [
    {
      "name": "Application",
      "description": "Backend version, supported GameCatalog schemas and declared capabilities."
    },
    {
      "name": "Catalog",
      "description": "Read-only GameCatalog getters over resources, relations and item variants."
    },
    {
      "name": "Network",
      "description": "Network parameter presets stored in the catalog data."
    },
    {
      "name": "Appearance",
      "description": "Appearance preset metadata stored in the catalog data."
    },
    {
      "name": "Save sessions",
      "description": "Read-only save-session lifecycle. Registered only when the explorer binds a loopback address."
    },
    {
      "name": "Character",
      "description": "Read-only character getters over an open save session. Registered only when the explorer binds a loopback address."
    },
    {
      "name": "Inventory",
      "description": "Read-only Inventory getters over an open save session. Registered only when the explorer binds a loopback address."
    },
    {
      "name": "World",
      "description": "Read-only world and progression getters over an open save session. Registered only when the explorer binds a loopback address."
    },
    {
      "name": "Explorer",
      "description": "Routes of the explorer process itself, not part of the endpoint surface."
    }
  ],
  "paths": {
    "/api/v1/application/info": {
      "get": {
        "operationId": "getApplicationInfo",
        "tags": ["Application"],
        "summary": "GetApplicationInfo",
        "description": "Returns the application version supplied by the backend caller, the supported GameCatalog schema version range and the capabilities the backend currently declares. Takes no query parameters.",
        "responses": {
          "200": {
            "description": "Application version, supported schemas and capabilities",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GetApplicationInfoResult"
                }
              }
            }
          },
          "500": {
            "description": "The backend supplied no application version",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/catalog/info": {
      "get": {
        "operationId": "getCatalogInfo",
        "tags": ["Catalog"],
        "summary": "GetCatalogInfo",
        "description": "Returns the manifest of the loaded GameCatalog: schema version, data version, game version, validation status and the used sources.",
        "responses": {
          "200": {
            "description": "Catalog manifest",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GetCatalogInfoResult"
                }
              }
            }
          },
          "500": {
            "description": "The loaded catalog manifest is missing or invalid",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/catalog/resource": {
      "get": {
        "operationId": "getResource",
        "tags": ["Catalog"],
        "summary": "GetResource",
        "description": "Returns one full catalog resource document of whatever kind the resource is: an ItemDocument for kind item, a ColosseumDocument for kind colosseum, a RegionDocument for kind region or a SummoningPoolDocument for kind summoning_pool. The resource identity is the exact pair (kind, key); the kind is resolved first and the key only inside that kind.",
        "parameters": [
          {
            "$ref": "#/components/parameters/ResourceKind"
          },
          {
            "$ref": "#/components/parameters/ResourceKey"
          }
        ],
        "responses": {
          "200": {
            "description": "The resource document",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GetResourceResult"
                }
              }
            }
          },
          "400": {
            "description": "kind or key is missing, or the pair is unknown. The message is the one the getter returned.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/catalog/resource-relations": {
      "get": {
        "operationId": "getResourceRelations",
        "tags": ["Catalog"],
        "summary": "GetResourceRelations",
        "description": "Returns the outgoing and incoming relations of one resource identified by the exact pair (kind, key), in catalog order. Relations only: the documents of the related resources belong to GetResource. A filtered-out direction stays an empty array, never null.",
        "parameters": [
          {
            "$ref": "#/components/parameters/ResourceKind"
          },
          {
            "$ref": "#/components/parameters/ResourceKey"
          },
          {
            "$ref": "#/components/parameters/RelationType"
          },
          {
            "$ref": "#/components/parameters/RelationDirection"
          }
        ],
        "responses": {
          "200": {
            "description": "The relations of the resource",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GetResourceRelationsResult"
                }
              }
            }
          },
          "400": {
            "description": "kind or key is missing, the pair is unknown, or relationType or direction is not supported. The message is the one the getter returned.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/catalog/resources": {
      "get": {
        "operationId": "getResources",
        "tags": ["Catalog"],
        "summary": "GetResources",
        "description": "Returns one page of catalog resources reduced to the fields a list or a picker needs: kind, key, family and name. family and capability describe items only, so colosseums, regions and summoning pools carry an empty family and are never matched by either filter. Every filter is matched exactly and case-sensitively except search, which is case-insensitive on the key and on the name. An empty filter never filters. The order is the catalog order, kind first and only then key. total counts the matches before paging, and a page beyond the last one returns an empty array, never null. Full documents belong to GetResource.",
        "parameters": [
          {
            "$ref": "#/components/parameters/ResourceTypeFilter"
          },
          {
            "$ref": "#/components/parameters/ItemFamilyFilter"
          },
          {
            "$ref": "#/components/parameters/CapabilityFilter"
          },
          {
            "$ref": "#/components/parameters/EndpointIDFilter"
          },
          {
            "$ref": "#/components/parameters/ResourceSearch"
          },
          {
            "$ref": "#/components/parameters/Page"
          },
          {
            "$ref": "#/components/parameters/PageSize"
          }
        ],
        "responses": {
          "200": {
            "description": "One page of matching resources",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GetResourcesResult"
                }
              }
            }
          },
          "400": {
            "description": "page or pageSize is not an integer or is negative, resourceType, family or capability is unknown, or endpointId is set. The message is the one the getter returned, except for a non-integer page or pageSize, which the route rejects before the getter sees it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/catalog/item-variants": {
      "get": {
        "operationId": "getItemVariants",
        "tags": ["Catalog"],
        "summary": "GetItemVariants",
        "description": "Returns the variants stored in one item document, in catalog order. An item without variants returns an empty list; the base item is never synthesised into a variant.",
        "parameters": [
          {
            "$ref": "#/components/parameters/ResourceKind"
          },
          {
            "$ref": "#/components/parameters/ResourceKey"
          }
        ],
        "responses": {
          "200": {
            "description": "The item variants",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GetItemVariantsResult"
                }
              }
            }
          },
          "400": {
            "description": "kind or key is missing, kind is not item, or the pair is unknown. The message is the one the getter returned.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/network/presets": {
      "get": {
        "operationId": "getNetworkPresets",
        "tags": ["Network"],
        "summary": "GetNetworkPresets",
        "description": "Returns the network parameter presets the backend offers today. An omitted or empty presetID returns every preset in the backend order; a non-empty presetID returns exactly that preset. The values come from regulation/network_params.json loaded and validated by GameCatalog. The route reads no save.",
        "parameters": [
          {
            "name": "presetID",
            "in": "query",
            "required": false,
            "description": "Public preset identifier, matched exactly and case-sensitively. It is never trimmed, normalised or resolved through an alias. Omitted or empty means every preset. The legacy core presets (fast-invasions, light-invasions, fast-summons, fast-blue, aggressive-host) are not exposed and are rejected as unknown.",
            "schema": {
              "type": "string",
              "enum": [
                "",
                "vanilla",
                "faster-reds",
                "aggressive-reds",
                "faster-summons",
                "aggressive-summons",
                "faster-blue",
                "aggressive-blue",
                "faster-summon-host",
                "aggressive-summon-host",
                "faster-summon-guest",
                "aggressive-summon-guest",
                "faster-hunter",
                "aggressive-hunter"
              ]
            },
            "example": "faster-reds"
          }
        ],
        "responses": {
          "200": {
            "description": "The requested presets with their full parameter values",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GetNetworkPresetsResult"
                }
              }
            }
          },
          "400": {
            "description": "Unknown presetID",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/appearance/presets": {
      "get": {
        "operationId": "getAppearancePresets",
        "tags": ["Appearance"],
        "summary": "GetAppearancePresets",
        "description": "Returns the appearance presets the backend offers today, reduced to the metadata a list needs. The values come from backend/gamecatalog/data/presets/appearance.json, which GameCatalog loads and validates once together with the preview images under backend/gamecatalog/data/assets/appearance. The results keep the file order. The route reads no save and modifies nothing. It reports the asset file name only; no route serves the images yet.",
        "parameters": [
          {
            "name": "search",
            "in": "query",
            "required": false,
            "description": "Case-insensitive substring matched against the preset id and name. Omitted or empty means no filtering. The value is never trimmed and never normalised.",
            "schema": {
              "type": "string"
            },
            "example": "witcher"
          },
          {
            "name": "tags",
            "in": "query",
            "required": false,
            "description": "Comma-separated tag list. Omitted or empty means no filtering. Every listed tag must be present on a preset, so the filter has AND semantics; tags are compared exactly and case-sensitively and no individual tag is trimmed. An empty element such as tags=foo,,bar is rejected. The stored presets carry no tags today, so any non-empty filter returns an empty list.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The matching preset summaries in catalog order",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GetAppearancePresetsResult"
                }
              }
            }
          },
          "400": {
            "description": "An empty tag element in the tags parameter",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions": {
      "post": {
        "operationId": "loadSave",
        "tags": ["Save sessions"],
        "summary": "LoadSave",
        "description": "Creates a save session from a read-only snapshot of a local save file and returns its metadata. The source path and the expected platform are passed to the endpoint exactly as sent: they are never trimmed, normalised or defaulted, and the source file is never modified. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LoadSaveRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The created session",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SessionInfo"
                }
              }
            }
          },
          "400": {
            "description": "Malformed body, an unknown body field, or a source SaveEngine cannot open, recognise or validate",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}": {
      "get": {
        "operationId": "getLoadedSave",
        "tags": ["Save sessions"],
        "summary": "GetLoadedSave",
        "description": "Returns the safe metadata of an existing save session: its identifier, platform, container format and unsaved-changes state. The identifier is matched exactly by SaveEngine. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          }
        ],
        "responses": {
          "200": {
            "description": "The session metadata",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SessionInfo"
                }
              }
            }
          },
          "400": {
            "description": "An empty, unknown or already closed session identifier",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      },
      "delete": {
        "operationId": "closeSave",
        "tags": ["Save sessions"],
        "summary": "CloseSave",
        "description": "Closes an existing save session and releases its private snapshot. Only the in-memory session is removed: the source save file is never written, moved or deleted. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          }
        ],
        "responses": {
          "200": {
            "description": "The session was closed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CloseSaveResult"
                }
              }
            }
          },
          "400": {
            "description": "An empty, unknown or already closed session identifier",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/write": {
      "post": {
        "operationId": "writeSave",
        "tags": ["Save sessions"],
        "summary": "WriteSave",
        "description": "Serializes and reload-validates the current session snapshot, then atomically writes it to the explicitly specified local target. A successful write always advances saveRevision, retires every ownedItemID from the previous revision and clears unsavedChanges. A failure leaves the session and an existing target unchanged. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/WriteSaveRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The persisted session and its new revision",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WriteSaveResult"
                }
              }
            }
          },
          "400": {
            "description": "Malformed body, an unknown body field, invalid or stale revision, unknown session, invalid candidate, or target write failure",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/account-id": {
      "patch": {
        "operationId": "setSaveAccountID",
        "tags": ["Save sessions"],
        "summary": "SetSaveAccountID",
        "description": "Writes the save owner identifier into the global UserData10 copy and into the own copy of every active character slot, as one atomic plan with verification and rollback. An inactive or residual slot is never touched. PC only: a PS4 session is rejected before any account-identifier field is read or written, because PS4 carries no confirmed Steam identity. accountID is a string holding the canonical decimal representation of a uint64, because JSON and JavaScript lose the precision of large identifiers; it is never echoed back in a result, an error or a log. The change stays in the private session snapshot until a later WriteSave persists it. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetSaveAccountIDRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The session and its new revision",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetSaveAccountIDResult"
                }
              }
            }
          },
          "400": {
            "description": "Malformed body, an unknown body field, a non-canonical accountID, an invalid or stale expectedRevision, an unknown session, a PS4 session, or an unparseable active slot",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters": {
      "get": {
        "operationId": "getSaveCharacters",
        "tags": ["Character"],
        "summary": "GetSaveCharacters",
        "description": "Returns one summary per physical character slot of an existing session, always ten of them in slot order. An inactive slot reports an empty name and level 0 even when residual data remains in the file. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          }
        ],
        "responses": {
          "200": {
            "description": "The ten character slot summaries",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SaveCharacters"
                }
              }
            }
          },
          "400": {
            "description": "An empty, unknown or already closed session identifier",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/appearance": {
      "get": {
        "operationId": "getCharacterAppearance",
        "tags": ["Character"],
        "summary": "GetCharacterAppearance",
        "description": "Returns the raw appearance stored in one character slot: the raw gender and voice type, the eight model identifiers and the raw face shape, body and skin parameter blocks as number arrays. Every value is the raw stored number; nothing is validated, normalised, clamped, computed or resolved to a name. An inactive slot is a normal result whose values are all zero. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "responses": {
          "200": {
            "description": "The raw character appearance",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CharacterAppearance"
                }
              }
            }
          },
          "400": {
            "description": "A non-decimal characterID, a characterID outside the slot range, or an empty, unknown or already closed session identifier",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      },
      "put": {
        "operationId": "setCharacterAppearance",
        "tags": ["Character"],
        "summary": "SetCharacterAppearance",
        "description": "Atomically replaces the complete raw appearance of one active character under expectedRevision control. Gender must be 0 or 1, voiceType must be 0 through 5, and all four raw arrays must have their exact physical lengths. Unknown FACE bytes remain unchanged and the two confirmed dependent sex-flag bytes are reset to zero. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetCharacterAppearanceRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed complete appearance assignment",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetCharacterAppearanceResult"
                }
              }
            }
          },
          "400": {
            "description": "A malformed request, invalid appearance value or array length, invalid expectedRevision, unknown session, inactive slot, stale revision, unsupported layout, or write error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/appearance/preset": {
      "put": {
        "operationId": "applyAppearancePreset",
        "tags": ["Appearance"],
        "summary": "ApplyAppearancePreset",
        "description": "Resolves one appearance preset exactly in the loaded GameCatalog and atomically applies its complete confirmed appearance model through SetCharacterAppearance under expectedRevision control. Unknown presets and unconfirmed model mappings fail without mutation. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ApplyAppearancePresetRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The selected preset and committed complete appearance assignment",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ApplyAppearancePresetResult"
                }
              }
            }
          },
          "400": {
            "description": "A malformed request, empty or unknown presetID, unconfirmed model mapping, invalid expectedRevision, unknown session, inactive slot, stale revision, unsupported layout, or write error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/equipment": {
      "get": {
        "operationId": "getEquipment",
        "tags": ["Equipment"],
        "summary": "GetEquipment",
        "description": "Returns the raw 22-slot ChrAsmEquipment state of one character slot. Every element of slots is the uint32 stored in the save: nothing is normalised, masked, turned into a handle, stripped of its type bits or resolved against GameCatalog. The meaning of each index is documented in docs/endpoints/equipment/get_equipment.md. An inactive slot is a normal result whose slots are all zero. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "responses": {
          "200": {
            "description": "The raw character equipment",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CharacterEquipment"
                }
              }
            }
          },
          "400": {
            "description": "A non-decimal characterID, a characterID outside the slot range, an empty, unknown or already closed session identifier, or an active slot whose equipment block cannot be located inside its slot",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/equipped-armaments": {
      "put": {
        "operationId": "setEquippedArmaments",
        "tags": ["Equipment"],
        "summary": "SetEquippedArmaments",
        "description": "Atomically replaces the six hand-armament slots of one active character in left 1, right 1, left 2, right 2, left 3 and right 3 order. Each position is a revision-scoped ownedItemID for a positive weapon record in Inventory common or null. GameCatalog must confirm that every selected weapon can be equipped in the corresponding left-hand or right-hand slot, and one physical weapon record cannot occupy two positions. Null selects the existing native Unarmed record; multiple null positions may share it. The operation fails closed if that technical record is absent because it never allocates or repacks GaItem. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetEquippedArmamentsRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed complete hand-armament assignment receipt",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetEquippedArmamentsResult"
                }
              }
            }
          },
          "400": {
            "description": "Malformed input, an invalid character or revision, an invalid or stale ownedItemID, a Storage or key record, zero quantity, a wrong item family or hand slot, a duplicated physical weapon record, a missing native Unarmed record, an unresolved handle, or inconsistent existing representations",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/equipped-armor": {
      "put": {
        "operationId": "setEquippedArmor",
        "tags": ["Equipment"],
        "summary": "SetEquippedArmor",
        "description": "Atomically replaces the head, chest, arms and legs armor slots of one active character. slotAssignments contains exactly four revision-scoped ownedItemID strings or null values in that order. Every token must address a positive-quantity armor record in Inventory common with confirmed equipment capability for the exact slot. Null selects the existing native bare-body record; the operation fails closed if that technical record is absent because it never allocates or repacks GaItem. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetEquippedArmorRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed complete armor assignment receipt",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetEquippedArmorResult"
                }
              }
            }
          },
          "400": {
            "description": "Malformed input, an invalid character or revision, an invalid or stale ownedItemID, a Storage or key record, zero quantity, a wrong armor family or slot, a missing native bare-body record, an unresolved handle, or inconsistent existing representations",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/quick-items": {
      "get": {
        "operationId": "getQuickItems",
        "tags": ["Equipment"],
        "summary": "GetQuickItems",
        "description": "Returns the ten raw Quick Items records of one character slot and the raw active-slot value behind them. Every itemID, equipIndex and activeQuick is the value stored in the save: nothing is normalised, validated or resolved against GameCatalog, so 0, 0xFFFFFFFF and a negative activeQuick are reported as written. An inactive slot is a normal result whose ten records are zeroed and whose activeQuick is 0. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "responses": {
          "200": {
            "description": "The raw character quick items",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CharacterQuickItems"
                }
              }
            }
          },
          "400": {
            "description": "A non-decimal characterID, a characterID outside the slot range, an empty, unknown or already closed session identifier, or an active slot whose quick items cannot be located inside its slot",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      },
      "put": {
        "operationId": "setQuickItems",
        "tags": ["Equipment"],
        "summary": "SetQuickItems",
        "description": "Atomically replaces all ten Quick Items positions. slotAssignments contains exactly ten nullable ownedItemID strings in stored order; null clears that exact position. Every ownedItemID must resolve to an active common Inventory record with positive quantity whose catalog resource allows the quick_item slot. The active Quick Item index is preserved. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetQuickItemsRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed Quick Items assignment",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetQuickItemsResult"
                }
              }
            }
          },
          "400": {
            "description": "An invalid position count, an unowned, unequipable or zero-quantity item, duplicate items, a stale revision or an active slot whose Quick Items state is inconsistent",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/pouch-items": {
      "get": {
        "operationId": "getPouchItems",
        "tags": ["Equipment"],
        "summary": "GetPouchItems",
        "description": "Returns the six raw Pouch records of one character slot. Every itemID and equipIndex is the value stored in the save: nothing is normalised, validated or resolved against GameCatalog, so 0, 0xFFFFFFFF and values with the high bit set are reported as written. An inactive slot is a normal result whose six records are zeroed. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "responses": {
          "200": {
            "description": "The raw character pouch items",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CharacterPouchItems"
                }
              }
            }
          },
          "400": {
            "description": "A non-decimal characterID, a characterID outside the slot range, an empty, unknown or already closed session identifier, or an active slot whose pouch items cannot be located inside its slot",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      },
      "put": {
        "operationId": "setPouchItems",
        "tags": ["Equipment"],
        "summary": "SetPouchItems",
        "description": "Atomically replaces all six Pouch slot positions. slotAssignments contains exactly six nullable ownedItemID strings in stored order; null clears that exact position. Every ownedItemID must resolve to an active Inventory record with positive quantity whose catalog resource allows the pouch slot. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetPouchItemsRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed Pouch items assignment",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetPouchItemsResult"
                }
              }
            }
          },
          "400": {
            "description": "An invalid position count, an unowned, unequipable or zero-quantity item, duplicate slots, a stale revision or an active slot whose pouch items cannot be located inside its slot",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },

    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/physick-mixture": {
      "get": {
        "operationId": "getPhysickMixture",
        "tags": ["Equipment"],
        "summary": "GetPhysickMixture",
        "description": "Returns the two raw Crystal Tear identifiers of the current Flask of Wondrous Physick mixture of one character slot, in stored order. Both values are the values stored in the save: nothing is normalised, validated or resolved against GameCatalog, so 0, 0xFFFFFFFF and values with the high bit set are reported as written. An inactive slot is a normal result whose two identifiers are zeroed. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "responses": {
          "200": {
            "description": "The raw character physick mixture",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CharacterPhysickMixture"
                }
              }
            }
          },
          "400": {
            "description": "A non-decimal characterID, a characterID outside the slot range, an empty, unknown or already closed session identifier, or an active slot whose physick mixture cannot be located inside its slot",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      },
      "put": {
        "operationId": "setPhysickMixture",
        "tags": ["Equipment"],
        "summary": "SetPhysickMixture",
        "description": "Atomically replaces both Flask of Wondrous Physick positions. crystalTearResources contains exactly two nullable ResourceRef entries in stored order; null clears that exact position. Every resource must resolve to an owned goods item with a confirmed equipment capability allowing the physick slot, the same tear cannot occupy both positions, and the character must own the Flask itself. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetPhysickMixtureRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed Physick mixture assignment",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetPhysickMixtureResult"
                }
              }
            }
          },
          "400": {
            "description": "Malformed input, an unknown or ineligible resource, an unowned tear or Flask, a duplicate tear, an inactive character, a stale revision, or a Physick block that cannot be located safely",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/equipped-spells": {
      "get": {
        "operationId": "getEquippedSpells",
        "tags": ["Equipment"],
        "summary": "GetEquippedSpells",
        "description": "Returns the twelve public playable EquippedSpells records of one character slot, in stored order, together with the Memory Slots the occupied records consume and the Memory Slots the character may fill. Every record keeps the raw MagicParam ID exactly as stored, including the native empty sentinel 0xFFFFFFFF, whose record exposes no resolved resource data. An occupied record is resolved through GameCatalog as an ItemDocument of the spell family; a raw identifier that resolves to no known spell with a known name and a known Memory Slots cost is an error, never an invented value. An inactive or residual slot is a normal result with active false, twelve zero-value records and both counts zero. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "responses": {
          "200": {
            "description": "The equipped spells of the character slot",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CharacterEquippedSpells"
                }
              }
            }
          },
          "400": {
            "description": "A non-decimal characterID, a characterID outside the slot range, an empty, unknown or already closed session identifier, an active slot whose equipped spells or capacity data cannot be located inside its slot, a spell record the game never writes, or an occupied raw MagicParam ID that does not resolve to a known spell",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      },
      "put": {
        "operationId": "setEquippedSpells",
        "tags": ["Equipment"],
        "summary": "SetEquippedSpells",
        "description": "Atomically replaces the equipped spell loadout for one active character. orderedResources contains at most 12 ResourceRef entries in desired memory slot order. Every resource must resolve to a spell item with confirmed equipment capability allowing spell_memory, each spell can appear at most once, and the sum of Memory Slots costs cannot exceed the character's available capacity. An empty list clears the loadout. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetEquippedSpellsRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed spell loadout receipt",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetEquippedSpellsResult"
                }
              }
            }
          },
          "400": {
            "description": "A non-decimal characterID, a characterID outside the slot range, an empty, unknown or already closed session identifier, a stale expectedRevision, more than 12 spells, duplicate spells, an item that is not a spell, or total Memory Slots cost exceeding available capacity",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/equipped-talismans": {
      "put": {
        "operationId": "setEquippedTalismans",
        "tags": ["Equipment"],
        "summary": "SetEquippedTalismans",
        "description": "Atomically replaces the compact talisman loadout for one active character. orderedOwnedItemIDs contains at most four revision-scoped tokens in desired slot order; the list length cannot exceed the character's unlocked talisman-slot count. Every token must address a positive-quantity talisman in Inventory common with confirmed equipment capability allowing talisman. An empty list clears all four player-visible fields. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetEquippedTalismansRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed talisman loadout receipt",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetEquippedTalismansResult"
                }
              }
            }
          },
          "400": {
            "description": "Malformed input, an invalid character or revision, too many talismans for the unlocked slots, an invalid or stale ownedItemID, a duplicate or ineligible talisman, or inconsistent existing representations",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/inventory": {
      "get": {
        "operationId": "getInventory",
        "tags": ["Inventory"],
        "summary": "GetInventory",
        "description": "Returns one page of InventoryHeld records of one character slot, each resolved to an ItemDocument identity. kind and key are the canonical GameCatalog resource reference, while gameID is the exact catalog game ID resolved from the save and therefore selects an item variant where applicable. The physical native order, raw gaItemHandle and acquisitionIndex, quantity mask, ownedItemID and paging rules are preserved. Only non-empty records are returned; the two native absent sentinels 0x00000000 and 0xFFFFFFFF are skipped. An unresolved GaItem handle or unknown catalog item rejects the whole request without a partial result. An inactive or residual slot is a normal result with active false, total 0 and an empty array. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          },
          {
            "$ref": "#/components/parameters/ContainerSection"
          },
          {
            "$ref": "#/components/parameters/Page"
          },
          {
            "$ref": "#/components/parameters/PageSize"
          }
        ],
        "responses": {
          "200": {
            "description": "One page of resolved inventory records of the character slot",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CharacterInventory"
                }
              }
            }
          },
          "400": {
            "description": "A non-decimal characterID, a characterID outside the slot range, an empty, unknown or already closed session identifier, a containerSection other than common, key or the empty string, a non-integer or negative page or pageSize, or an active slot whose inventory cannot be located inside its slot",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/item-capacity": {
      "get": {
        "operationId": "getItemCapacity",
        "tags": ["Inventory"],
        "summary": "GetItemCapacity",
        "description": "Performs a read-only preflight for adding one goods or talisman resource to common Inventory or common Storage. The result reports the current and resulting item quantity, the catalog container limit, free and required physical records, free and required GaItemData entries, and the first limiting factor. It is not a reservation: a later mutation must verify its own expectedRevision and repeat every capacity check. key_items, families that require the retired variable-length GaItem allocator, the unresolved Flasks shared-charge family, and non-depositable goods targeting Storage are rejected fail-closed. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {"$ref": "#/components/parameters/SaveSessionID"},
          {"$ref": "#/components/parameters/CharacterID"},
          {
            "name": "destination",
            "in": "query",
            "required": true,
            "schema": {"type": "string", "enum": ["inventory", "storage"]}
          },
          {
            "name": "kind",
            "in": "query",
            "required": true,
            "schema": {"type": "string", "minLength": 1}
          },
          {
            "name": "key",
            "in": "query",
            "required": true,
            "schema": {"type": "string", "minLength": 1}
          },
          {
            "name": "variantID",
            "in": "query",
            "required": false,
            "schema": {"type": "integer", "format": "int64", "minimum": 0, "maximum": 4294967295}
          },
          {
            "name": "quantity",
            "in": "query",
            "required": true,
            "schema": {"type": "integer", "format": "int64", "minimum": 1, "maximum": 4294967295}
          }
        ],
        "responses": {
          "200": {
            "description": "The current non-reserving capacity preflight",
            "content": {
              "application/json": {
                "schema": {"$ref": "#/components/schemas/ItemCapacity"}
              }
            }
          },
          "400": {
            "description": "Malformed query data, an unknown session, character, resource or variant, unsupported or incomplete catalog semantics, ambiguous key routing, malformed save data, or an unsupported item family",
            "content": {
              "application/json": {
                "schema": {"$ref": "#/components/schemas/Error"}
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/inventory/order": {
      "put": {
        "operationId": "setInventoryOrder",
        "tags": ["Inventory"],
        "summary": "SetInventoryOrder",
        "description": "Replaces the complete logical order of supported common Inventory records. The request must contain every current ownedItemID whose GameCatalog category is melee_armaments, ranged_and_catalysts, shields, talismans, head, chest, arms or legs, excluding the technical Unarmed record, exactly once. Unsupported common records and every key record remain untouched. A successful mutation assigns fresh even acquisition indices with stride 2, starting above the reserved range and avoiding retained sort buckets; it rejects any plan that would reach acquisition index 10000. NextAcquisitionSortId advances past the assigned range, while physical rows, handles, quantities, NextEquipIndex, Equipment, Storage and GaItem data remain unchanged. The mutation advances saveRevision by exactly one, marks the session dirty and retires all previous-revision ownedItemID tokens. WriteSave remains separate. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetInventoryOrderRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed Inventory order and its new revision",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetInventoryOrderResult"
                }
              }
            }
          },
          "400": {
            "description": "Malformed input, an invalid character or revision, an incomplete or duplicate order, a stale, foreign, Storage, key or unsupported ownedItemID, inconsistent catalog or save data, or no safe acquisition-index range",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/inventory/items": {
      "post": {
        "operationId": "addItemToInventory",
        "tags": ["Inventory"],
        "summary": "AddItemToInventory",
        "description": "Adds one resource or variant to the common section of InventoryHeld in the session snapshot. quantity is a delta and is always at least 1: an item that stores a quantity in one record is topped up on its first common record and receives a new record otherwise, and an item that stores every copy in its own record always receives a new record with quantity 1. The endpoint resolves (kind, key, variantID) through GameCatalog and rejects, without changing the session, every family other than goods and talisman, a family that disagrees with the game-ID prefix, a key_items resource, an unknown category, record mode or inventory limit, a quantity above the per-record or container limit, an inactive character, a full common section, an item that already holds a key record, and a stale or malformed expectedRevision. A successful mutation writes the record, the common item count, NextEquipIndex, NextAcquisitionSortId and, for an item the character owns no physical record of yet, one active GaItemData entry; it advances saveRevision by exactly one, marks the session dirty and retires every ownedItemID from the previous revision. WriteSave remains a separate operation. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AddItemToInventoryRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed add and its new revision",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AddItemToInventoryResult"
                }
              }
            }
          },
          "400": {
            "description": "Malformed or unknown body data, an invalid characterID, quantity or expectedRevision, an unknown session, kind, key or variantID, a resource this endpoint never adds, unusable catalog data, an inactive character, a full container, or a rejected atomic mutation",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/storage": {
      "get": {
        "operationId": "getStorage",
        "tags": ["Inventory"],
        "summary": "GetStorage",
        "description": "Returns one page of Storage Box records of one character slot, each resolved to an ItemDocument identity. kind and key are the canonical GameCatalog resource reference, while gameID is the exact catalog game ID resolved from the save and therefore selects an item variant where applicable. The physical native order, raw gaItemHandle and acquisitionIndex, quantity mask, ownedItemID and paging rules are preserved. Only non-empty records are returned; the two native absent sentinels 0x00000000 and 0xFFFFFFFF are skipped. An unresolved GaItem handle or unknown catalog item rejects the whole request without a partial result. An inactive or residual slot is a normal result with active false, total 0 and an empty array. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          },
          {
            "$ref": "#/components/parameters/StorageContainerSection"
          },
          {
            "$ref": "#/components/parameters/Page"
          },
          {
            "$ref": "#/components/parameters/PageSize"
          }
        ],
        "responses": {
          "200": {
            "description": "One page of resolved storage records of the character slot",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CharacterStorage"
                }
              }
            }
          },
          "400": {
            "description": "A non-decimal characterID, a characterID outside the slot range, an empty, unknown or already closed session identifier, a containerSection other than common, key or the empty string, a non-integer or negative page or pageSize, an active slot that carries no storage anchor, an acquired-projectile count above the accepted maximum, or an active slot whose storage cannot be located inside its slot",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/storage/items": {
      "post": {
        "operationId": "addItemToStorage",
        "tags": ["Inventory"],
        "summary": "AddItemToStorage",
        "description": "Adds one goods or talisman resource or variant to common Storage in the session snapshot. quantity is a positive delta: the first matching quantity stack is topped up, while a new stack or separate instance receives the first free physical row. The endpoint rejects key_items, the unresolved Flasks subcategory, non-depositable goods, unknown or zero Storage limits, unsupported families and record modes, inconsistent game-ID prefixes, full containers, unsafe allocators, key-section conflicts and stale revisions before mutation. A genuinely empty Storage uses the native index 2 and initializes NextEquipIndex to 128; populated Storage preserves NextEquipIndex and advances its acquisition high-water mark. A first physical copy also receives one active GaItemData entry without repacking any section. The mutation is atomic, advances saveRevision once and leaves persistence to WriteSave. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AddItemToStorageRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed Storage add and its new revision",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AddItemToStorageResult"
                }
              }
            }
          },
          "400": {
            "description": "Malformed or unknown body data, an invalid characterID, quantity or expectedRevision, an unknown session, kind, key or variantID, an unsupported or non-depositable resource, unusable catalog or save data, an inactive character, a full container, an unsafe allocator or a rejected atomic mutation",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/storage/order": {
      "put": {
        "operationId": "setStorageOrder",
        "tags": ["Inventory"],
        "summary": "SetStorageOrder",
        "description": "Replaces the complete logical order of supported common Storage records. The request must contain every current ownedItemID whose GameCatalog category is melee_armaments, ranged_and_catalysts, shields, talismans, head, chest, arms or legs, excluding the technical Unarmed record, exactly once. Unsupported common records and every key record remain untouched. A successful mutation assigns fresh even acquisition indices with stride 2, starting above the reserved range and avoiding retained sort buckets; it rejects any plan that would reach acquisition index 10000. Storage NextAcquisitionSortId advances past the assigned range, while physical rows, handles, quantities, Storage NextEquipIndex, Inventory, Equipment and GaItem data remain unchanged. The mutation advances saveRevision by exactly one, marks the session dirty and retires all previous-revision ownedItemID tokens. WriteSave remains separate. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetStorageOrderRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed Storage order and its new revision",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetStorageOrderResult"
                }
              }
            }
          },
          "400": {
            "description": "Malformed input, an invalid character or revision, an incomplete or duplicate order, a stale, foreign, Inventory, key or unsupported ownedItemID, inconsistent catalog or save data, or no safe acquisition-index range",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}": {
      "get": {
        "operationId": "getOwnedItem",
        "tags": ["Inventory"],
        "summary": "GetOwnedItem",
        "description": "Returns one owned item instance addressed by its opaque ownedItemID, resolved to an ItemDocument identity. The identifier is valid only inside the save session and the saveRevision that issued it; it is compared byte for byte and is never parsed, trimmed, normalised or reconstructed. Only the container the identifier was minted in is read, so a Storage identifier never resolves into an Inventory record at the same coordinates and there is no fallback search of the other container. kind and key are the canonical GameCatalog resource reference, while gameID is the exact catalog game ID resolved from the save and therefore selects an item variant where applicable. The physical fields are preserved: gaItemHandle and acquisitionIndex stay raw and only the documented high bit of quantity is masked off. An empty, unknown, retired, foreign or wrong-character identifier, a record that no longer exists, an unresolved GaItem handle and an unknown catalog item each reject the whole request. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          },
          {
            "$ref": "#/components/parameters/OwnedItemID"
          }
        ],
        "responses": {
          "200": {
            "description": "The one resolved owned item instance",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OwnedItem"
                }
              }
            }
          },
          "400": {
            "description": "A non-decimal characterID, a characterID outside the slot range, an empty, unknown or already closed session identifier, an empty, unknown, retired or foreign ownedItemID, an ownedItemID belonging to another character, an ownedItemID whose physical record no longer exists, an unresolvable GaItem handle, or a game ID absent from GameCatalog",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      },
      "delete": {
        "operationId": "removeOwnedItem",
        "tags": ["Inventory"],
        "summary": "RemoveOwnedItem",
        "description": "Removes one existing owned item instance from the session snapshot. The opaque ownedItemID selects the single physical record it was minted for, in the character and the physical container that minted it; it is compared byte for byte and is never parsed, there is no lookup by game ID and no fallback into the other container. Each physical section is cleared its own confirmed way: an InventoryHeld record keeps its physical row number in the third field and only the common count of that container is lowered, while the InventoryHeld key_count header stays untouched; a Storage common record is zeroed whole and the Storage common count is lowered. Storage key records are refused as unsupported, because this project has no confirmed native write contract for that section. A removal is also refused when an InventoryHeld common record's physical row is still named by one of the 22 Equipment, 10 Quick Item or 6 Pouch {GaItem handle, 0x180 + row} reference pairs of the character; the physical row identifies the instance, because one GaItem handle is legitimately shared by several records, so a shared handle at another row never blocks a removal, while a pair naming the addressed row with a different handle is refused fail-closed. Inventory key records and both Storage sections are never checked, because no row field can name one. Nothing is ever unequipped or cascaded. The GaItem table and the trailing NextEquipIndex and NextAcquisitionSortId counters are left untouched, and nothing is merged, moved, reindexed or repacked. An empty, unknown, retired, foreign or wrong-character ownedItemID, a record that no longer exists, an unresolvable GaItem handle and a malformed or stale expectedRevision each reject the whole request before any byte changes; a write that cannot be verified restores every byte the removal had already changed. A successful removal advances saveRevision by exactly one, marks the session dirty and retires every ownedItemID of the previous revision; WriteSave remains a separate operation. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          },
          {
            "$ref": "#/components/parameters/OwnedItemID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RemoveOwnedItemRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed removal and its new revision",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RemoveOwnedItemResult"
                }
              }
            }
          },
          "400": {
            "description": "Malformed or unknown body data, an invalid characterID or expectedRevision, an unknown or stale session or ownedItemID, an ownedItemID belonging to another character, an ownedItemID whose physical record no longer exists, an ownedItemID addressing the unsupported Storage key section, an InventoryHeld common record whose physical row an Equipment, Quick Item or Pouch reference pair still names, an unresolvable GaItem handle, or a rejected atomic mutation",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}/quantity": {
      "patch": {
        "operationId": "setOwnedItemQuantity",
        "tags": ["Inventory"],
        "summary": "SetOwnedItemQuantity",
        "description": "Sets the quantity of one existing owned item in the session snapshot. The opaque ownedItemID and expectedRevision are compared exactly and never parsed, trimmed or normalised. The endpoint resolves the record through GameCatalog, derives the stack and physical-container limits, and rejects zero, overflow, stale identity, stale revision and unusable catalog data without changing the session. A successful mutation advances saveRevision by exactly one, marks the session dirty and retires every ownedItemID from the previous revision; WriteSave remains a separate operation. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          },
          {
            "$ref": "#/components/parameters/OwnedItemID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetOwnedItemQuantityRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed quantity mutation and its new revision",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetOwnedItemQuantityResult"
                }
              }
            }
          },
          "400": {
            "description": "Malformed or unknown body data, an invalid characterID, quantity or expectedRevision, an unknown or stale session or ownedItemID, an ownedItemID belonging to another character, an unresolved item, unusable catalog limits, or a rejected atomic mutation",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}/move-to-inventory": {
      "post": {
        "operationId": "moveOwnedItemToInventory",
        "tags": ["Inventory"],
        "summary": "MoveOwnedItemToInventory",
        "description": "Moves one complete physical record from Storage common to the first free Inventory common row and places it at the requested logical acquisition-order position without shifting existing physical Inventory rows. GameCatalog must provide a known non-key category and positive maxInventory. SaveEngine rejects a Storage key record, a full or over-limit destination, ambiguous or inconsistent acquisition ordering, an invalid position, stale identity or stale revision before changing any byte. The move preserves the handle and raw quantity, allocates no GaItem record, performs no merge, rehandle or repack, leaves NextEquipIndex and every Equipment, Quick Item and Pouch reference untouched, and advances Inventory NextAcquisitionSortId. A successful mutation advances saveRevision by exactly one and retires every ownedItemID from the previous revision. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          },
          {
            "$ref": "#/components/parameters/OwnedItemID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MoveOwnedItemToInventoryRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed Storage-to-Inventory move and its new revision",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MoveOwnedItemToInventoryResult"
                }
              }
            }
          },
          "400": {
            "description": "Malformed or unknown body data, an invalid characterID, targetPosition or expectedRevision, an unknown or stale session or ownedItemID, a non-Storage or key-section record, an unresolved or ambiguously routed item, a destination limit or capacity failure, inconsistent acquisition ordering, or a rejected atomic mutation",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}/move-to-storage": {
      "post": {
        "operationId": "moveOwnedItemToStorage",
        "tags": ["Inventory"],
        "summary": "MoveOwnedItemToStorage",
        "description": "Moves one complete physical record from Inventory common to common Storage at the requested position among non-empty Storage records. GameCatalog must provide a positive maxStorage, and SaveEngine rejects an Inventory key record, a full or over-limit destination, an active Equipment, Quick Item or Pouch reference, an invalid position, stale identity or stale revision before changing any byte. The move preserves the handle and raw quantity, allocates no GaItem record, performs no merge, rehandle or repack, leaves NextEquipIndex untouched and advances only Storage NextAcquisitionSortId. A successful mutation advances saveRevision by exactly one and retires every ownedItemID from the previous revision. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          },
          {
            "$ref": "#/components/parameters/OwnedItemID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MoveOwnedItemToStorageRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed Inventory-to-Storage move and its new revision",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MoveOwnedItemToStorageResult"
                }
              }
            }
          },
          "400": {
            "description": "Malformed or unknown body data, an invalid characterID, targetPosition or expectedRevision, an unknown or stale session or ownedItemID, a non-Inventory or key-section record, an unresolved or unstorable item, a destination limit or capacity failure, an active reference, or a rejected atomic mutation",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}/upgrade-level": {
      "patch": {
        "operationId": "setWeaponUpgradeLevel",
        "tags": ["Inventory"],
        "summary": "SetWeaponUpgradeLevel",
        "description": "Sets the upgrade level of one existing owned weapon while preserving its base weapon and affinity. GameCatalog validates the standard or somber model and its maximum level; SaveEngine atomically updates the weapon GaItem ID, GaItemData and matching equipped hand-slot ID representations without allocating or repacking records. A successful mutation advances saveRevision by exactly one and retires every ownedItemID from the previous revision. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          },
          {
            "$ref": "#/components/parameters/OwnedItemID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetWeaponUpgradeLevelRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed weapon upgrade mutation and its new revision",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetWeaponUpgradeLevelResult"
                }
              }
            }
          },
          "400": {
            "description": "Malformed or unknown body data, an invalid characterID, upgradeLevel or expectedRevision, an unknown or stale session or ownedItemID, an unsupported weapon or upgrade model, an inconsistent save-side reference, or a rejected atomic mutation",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}/spirit-ash-upgrade-level": {
      "patch": {
        "operationId": "setSpiritAshUpgradeLevel",
        "tags": ["Inventory"],
        "summary": "SetSpiritAshUpgradeLevel",
        "description": "Sets the upgrade level of one existing owned Spirit Ash. GameCatalog selects the exact stored grave- or ghost-glovewort variant and enforces its maximum level; SaveEngine atomically updates the goods handle, GaItemData and matching Quick Items or Pouch representations without allocating or repacking records. A successful mutation advances saveRevision by exactly one and retires every ownedItemID from the previous revision. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          },
          {
            "$ref": "#/components/parameters/OwnedItemID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetSpiritAshUpgradeLevelRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed Spirit Ash upgrade mutation and its new revision",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetSpiritAshUpgradeLevelResult"
                }
              }
            }
          },
          "400": {
            "description": "Malformed or unknown body data, an invalid characterID, upgradeLevel or expectedRevision, an unknown or stale session or ownedItemID, an unsupported Spirit Ash or upgrade model, an inconsistent save-side reference, or a rejected atomic mutation",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}/infusion": {
      "patch": {
        "operationId": "setWeaponInfusion",
        "tags": ["Inventory"],
        "summary": "SetWeaponInfusion",
        "description": "Sets the affinity of one existing owned weapon while preserving its upgrade level and mounted Ash of War. GameCatalog validates the affinity against the weapon's confirmed capability and stored variant anchors; SaveEngine atomically updates the weapon GaItem ID, GaItemData and matching equipped hand-slot ID representations without allocating or repacking records. A successful mutation advances saveRevision by exactly one and retires every ownedItemID from the previous revision. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          },
          {
            "$ref": "#/components/parameters/OwnedItemID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetWeaponInfusionRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed weapon infusion mutation and its new revision",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetWeaponInfusionResult"
                }
              }
            }
          },
          "400": {
            "description": "Malformed or unknown body data, an invalid characterID, affinity or expectedRevision, an unknown or stale session or ownedItemID, an unsupported weapon affinity, missing catalog anchors, an inconsistent save-side reference, or a rejected atomic mutation",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/owned-items/{ownedItemID}/ash-of-war": {
      "patch": {
        "operationId": "setWeaponAshOfWar",
        "tags": ["Inventory"],
        "summary": "SetWeaponAshOfWar",
        "description": "Mounts an existing free Ash of War copy on one owned weapon, changes its current custom Ash, or removes it. For mounting, GameCatalog validates the weapon capability and the compatible_with_aow relation. SaveEngine requires unique weapon and Ash GaItem records, rejects shared or malformed references, and changes only the four-byte AoW handle field of the weapon. Both resource selectors set to null remove the custom Ash and write the native zero sentinel. The endpoint never allocates, removes or repacks a GaItem. A successful mutation advances saveRevision by exactly one and retires every ownedItemID from the previous revision. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          },
          {
            "$ref": "#/components/parameters/OwnedItemID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetWeaponAshOfWarRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed weapon Ash of War mutation and its new revision",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetWeaponAshOfWarResult"
                }
              }
            }
          },
          "400": {
            "description": "Malformed or unknown body data, missing or mismatched Ash selectors, an invalid characterID or expectedRevision, an unknown or stale session or ownedItemID, an unsupported weapon, an unknown or incompatible Ash of War, no free physical copy, a shared or malformed save-side reference, or a rejected atomic mutation",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/gestures": {
      "get": {
        "operationId": "getGestures",
        "tags": ["World"],
        "summary": "GetGestures",
        "description": "Returns every gesture GameCatalog knows, each one marked with whether the character slot has unlocked it. The unlock state comes from the raw 64-record GestureGameData block of that slot and from nothing else: a gesture is unlocked when its canonical slotID from GameCatalog is present in the block as an exact uint32 match. No event flag is read, no even value is converted into an odd one and the save is never sanitised, repaired or normalised. The 0xFFFFFFFE empty sentinel, a plain 0 and any value that matches no canonical slotID unlock nothing, and the same slotID stored twice still marks its one entry unlocked once. One catalog resource may declare more than one gesture slot, so kind and key can repeat and slotID distinguishes the entries. The order is category, then name, then slotID, and it does not depend on the unlock state. An inactive or residual slot is a normal result with active false and every gesture locked; its GestureGameData is never read. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          },
          {
            "$ref": "#/components/parameters/AvailabilityFilter"
          }
        ],
        "responses": {
          "200": {
            "description": "The gestures of the character slot that passed availabilityFilter",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GetGesturesResult"
                }
              }
            }
          },
          "400": {
            "description": "A non-decimal characterID, a characterID outside the slot range, an empty, unknown or already closed session identifier, an availabilityFilter other than unlocked, locked or the empty string, an active slot that carries no gesture anchor, an acquired-projectile count above the accepted maximum, an active slot whose GestureGameData cannot be located inside its slot, or a gesture resource whose required catalog data is missing",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/gestures/unlock": {
      "put": {
        "operationId": "setGestureUnlocked",
        "tags": ["World"],
        "summary": "SetGestureUnlocked",
        "description": "Sets the unlock state of one logical gesture resource in an active character slot. The request identifies the gesture only by its exact GameCatalog kind and key; the endpoint resolves the canonical save slot internally and never exposes a writable slotID. GestureGameData stores an unlocked gesture as its odd canonical ID and its locked form as the preceding even value. Unlocking replaces that exact even record first and otherwise consumes the first 0xFFFFFFFE sentinel; locking replaces every duplicate of the exact odd ID. Unknown values, zeroes, unrelated records and empty slots are never sanitised or reordered. The twelve confirmed starting gestures cannot be locked. Ring of Miquella is the one confirmed alias: slot 227 is the protected pre-order grant and slot 233 is the earnable form. Either form satisfies unlock; a missing logical Ring creates only 233, and the resource cannot be locked while 227 exists. A successful assignment, including an idempotent one, advances saveRevision by exactly one and marks the private session dirty; WriteSave remains separate. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetGestureUnlockedRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The receipt for the committed logical gesture state assignment",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetGestureUnlockedResult"
                }
              }
            }
          },
          "400": {
            "description": "Malformed or unknown body data, a non-decimal or out-of-range characterID, an invalid or stale expectedRevision, an unknown session, an inactive slot, a non-gesture or unsupported multi-slot catalog resource, an attempt to lock a protected starting or pre-order gesture, a full GestureGameData block, an invalid gesture layout, or a rejected atomic mutation",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/cookbooks": {
      "get": {
        "operationId": "getCookbooks",
        "tags": ["World"],
        "summary": "GetCookbooks",
        "description": "Returns every cookbook GameCatalog declares, each one marked with whether the character slot has unlocked it. GameCatalog is the only source of a cookbook's identity, name, category and event flag: a cookbook is an ItemDocument of family goods that carries exactly one item.unlocks entry of kind cookbook, and the eventFlagID of that entry is the only state the unlock is read from. One declaring resource yields exactly one entry, so the kind and key identity is unique across a result; another family or a second cookbook unlock in one resource is a fail-closed error naming the resource key and the reason. The identifier itself stays internal and is never part of the response; acquisition.worldPickupFlagID is not consulted and neither is the inventory, so an owned but unregistered cookbook item is not an unlock. The order is category, then name, then the key of the declaring resource, and it does not depend on the unlock state. An inactive or residual slot is a normal result with active false and every cookbook locked; its event flags are never read. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          },
          {
            "$ref": "#/components/parameters/AvailabilityFilter"
          }
        ],
        "responses": {
          "200": {
            "description": "The cookbooks of the character slot that passed availabilityFilter",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GetCookbooksResult"
                }
              }
            }
          },
          "400": {
            "description": "A non-decimal characterID, a characterID outside the slot range, an empty, unknown or already closed session identifier, an availabilityFilter other than unlocked, locked or the empty string, an active slot that carries no event flag anchor, a declared record count or payload size above the accepted maximum, an active slot whose event flag section does not fit into its slot or into the save file, or a cookbook whose required catalog data is missing, whose declaring resource is not a single-unlock goods item, or which conflicts with another cookbook",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/bell-bearings": {
      "get": {
        "operationId": "getBellBearings",
        "tags": ["World"],
        "summary": "GetBellBearings",
        "description": "Returns every goods resource that GameCatalog declares through exactly one item.unlocks entry of kind bell_bearing. GameCatalog owns the public kind, key, name, category and event flag, while the raw flag remains private. A Bell Bearing is unlocked only when its acquisition flag is set, matching SaveForge 1.5.8 and 1.6.8 semantics for a Bell Bearing handed to the Twin Maiden Husks; an item still held in Inventory or Storage is not treated as handed in. Results are ordered by category, name and key. An inactive or residual slot reports active false and every entry locked without reading its event flag bitfield. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          },
          {
            "$ref": "#/components/parameters/AvailabilityFilter"
          }
        ],
        "responses": {
          "200": {
            "description": "The Bell Bearings of the character slot that passed availabilityFilter",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GetBellBearingsResult"
                }
              }
            }
          },
          "400": {
            "description": "A non-decimal or out-of-range characterID, an unknown session, an invalid availabilityFilter, a malformed event flag section, an unsupported event flag block, or incomplete or conflicting Bell Bearing catalog data",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/bell-bearings/unlock": {
      "put": {
        "operationId": "setBellBearingUnlocked",
        "tags": ["World"],
        "summary": "SetBellBearingUnlocked",
        "description": "Sets or clears the handed-in state of one catalog Bell Bearing. Setting unlocked=true atomically sets its acquisition flag and consumes every matching raw-ID or goods-handle record from Inventory common/key and Storage common. Setting unlocked=false clears only the flag and never creates an item. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetBellBearingUnlockedRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The receipt for the committed Bell Bearing handed-in state change",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetBellBearingUnlockedResult"
                }
              }
            }
          },
          "400": {
            "description": "A non-decimal or out-of-range characterID, invalid expectedRevision, unknown session, inactive slot, invalid Bell Bearing resource, malformed event flags or containers, a referenced matching record, a matching Storage key record, or a write error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/colosseums": {
      "get": {
        "operationId": "getColosseums",
        "tags": ["World"],
        "summary": "GetColosseums",
        "description": "Returns the three colosseum resources GameCatalog declares, each with its unlock state. GameCatalog owns the public kind, key and name as well as the unlock event flag; this result carries no event flag identifier and no save-layout detail, while the full catalog document with its provenance stays available through GetResource. All flags are read in one bulk SaveEngine call. Results are ordered by name and then key. An inactive or residual slot reports active false and every entry locked without reading its slot data. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "responses": {
          "200": {
            "description": "The colosseums of the character slot",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GetColosseumsResult"
                }
              }
            }
          },
          "400": {
            "description": "A non-decimal or out-of-range characterID, an unknown session, an active slot whose event flag section is malformed, an unsupported event flag block, or incomplete or conflicting colosseum catalog data",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/regions": {
      "get": {
        "operationId": "getRegions",
        "tags": ["World"],
        "summary": "GetRegions",
        "description": "Returns the 274 curated invasion and blue-summon regions GameCatalog declares, each with its exact membership state in the character's UnlockedRegions list. Unknown raw IDs remain private. Results are ordered by area, name and key. An inactive or residual slot reports active false and every entry locked without reading its slot data. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "responses": {
          "200": {
            "description": "The curated regions of the character slot",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GetRegionsResult"
                }
              }
            }
          },
          "400": {
            "description": "A non-decimal or out-of-range characterID, an unknown session, a malformed UnlockedRegions section, or conflicting region catalog data",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/summoning-pools": {
      "get": {
        "operationId": "getSummoningPools",
        "tags": ["World"],
        "summary": "GetSummoningPools",
        "description": "Returns the 213 curated Martyr Effigy summoning pools GameCatalog declares, each with its activation state read from the confirmed event flag block 670. Raw event flag IDs are not exposed. regionLabel is a curated presentation and grouping label, not a reference to a region resource. Results are ordered by regionLabel, name and key. An inactive or residual slot reports active false and every entry deactivated without reading its slot data. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "responses": {
          "200": {
            "description": "The curated summoning pools of the character slot",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GetSummoningPoolsResult"
                }
              }
            }
          },
          "400": {
            "description": "A non-decimal or out-of-range characterID, an unknown session, an unreadable event flag section, or conflicting summoning pool catalog data",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/whetblades": {
      "get": {
        "operationId": "getWhetblades",
        "tags": ["World"],
        "summary": "GetWhetblades",
        "description": "Returns the six goods resources that GameCatalog declares through exactly one item.unlocks entry of kind whetblade: Whetstone Knife and the five Whetblades. A resource is unlocked when its main event flag is set or its positive-quantity goods record is present in common or key InventoryHeld, matching the confirmed SaveForge 1.5.8 and 1.6.8 state rule. GameCatalog owns each public kind, key, name, game ID and event flag; the raw identifiers stay private. Results are ordered by name and then key. An inactive or residual slot reports active false and every entry locked without reading its slot data. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          },
          {
            "$ref": "#/components/parameters/AvailabilityFilter"
          }
        ],
        "responses": {
          "200": {
            "description": "The Whetblades of the character slot that passed availabilityFilter",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GetWhetbladesResult"
                }
              }
            }
          },
          "400": {
            "description": "A non-decimal or out-of-range characterID, an unknown session, an invalid availabilityFilter, an active slot whose InventoryHeld or event flag section is malformed, an unsupported event flag block, or incomplete or conflicting Whetblade catalog data",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/whetblades/unlock": {
      "put": {
        "operationId": "setWhetbladeUnlocked",
        "tags": ["World"],
        "summary": "SetWhetbladeUnlocked",
        "description": "Sets or clears one catalog Whetblade, its related affinity flags and its Inventory item in one atomic mutation. The shared Ashes of War menu flag remains set while any other Whetblade has its main event flag or positive-quantity Inventory item. Bundled acquisitions such as Storm Stomp are separate resources and remain unchanged. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetWhetbladeUnlockedRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The receipt for the committed Whetblade state change",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetWhetbladeUnlockedResult"
                }
              }
            }
          },
          "400": {
            "description": "A non-decimal or out-of-range characterID, invalid expectedRevision, unknown session, inactive slot, invalid or conflicting Whetblade catalog data, malformed Inventory or event flags, duplicate target records, a referenced target record, or a write error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/cookbooks/unlock": {
      "put": {
        "operationId": "setCookbookUnlocked",
        "tags": ["World"],
        "summary": "SetCookbookUnlocked",
        "description": "Sets or clears the unlock state of one catalog cookbook in a character slot of a save session. Resolves the single event flag ID from GameCatalog and mutates only that bit. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetCookbookUnlockedRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The receipt for the committed cookbook unlock state change",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetCookbookUnlockedResult"
                }
              }
            }
          },
          "400": {
            "description": "A non-decimal characterID, invalid expectedRevision, unknown session, inactive slot, invalid catalog resource, or write error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/profile": {
      "get": {
        "operationId": "getCharacterProfile",
        "tags": ["Character"],
        "summary": "GetCharacterProfile",
        "description": "Returns the confirmed profile of one character slot: name, level, raw startingClassID, raw gender and raw secondsPlayed. An inactive slot is a normal result with zeroed values. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "responses": {
          "200": {
            "description": "The character profile",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CharacterProfile"
                }
              }
            }
          },
          "400": {
            "description": "A non-decimal characterID, a characterID outside the slot range, or an empty, unknown or already closed session identifier",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{sourceCharacterID}/clone": {
      "post": {
        "operationId": "cloneCharacter",
        "tags": ["Character"],
        "summary": "CloneCharacter",
        "description": "Copies one active character into one completely empty physical slot under expectedRevision control. The complete slot data and ProfileSummary are copied, the target activity flag is enabled, and both stored name copies receive the first available suffix starting at ' 2' within the 16-UTF-16-unit limit. Active, residual, malformed or otherwise non-empty target slots are rejected without mutation. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "name": "sourceCharacterID",
            "in": "path",
            "required": true,
            "description": "Physical source character slot, from 0 through 9.",
            "schema": {
              "type": "integer",
              "format": "int32",
              "minimum": 0,
              "maximum": 9
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CloneCharacterRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed character clone",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CloneCharacterResult"
                }
              }
            }
          },
          "400": {
            "description": "A malformed request, invalid expectedRevision, unknown session, invalid source or target slot, inactive or unnamed source, non-empty or malformed target, unsupported activity flag, stale revision, missing layout evidence, or write error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}": {
      "delete": {
        "operationId": "deleteCharacter",
        "tags": ["Character"],
        "summary": "DeleteCharacter",
        "description": "Permanently clears one active or residual character in place without shifting any other slot. The mutation zeroes exactly the target slot data, activity flag and complete profile summary under expectedRevision control. This differs from SetCharacterActive(false), which preserves residual character data. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DeleteCharacterRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed character deletion",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DeleteCharacterResult"
                }
              }
            }
          },
          "400": {
            "description": "A malformed request, invalid expectedRevision, unknown session, invalid slot, empty or malformed slot, unsupported activity flag, stale revision, or write error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/active": {
      "patch": {
        "operationId": "setCharacterActive",
        "tags": ["Character"],
        "summary": "SetCharacterActive",
        "description": "Changes one physical character slot's activity flag under expectedRevision control. Deactivation preserves all character data. Reactivation is allowed only for a residual slot with a confirmed statistics anchor and a non-empty PlayerGameData or profile-summary name. An idempotent request returns the current revision without marking the session dirty. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetCharacterActiveRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The accepted character activity state",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetCharacterActiveResult"
                }
              }
            }
          },
          "400": {
            "description": "A malformed request, invalid expectedRevision, unknown session, invalid slot, empty or malformed residual slot, unsupported activity flag, stale revision, or write error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/name": {
      "patch": {
        "operationId": "setCharacterName",
        "tags": ["Character"],
        "summary": "SetCharacterName",
        "description": "Assigns the exact character name in both confirmed UTF-16LE save fields under expectedRevision control. The name must be non-empty valid UTF-8, contain no U+0000, and fit in 16 UTF-16 code units; it is never trimmed, normalised, truncated, or filtered. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetCharacterNameRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed character-name assignment",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetCharacterNameResult"
                }
              }
            }
          },
          "400": {
            "description": "A malformed request, invalid name or expectedRevision, unknown session, inactive slot, stale revision, unsupported layout, or write error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/gender": {
      "patch": {
        "operationId": "setCharacterGender",
        "tags": ["Character"],
        "summary": "SetCharacterGender",
        "description": "Switches the character to body type 0 (Type B) or 1 (Type A) by applying the complete confirmed default appearance preset for that type: Ciri for Type B and Geralt for Type A. The operation uses the same atomic mutation as SetCharacterAppearance and therefore also updates voice type, all confirmed FACE fields and the two dependent sex-flag bytes. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetCharacterGenderRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The selected default preset and committed complete appearance assignment",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetCharacterGenderResult"
                }
              }
            }
          },
          "400": {
            "description": "A malformed request, gender outside 0..1, missing default preset, unconfirmed model mapping, invalid expectedRevision, unknown session, inactive slot, stale revision, unsupported layout, or write error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/runes": {
      "patch": {
        "operationId": "setCharacterRunes",
        "tags": ["Character"],
        "summary": "SetCharacterRunes",
        "description": "Assigns the exact number of held runes under expectedRevision control. The accepted range is 0 through 999,999,999 inclusive; an out-of-range value is rejected rather than clamped. The adjacent TotalGetSoul field and every unrelated field remain unchanged. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetCharacterRunesRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed held-runes assignment",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetCharacterRunesResult"
                }
              }
            }
          },
          "400": {
            "description": "A malformed request, runes outside 0..999999999, invalid expectedRevision, unknown session, inactive slot, stale revision, unsupported layout, or write error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/undo": {
      "get": {
        "operationId": "getUndoState",
        "tags": ["Character"],
        "summary": "GetUndoState",
        "description": "Reports whether the session still holds a usable undo point for this character and which operation created it. The session keeps exactly one undo point: it belongs to the newest committed character mutation, is valid only for the revision that mutation created, and is dropped by any later mutation, by a successful WriteSave and by closing the session. The getter changes nothing and returns no save bytes. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "responses": {
          "200": {
            "description": "The current undo state of the character",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CharacterUndoState"
                }
              }
            }
          },
          "400": {
            "description": "A missing or unknown session, or a characterID outside 0..9",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      },
      "post": {
        "operationId": "undoCharacterChanges",
        "tags": ["Character"],
        "summary": "UndoCharacterChanges",
        "description": "Restores the character slot data, its ProfileSummary and its activity flag to the state captured before the last committed mutation of that character. The undoToken and expectedRevision must both match the stored undo point exactly. A successful undo consumes the point, restores the unsaved-changes flag that was in force before the undone mutation, advances saveRevision by exactly one and retires every OwnedItemID of the previous revision. It creates no redo point. A rejected or failed undo leaves the session, revision and undo point unchanged. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UndoCharacterChangesRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The consumed undo point and the revision it created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UndoCharacterChangesResult"
                }
              }
            }
          },
          "400": {
            "description": "A malformed request, unknown JSON field, missing or mismatched undoToken, invalid or stale expectedRevision, unknown session, characterID outside 0..9, absent undo point, or a write/verification error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/characters/{characterID}/stats": {
      "get": {
        "operationId": "getCharacterStats",
        "tags": ["Character"],
        "summary": "GetCharacterStats",
        "description": "Returns the raw statistics stored in one character slot: the eight attributes, the level and the stored HP, FP and SP values with their maximum and base maximum. Every value is the raw stored number; nothing is computed or normalised. An inactive slot is a normal result with zeroed values. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "responses": {
          "200": {
            "description": "The raw character statistics",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CharacterStats"
                }
              }
            }
          },
          "400": {
            "description": "A non-decimal characterID, a characterID outside the slot range, or an empty, unknown or already closed session identifier",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      },
      "patch": {
        "operationId": "setCharacterStats",
        "tags": ["Character"],
        "summary": "SetCharacterStats",
        "description": "Atomically assigns the eight attributes of one active character under expectedRevision control. All eight attributes are mandatory and must lie in 1..99 and at or above the base value of the character's own starting class; an unknown starting class is rejected. levelPolicy accepts exactly the value \"recalculate\": the level is never an input, it is always recalculated as the attribute sum minus 79 and written to PlayerGameData and to the character's profile summary. TotalGetSoul, the lifetime-runes value the legacy implementation calls SoulMemory, is raised to the minimum that level requires and otherwise left unchanged. HP, FP and SP with their maximum and base maximum, the held runes, the starting class and every unrelated field remain untouched. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          },
          {
            "$ref": "#/components/parameters/CharacterID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetCharacterStatsRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed statistics assignment",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetCharacterStatsResult"
                }
              }
            }
          },
          "400": {
            "description": "A malformed request, a missing or unknown attribute field, an attribute outside 1..99 or below the starting-class minimum, a recalculated level outside 1..713, an unknown starting class, a levelPolicy other than \"recalculate\", invalid expectedRevision, unknown session, inactive slot, stale revision, unsupported layout, or write error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/network-settings": {
      "get": {
        "operationId": "getNetworkSettings",
        "tags": ["Network"],
        "summary": "GetNetworkSettings",
        "description": "Returns the 22 network parameters stored in UserData11 of an existing PC or PS4 session. The values are reported exactly as the save stores them: none is validated against a preset range, normalised or replaced by a default, and a UserData11 that is missing, truncated, malformed or of an unsupported compression variant produces an error instead of a partial or fallback parameter set. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          }
        ],
        "responses": {
          "200": {
            "description": "The network parameters stored in the save",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GetNetworkSettingsResult"
                }
              }
            }
          },
          "400": {
            "description": "An empty, unknown or already closed session identifier, or a save whose UserData11 cannot be decoded",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      },
      "put": {
        "operationId": "setNetworkSettings",
        "tags": ["Network"],
        "summary": "SetNetworkSettings",
        "description": "Atomically replaces all 22 supported NetworkParam values in UserData11 after validating every field, the three cross-field relationships and expectedRevision. PC DFLT/ZSTD and PS4 DFLT/ZSTD use their confirmed platform-specific write paths; no preset is loaded and no other regulation row is changed. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetNetworkSettingsRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The committed complete network parameter assignment",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetNetworkSettingsResult"
                }
              }
            }
          },
          "400": {
            "description": "Malformed JSON, an invalid field or field relationship, a revision conflict, an unknown session, or a UserData11 that cannot be safely rewritten",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/save-sessions/{saveSessionID}/network-settings/preset": {
      "put": {
        "operationId": "applyNetworkPreset",
        "tags": ["Network"],
        "summary": "ApplyNetworkPreset",
        "description": "Resolves one exact, case-sensitive presetID from the loaded GameCatalog and applies its complete 22-field parameter set through the same SaveEngine operation as SetNetworkSettings. Legacy aliases absent from the catalog are rejected; no fallback or partial application exists. Available only in the local loopback mode of the explorer: an explorer started with -allow-external-bind does not register this route and answers 404.",
        "parameters": [
          {
            "$ref": "#/components/parameters/SaveSessionID"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ApplyNetworkPresetRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The selected preset and committed network settings",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ApplyNetworkPresetResult"
                }
              }
            }
          },
          "400": {
            "description": "Malformed JSON, a missing or unknown presetID, a revision conflict, an unavailable catalog or session, or a UserData11 that cannot be safely rewritten",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            }
          }
        }
      }
    },
    "/healthz": {
      "get": {
        "operationId": "healthz",
        "tags": ["Explorer"],
        "summary": "Liveness probe",
        "responses": {
          "200": {
            "description": "The explorer is running",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "status": {
                      "type": "string",
                      "example": "ok"
                    }
                  },
                  "required": ["status"]
                }
              }
            }
          }
        }
      }
    },
    "/openapi.json": {
      "get": {
        "operationId": "openapiDocument",
        "tags": ["Explorer"],
        "summary": "This OpenAPI document",
        "responses": {
          "200": {
            "description": "OpenAPI 3.0 specification",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "parameters": {
      "SaveSessionID": {
        "name": "saveSessionID",
        "in": "path",
        "required": true,
        "description": "Identifier of an existing save session, matched exactly by SaveEngine. It is never trimmed, normalised or guessed.",
        "schema": {
          "type": "string"
        }
      },
      "CharacterID": {
        "name": "characterID",
        "in": "path",
        "required": true,
        "description": "Physical character slot index 0..9, parsed as a decimal integer. The allowed range is validated by SaveEngine.",
        "schema": {
          "type": "integer",
          "format": "int32"
        },
        "example": 0
      },
      "OwnedItemID": {
        "name": "ownedItemID",
        "in": "path",
        "required": true,
        "description": "Opaque owned-item identity exactly as a previous GetInventory or GetStorage result reported it. It is valid only inside that save session and only while its saveRevision is unchanged. Pass it back unchanged: it is never parsed, trimmed, normalised or reconstructed, and it encodes no handle, no index and no slot address.",
        "schema": {
          "type": "string",
          "minLength": 1
        }
      },
      "ResourceKind": {
        "name": "kind",
        "in": "query",
        "required": true,
        "description": "schema.Resource.Kind, matched exactly. The kind is resolved before the key.",
        "schema": {
          "type": "string"
        },
        "example": "item"
      },
      "ResourceKey": {
        "name": "key",
        "in": "query",
        "required": true,
        "description": "schema.Resource.Key inside the given kind, matched exactly. Its shape depends on the kind: an item key is eight uppercase hexadecimal characters; colosseum, region and summoning pool keys use lowercase letters, digits and underscores. It never carries a kind prefix and is not an Item.GameID, not a variant ID and not a data file name.",
        "schema": {
          "type": "string"
        },
        "example": "000F4240"
      },
      "RelationType": {
        "name": "relationType",
        "in": "query",
        "required": false,
        "description": "schema.RelationKind, matched exactly and case-sensitively. Omitted or empty means every relation type.",
        "schema": {
          "type": "string",
          "enum": ["", "compatible_with_aow", "requires_container"]
        },
        "example": "compatible_with_aow"
      },
      "RelationDirection": {
        "name": "direction",
        "in": "query",
        "required": false,
        "description": "Relation direction, matched exactly. Omitted or empty means both directions; there is deliberately no 'both' value. The filtered-out direction stays an empty array.",
        "schema": {
          "type": "string",
          "enum": ["", "outgoing", "incoming"]
        },
        "example": "outgoing"
      },
      "ResourceTypeFilter": {
        "name": "resourceType",
        "in": "query",
        "required": false,
        "description": "schema.Resource.Kind, matched exactly and case-sensitively. Omitted or empty means every kind, which today mixes items, colosseums, regions and summoning pools in one page.",
        "schema": {
          "type": "string",
          "enum": ["", "item", "colosseum", "region", "summoning_pool"]
        },
        "example": "item"
      },
      "ItemFamilyFilter": {
        "name": "family",
        "in": "query",
        "required": false,
        "description": "schema.ItemFamily, matched exactly and case-sensitively. Omitted or empty means every family. A resource whose family is not known never matches.",
        "schema": {
          "type": "string",
          "enum": [
            "",
            "weapon",
            "armor",
            "talisman",
            "ash_of_war",
            "spell",
            "spirit_ash",
            "goods",
            "gesture"
          ]
        },
        "example": "weapon"
      },
      "CapabilityFilter": {
        "name": "capability",
        "in": "query",
        "required": false,
        "description": "One field of schema.ItemCapabilities. A resource matches only when that capability is both known and enabled, so undecided data never widens the result. Omitted or empty means no capability filter.",
        "schema": {
          "type": "string",
          "enum": ["", "upgrade", "infusion", "ashOfWarMount", "stack", "equipment"]
        },
        "example": "infusion"
      },
      "EndpointIDFilter": {
        "name": "endpointId",
        "in": "query",
        "required": false,
        "description": "Not supported. GameCatalog declares no resource-to-endpoint relation, so any non-empty value is rejected with 400 instead of being answered from a guessed mapping. Only the empty value is accepted.",
        "schema": {
          "type": "string",
          "enum": [""]
        }
      },
      "ResourceSearch": {
        "name": "search",
        "in": "query",
        "required": false,
        "description": "Case-insensitive substring matched against Resource.Key and the item name. An unknown name is an empty string and is never searched as a placeholder. Omitted or empty means no search.",
        "schema": {
          "type": "string"
        },
        "example": "dagger"
      },
      "ContainerSection": {
        "name": "containerSection",
        "in": "query",
        "required": false,
        "description": "Physical InventoryHeld section. Omitted or empty means both sections, common means the 0xA80 common records and key means the 0x180 key records. The value is matched exactly and case-sensitively; it is never trimmed and has no alias.",
        "schema": {
          "type": "string",
          "enum": ["", "common", "key"]
        },
        "example": "common"
      },
      "StorageContainerSection": {
        "name": "containerSection",
        "in": "query",
        "required": false,
        "description": "Physical Storage Box section. Omitted or empty means both sections, common means the 0x780 common records and key means the 0x80 key records. The value is matched exactly and case-sensitively; it is never trimmed and has no alias. The two sections are smaller than the InventoryHeld ones, so this parameter is deliberately not the shared containerSection of GetInventory.",
        "schema": {
          "type": "string",
          "enum": ["", "common", "key"]
        },
        "example": "common"
      },
      "AvailabilityFilter": {
        "name": "availabilityFilter",
        "in": "query",
        "required": false,
        "description": "Unlock-state filter. Omitted or empty means every gesture, unlocked means the unlocked ones only and locked means the rest. The value is matched exactly and case-sensitively; it is never trimmed and has no alias. It can only remove entries and never changes their order.",
        "schema": {
          "type": "string",
          "enum": ["", "unlocked", "locked"]
        },
        "example": "unlocked"
      },
      "Page": {
        "name": "page",
        "in": "query",
        "required": false,
        "description": "One-based page number. Omitted, empty or 0 means page 1. A negative value is rejected. A page beyond the last one returns an empty array with the real total.",
        "schema": {
          "type": "integer",
          "format": "int32"
        },
        "example": 1
      },
      "PageSize": {
        "name": "pageSize",
        "in": "query",
        "required": false,
        "description": "Number of entries per page. Omitted, empty or 0 means 50. A negative value is rejected. There is deliberately no maximum.",
        "schema": {
          "type": "integer",
          "format": "int32"
        },
        "example": 50
      }
    },
    "schemas": {
      "LoadSaveRequest": {
        "type": "object",
        "description": "Body of LoadSave. Both fields reach the endpoint exactly as sent; unknown fields are rejected.",
        "properties": {
          "source": {
            "type": "string",
            "description": "Path of a local save file. Never trimmed, normalised or defaulted."
          },
          "expectedPlatform": {
            "type": "string",
            "description": "Platform the caller expects. An empty value means the platform is recognised from the container.",
            "enum": ["pc", "ps4", ""]
          }
        },
        "required": ["source"]
      },
      "WriteSaveRequest": {
        "type": "object",
        "description": "Body of WriteSave. Both fields reach the endpoint exactly as sent; unknown fields are rejected.",
        "additionalProperties": false,
        "properties": {
          "expectedRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Current canonical decimal saveRevision, compared exactly and never normalised."
          },
          "target": {
            "type": "string",
            "minLength": 1,
            "description": "Explicit local destination path, passed unchanged and never inferred from LoadSave."
          }
        },
        "required": ["expectedRevision", "target"]
      },
      "WriteSaveResult": {
        "type": "object",
        "description": "The session revision committed by a successful WriteSave.",
        "properties": {
          "saveSessionID": {
            "type": "string",
            "minLength": 1
          },
          "saveRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$"
          }
        },
        "required": ["saveSessionID", "saveRevision"]
      },
      "SetSaveAccountIDRequest": {
        "type": "object",
        "description": "Body of SetSaveAccountID. Both fields are required and reach the endpoint exactly as sent; unknown fields are rejected.",
        "additionalProperties": false,
        "properties": {
          "accountID": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Canonical decimal representation of a uint64: 0, or an unsigned number without leading zeros that fits into 64 bits. It is a string because JSON and JavaScript lose the precision of large identifiers."
          },
          "expectedRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Current canonical decimal saveRevision, compared exactly and never normalised."
          }
        },
        "required": ["accountID", "expectedRevision"]
      },
      "SetSaveAccountIDResult": {
        "type": "object",
        "description": "The session revision committed by a successful SetSaveAccountID. It never carries the identifier.",
        "properties": {
          "saveSessionID": {
            "type": "string",
            "minLength": 1
          },
          "saveRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$"
          }
        },
        "required": ["saveSessionID", "saveRevision"]
      },
      "CloseSaveResult": {
        "type": "object",
        "description": "Confirmation that the named session was removed from memory. The source save file is untouched.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "closed": {
            "type": "boolean"
          }
        },
        "required": ["saveSessionID", "closed"]
      },
      "SessionInfo": {
        "type": "object",
        "description": "Safe public metadata of a save session. It carries no path, SteamID, offset, handle or save byte.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "platform": {
            "type": "string",
            "enum": ["pc", "ps4"]
          },
          "format": {
            "type": "string"
          },
          "unsavedChanges": {
            "type": "boolean",
            "description": "True after a committed in-memory mutation and false after LoadSave or a successful WriteSave."
          }
        },
        "required": ["saveSessionID", "platform", "format", "unsavedChanges"]
      },
      "CharacterSummary": {
        "type": "object",
        "description": "Summary of one physical character slot. Name and level are reported for an active slot only.",
        "properties": {
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "active": {
            "type": "boolean"
          },
          "name": {
            "type": "string"
          },
          "level": {
            "type": "integer",
            "format": "int64"
          }
        },
        "required": ["characterID", "active", "name", "level"]
      },
      "SaveCharacters": {
        "type": "object",
        "description": "The session that was read and one summary per physical slot, always ten of them in slot order.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "characters": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CharacterSummary"
            }
          }
        },
        "required": ["saveSessionID", "characters"]
      },
      "CharacterProfile": {
        "type": "object",
        "description": "Profile of one character slot. startingClassID and gender are raw identifiers and secondsPlayed is a raw second count: none is mapped, formatted or rejected for being unknown.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "active": {
            "type": "boolean"
          },
          "name": {
            "type": "string"
          },
          "level": {
            "type": "integer",
            "format": "int64"
          },
          "startingClassID": {
            "type": "integer",
            "format": "int32"
          },
          "gender": {
            "type": "integer",
            "format": "int32"
          },
          "secondsPlayed": {
            "type": "integer",
            "format": "int64"
          }
        },
        "required": [
          "saveSessionID",
          "characterID",
          "active",
          "name",
          "level",
          "startingClassID",
          "gender",
          "secondsPlayed"
        ]
      },
      "CloneCharacterRequest": {
        "type": "object",
        "additionalProperties": false,
        "description": "Completely empty target slot and optimistic-concurrency revision for one character clone.",
        "properties": {
          "targetSlotID": {
            "type": "integer",
            "format": "int32",
            "minimum": 0,
            "maximum": 9,
            "description": "Physical target slot. It must differ from the source and contain only the native empty state."
          },
          "expectedRevision": {
            "type": "string",
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Canonical decimal revision that must equal the session's current revision."
          }
        },
        "required": ["targetSlotID", "expectedRevision"]
      },
      "CloneCharacterResult": {
        "type": "object",
        "description": "Receipt for one committed character clone.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string"
          },
          "sourceCharacterID": {
            "type": "integer",
            "format": "int32"
          },
          "targetSlotID": {
            "type": "integer",
            "format": "int32"
          },
          "name": {
            "type": "string",
            "description": "Unique name assigned to both stored name copies of the clone."
          }
        },
        "required": [
          "saveSessionID",
          "saveRevision",
          "sourceCharacterID",
          "targetSlotID",
          "name"
        ]
      },
      "DeleteCharacterRequest": {
        "type": "object",
        "additionalProperties": false,
        "description": "Optimistic-concurrency revision for one permanent character deletion.",
        "properties": {
          "expectedRevision": {
            "type": "string",
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Canonical decimal revision that must equal the session's current revision."
          }
        },
        "required": ["expectedRevision"]
      },
      "DeleteCharacterResult": {
        "type": "object",
        "description": "Receipt for one committed permanent character deletion.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          }
        },
        "required": ["saveSessionID", "saveRevision", "characterID"]
      },
      "SetCharacterActiveRequest": {
        "type": "object",
        "additionalProperties": false,
        "description": "Target slot activity and optimistic-concurrency revision.",
        "properties": {
          "active": {
            "type": "boolean",
            "description": "Target activity state. False preserves residual character bytes; true requires confirmed residual character data when the slot is currently inactive."
          },
          "expectedRevision": {
            "type": "string",
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Canonical decimal revision that must equal the session's current revision."
          }
        },
        "required": ["active", "expectedRevision"]
      },
      "SetCharacterActiveResult": {
        "type": "object",
        "description": "Receipt for one accepted character activity assignment.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "active": {
            "type": "boolean"
          }
        },
        "required": ["saveSessionID", "saveRevision", "characterID", "active"]
      },
      "SetCharacterNameRequest": {
        "type": "object",
        "additionalProperties": false,
        "description": "Exact character-name assignment and optimistic-concurrency revision. The name is limited by UTF-16 code units rather than Unicode code points.",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "Exact name to store. It must be valid UTF-8, contain no U+0000, and encode to at most 16 UTF-16 code units. No trimming, normalisation, truncation, or character whitelist is applied."
          },
          "expectedRevision": {
            "type": "string",
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Canonical decimal revision that must equal the session's current revision."
          }
        },
        "required": ["name", "expectedRevision"]
      },
      "SetCharacterNameResult": {
        "type": "object",
        "description": "Receipt for one committed character-name assignment.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "name": {
            "type": "string"
          }
        },
        "required": ["saveSessionID", "saveRevision", "characterID", "name"]
      },
      "SetCharacterGenderRequest": {
        "type": "object",
        "additionalProperties": false,
        "description": "Body-type assignment through the confirmed default appearance preset and optimistic-concurrency revision.",
        "properties": {
          "gender": {
            "type": "integer",
            "format": "int32",
            "enum": [0, 1],
            "description": "Target body type: 0 applies the Type B Ciri default; 1 applies the Type A Geralt default."
          },
          "expectedRevision": {
            "type": "string",
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Canonical decimal revision that must equal the session's current revision."
          }
        },
        "required": ["gender", "expectedRevision"]
      },
      "SetCharacterGenderResult": {
        "type": "object",
        "description": "Receipt for one body-type change through its complete default appearance preset.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "presetID": {
            "type": "string"
          },
          "appearance": {
            "$ref": "#/components/schemas/CharacterAppearanceValues"
          }
        },
        "required": ["saveSessionID", "saveRevision", "characterID", "presetID", "appearance"]
      },
      "CharacterUndoState": {
        "type": "object",
        "description": "The undo state of one character slot in one save session.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string",
            "description": "The session's current canonical decimal revision."
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "available": {
            "type": "boolean",
            "description": "True only when the session's single undo point belongs to this character and to the current revision."
          },
          "undoToken": {
            "type": "string",
            "description": "Opaque identifier of the available undo point. Absent when available is false. It is not a GameResource reference."
          },
          "operationID": {
            "type": "string",
            "description": "EndpointID of the mutation the undo point would revert, such as set_character_stats. Absent when available is false."
          }
        },
        "required": ["saveSessionID", "saveRevision", "characterID", "available"]
      },
      "UndoCharacterChangesRequest": {
        "type": "object",
        "additionalProperties": false,
        "description": "The undo point to consume and the optimistic-concurrency revision.",
        "properties": {
          "undoToken": {
            "type": "string",
            "description": "The exact token reported by GetUndoState for this character. It is matched byte for byte."
          },
          "expectedRevision": {
            "type": "string",
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Canonical decimal revision that must equal the session's current revision."
          }
        },
        "required": ["undoToken", "expectedRevision"]
      },
      "UndoCharacterChangesResult": {
        "type": "object",
        "description": "Receipt for one consumed undo point.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string",
            "description": "The revision created by the undo, exactly one above the revision the request matched."
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "undoneOperationID": {
            "type": "string",
            "description": "EndpointID of the mutation that was reverted."
          }
        },
        "required": ["saveSessionID", "saveRevision", "characterID", "undoneOperationID"]
      },
      "SetCharacterRunesRequest": {
        "type": "object",
        "additionalProperties": false,
        "description": "Exact held-runes assignment and optimistic-concurrency revision.",
        "properties": {
          "runes": {
            "type": "integer",
            "format": "int64",
            "minimum": 0,
            "maximum": 999999999,
            "description": "Exact held-runes value to store. Values outside the legal range are rejected, never clamped."
          },
          "expectedRevision": {
            "type": "string",
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Canonical decimal revision that must equal the session's current revision."
          }
        },
        "required": ["runes", "expectedRevision"]
      },
      "SetCharacterRunesResult": {
        "type": "object",
        "description": "Receipt for one committed held-runes assignment.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "runes": {
            "type": "integer",
            "format": "int64",
            "minimum": 0,
            "maximum": 999999999
          }
        },
        "required": ["saveSessionID", "saveRevision", "characterID", "runes"]
      },
      "CharacterAttributes": {
        "type": "object",
        "additionalProperties": false,
        "description": "The complete writable attribute set. All eight fields are mandatory; an omitted field is rejected, never read as zero.",
        "properties": {
          "vigor": { "type": "integer", "format": "int64", "minimum": 1, "maximum": 99 },
          "mind": { "type": "integer", "format": "int64", "minimum": 1, "maximum": 99 },
          "endurance": { "type": "integer", "format": "int64", "minimum": 1, "maximum": 99 },
          "strength": { "type": "integer", "format": "int64", "minimum": 1, "maximum": 99 },
          "dexterity": { "type": "integer", "format": "int64", "minimum": 1, "maximum": 99 },
          "intelligence": { "type": "integer", "format": "int64", "minimum": 1, "maximum": 99 },
          "faith": { "type": "integer", "format": "int64", "minimum": 1, "maximum": 99 },
          "arcane": { "type": "integer", "format": "int64", "minimum": 1, "maximum": 99 }
        },
        "required": [
          "vigor", "mind", "endurance", "strength",
          "dexterity", "intelligence", "faith", "arcane"
        ]
      },
      "SetCharacterStatsRequest": {
        "type": "object",
        "additionalProperties": false,
        "description": "Complete attribute assignment, level policy and optimistic-concurrency revision. The level is not an input.",
        "properties": {
          "attributes": {
            "$ref": "#/components/schemas/CharacterAttributes"
          },
          "levelPolicy": {
            "type": "string",
            "enum": ["recalculate"],
            "description": "Accepted exactly as written. The value is never trimmed or normalised, and no other policy exists."
          },
          "expectedRevision": {
            "type": "string",
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Canonical decimal revision that must equal the session's current revision."
          }
        },
        "required": ["attributes", "levelPolicy", "expectedRevision"]
      },
      "SetCharacterStatsResult": {
        "type": "object",
        "description": "Receipt for one committed statistics assignment. It reports the accepted attributes and the two values SaveEngine derived from them.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "attributes": {
            "$ref": "#/components/schemas/CharacterAttributes"
          },
          "level": {
            "type": "integer",
            "format": "int64",
            "minimum": 1,
            "maximum": 713
          },
          "soulMemory": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          }
        },
        "required": [
          "saveSessionID", "saveRevision", "characterID",
          "attributes", "level", "soulMemory"
        ]
      },
      "CharacterStats": {
        "type": "object",
        "description": "Raw statistics stored in one character slot. Every number is the value stored in the save: nothing is validated, normalised, clamped or computed.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "active": {
            "type": "boolean"
          },
          "vigor": {
            "type": "integer",
            "format": "int64"
          },
          "mind": {
            "type": "integer",
            "format": "int64"
          },
          "endurance": {
            "type": "integer",
            "format": "int64"
          },
          "strength": {
            "type": "integer",
            "format": "int64"
          },
          "dexterity": {
            "type": "integer",
            "format": "int64"
          },
          "intelligence": {
            "type": "integer",
            "format": "int64"
          },
          "faith": {
            "type": "integer",
            "format": "int64"
          },
          "arcane": {
            "type": "integer",
            "format": "int64"
          },
          "level": {
            "type": "integer",
            "format": "int64"
          },
          "hp": {
            "type": "integer",
            "format": "int64"
          },
          "maxHP": {
            "type": "integer",
            "format": "int64"
          },
          "baseMaxHP": {
            "type": "integer",
            "format": "int64"
          },
          "fp": {
            "type": "integer",
            "format": "int64"
          },
          "maxFP": {
            "type": "integer",
            "format": "int64"
          },
          "baseMaxFP": {
            "type": "integer",
            "format": "int64"
          },
          "sp": {
            "type": "integer",
            "format": "int64"
          },
          "maxSP": {
            "type": "integer",
            "format": "int64"
          },
          "baseMaxSP": {
            "type": "integer",
            "format": "int64"
          }
        },
        "required": [
          "saveSessionID",
          "characterID",
          "active",
          "vigor",
          "mind",
          "endurance",
          "strength",
          "dexterity",
          "intelligence",
          "faith",
          "arcane",
          "level",
          "hp",
          "maxHP",
          "baseMaxHP",
          "fp",
          "maxFP",
          "baseMaxFP",
          "sp",
          "maxSP",
          "baseMaxSP"
        ]
      },
      "CharacterAppearance": {
        "type": "object",
        "description": "Raw appearance stored in one character slot. Every number is the value stored in the save: nothing is validated, normalised, clamped, computed or resolved to a name. An inactive slot reports active false and zero values.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "active": {
            "type": "boolean"
          },
          "gender": {
            "type": "integer",
            "format": "int32"
          },
          "voiceType": {
            "type": "integer",
            "format": "int32"
          },
          "modelIDs": {
            "type": "array",
            "description": "The eight raw model identifiers, in stored order.",
            "minItems": 8,
            "maxItems": 8,
            "items": {
              "type": "integer",
              "format": "int64"
            }
          },
          "faceShape": {
            "type": "array",
            "description": "The raw face shape parameter block, one number per stored byte.",
            "minItems": 64,
            "maxItems": 64,
            "items": {
              "type": "integer",
              "format": "int32"
            }
          },
          "body": {
            "type": "array",
            "description": "The raw body parameter block, one number per stored byte.",
            "minItems": 7,
            "maxItems": 7,
            "items": {
              "type": "integer",
              "format": "int32"
            }
          },
          "skin": {
            "type": "array",
            "description": "The raw skin parameter block, one number per stored byte.",
            "minItems": 91,
            "maxItems": 91,
            "items": {
              "type": "integer",
              "format": "int32"
            }
          }
        },
        "required": [
          "saveSessionID",
          "characterID",
          "active",
          "gender",
          "voiceType",
          "modelIDs",
          "faceShape",
          "body",
          "skin"
        ]
      },
      "CharacterAppearanceValues": {
        "type": "object",
        "additionalProperties": false,
        "description": "Complete writable raw appearance model. Model identifiers retain their full uint32 values; the other arrays contain raw save bytes.",
        "properties": {
          "gender": {
            "type": "integer",
            "format": "int32",
            "enum": [0, 1]
          },
          "voiceType": {
            "type": "integer",
            "format": "int32",
            "minimum": 0,
            "maximum": 5
          },
          "modelIDs": {
            "type": "array",
            "minItems": 8,
            "maxItems": 8,
            "items": {
              "type": "integer",
              "format": "int64",
              "minimum": 0,
              "maximum": 4294967295
            }
          },
          "faceShape": {
            "type": "array",
            "minItems": 64,
            "maxItems": 64,
            "items": {
              "type": "integer",
              "format": "int32",
              "minimum": 0,
              "maximum": 255
            }
          },
          "body": {
            "type": "array",
            "minItems": 7,
            "maxItems": 7,
            "items": {
              "type": "integer",
              "format": "int32",
              "minimum": 0,
              "maximum": 255
            }
          },
          "skin": {
            "type": "array",
            "minItems": 91,
            "maxItems": 91,
            "items": {
              "type": "integer",
              "format": "int32",
              "minimum": 0,
              "maximum": 255
            }
          }
        },
        "required": ["gender", "voiceType", "modelIDs", "faceShape", "body", "skin"]
      },
      "SetCharacterAppearanceRequest": {
        "type": "object",
        "additionalProperties": false,
        "description": "Complete raw appearance assignment and optimistic-concurrency revision.",
        "properties": {
          "appearance": {
            "$ref": "#/components/schemas/CharacterAppearanceValues"
          },
          "expectedRevision": {
            "type": "string",
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Canonical decimal revision that must equal the session's current revision."
          }
        },
        "required": ["appearance", "expectedRevision"]
      },
      "SetCharacterAppearanceResult": {
        "type": "object",
        "description": "Receipt for one committed complete appearance assignment.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "appearance": {
            "$ref": "#/components/schemas/CharacterAppearanceValues"
          }
        },
        "required": ["saveSessionID", "saveRevision", "characterID", "appearance"]
      },
      "ApplyAppearancePresetRequest": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "presetID": {
            "type": "string",
            "minLength": 1,
            "description": "Exact, case-sensitive appearance preset ID from GameCatalog."
          },
          "expectedRevision": {
            "type": "string",
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Canonical decimal revision that must equal the session's current revision."
          }
        },
        "required": ["presetID", "expectedRevision"]
      },
      "ApplyAppearancePresetResult": {
        "type": "object",
        "description": "Receipt for one appearance preset applied through the complete appearance mutation.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "presetID": {
            "type": "string"
          },
          "appearance": {
            "$ref": "#/components/schemas/CharacterAppearanceValues"
          }
        },
        "required": ["saveSessionID", "saveRevision", "characterID", "presetID", "appearance"]
      },
      "CharacterEquipment": {
        "type": "object",
        "description": "Raw 22-slot ChrAsmEquipment state of one character slot. Every number is the value stored in the save: nothing is normalised, masked, turned into a handle, stripped of its type bits or resolved against GameCatalog. An inactive slot reports active false and an all-zero slots array.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "active": {
            "type": "boolean"
          },
          "slots": {
            "type": "array",
            "description": "The 22 raw equipment fields, in stored order. The meaning of each index is documented in docs/endpoints/equipment/get_equipment.md; the values are returned without any normalisation.",
            "minItems": 22,
            "maxItems": 22,
            "items": {
              "type": "integer",
              "format": "int64"
            }
          }
        },
        "required": [
          "saveSessionID",
          "characterID",
          "active",
          "slots"
        ]
      },
      "SetEquippedArmamentsRequest": {
        "type": "object",
        "additionalProperties": false,
        "description": "Complete six-position hand-armament assignment and optimistic-concurrency revision.",
        "properties": {
          "slotAssignments": {
            "type": "array",
            "minItems": 6,
            "maxItems": 6,
            "description": "Exactly six positions in left 1, right 1, left 2, right 2, left 3 and right 3 order. Each position is null to select the native Unarmed record, or an ownedItemID string for a positive Inventory common weapon record.",
            "items": {
              "type": "string",
              "minLength": 1,
              "nullable": true
            }
          },
          "expectedRevision": {
            "type": "string",
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Canonical decimal revision that must equal the session's current revision."
          }
        },
        "required": ["slotAssignments", "expectedRevision"]
      },
      "SetEquippedArmamentsResult": {
        "type": "object",
        "description": "Receipt for one committed complete hand-armament assignment, expressed with public catalog identities.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "slotAssignments": {
            "type": "array",
            "minItems": 6,
            "maxItems": 6,
            "items": {
              "allOf": [
                {
                  "$ref": "#/components/schemas/EquippedArmamentAssignment"
                }
              ],
              "nullable": true
            }
          }
        },
        "required": ["saveSessionID", "saveRevision", "characterID", "slotAssignments"]
      },
      "EquippedArmamentAssignment": {
        "type": "object",
        "description": "Canonical catalog identity plus the exact materialized weapon game ID, which preserves upgrade and infusion variants.",
        "properties": {
          "kind": {
            "type": "string",
            "enum": ["item"]
          },
          "key": {
            "type": "string",
            "pattern": "^[0-9A-F]{8}$"
          },
          "gameID": {
            "type": "integer",
            "format": "uint32",
            "minimum": 0
          }
        },
        "required": ["kind", "key", "gameID"]
      },
      "SetEquippedArmorRequest": {
        "type": "object",
        "additionalProperties": false,
        "description": "Complete four-position armor assignment and optimistic-concurrency revision.",
        "properties": {
          "slotAssignments": {
            "type": "array",
            "minItems": 4,
            "maxItems": 4,
            "description": "Exactly four positions in head, chest, arms and legs order. Each position is null to select the native bare-body record, or an ownedItemID string for a positive Inventory common armor record.",
            "items": {
              "type": "string",
              "minLength": 1,
              "nullable": true
            }
          },
          "expectedRevision": {
            "type": "string",
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Canonical decimal revision that must equal the session's current revision."
          }
        },
        "required": ["slotAssignments", "expectedRevision"]
      },
      "SetEquippedArmorResult": {
        "type": "object",
        "description": "Receipt for one committed complete armor assignment, expressed with public catalog identities.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "slotAssignments": {
            "type": "array",
            "minItems": 4,
            "maxItems": 4,
            "items": {
              "allOf": [
                {
                  "$ref": "#/components/schemas/ResourceRef"
                }
              ],
              "nullable": true
            }
          }
        },
        "required": ["saveSessionID", "saveRevision", "characterID", "slotAssignments"]
      },
      "QuickItemSlot": {
        "type": "object",
        "description": "One raw Quick Items record exactly as stored: the item field followed by the equip-index field, both little-endian uint32. Neither value is normalised, validated or resolved against GameCatalog.",
        "properties": {
          "itemID": {
            "type": "integer",
            "format": "int64"
          },
          "equipIndex": {
            "type": "integer",
            "format": "int64"
          }
        },
        "required": [
          "itemID",
          "equipIndex"
        ]
      },
      "CharacterQuickItems": {
        "type": "object",
        "description": "Raw Quick Items state of one character slot: the ten EquipItemData records in stored order and the raw active-slot value behind them. Nothing is normalised, validated or resolved against GameCatalog. An inactive slot reports active false, ten zeroed records and activeQuick 0.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "active": {
            "type": "boolean"
          },
          "items": {
            "type": "array",
            "description": "The ten raw Quick Items records, in stored order. The values are returned without any normalisation.",
            "minItems": 10,
            "maxItems": 10,
            "items": {
              "$ref": "#/components/schemas/QuickItemSlot"
            }
          },
          "activeQuick": {
            "type": "integer",
            "format": "int32",
            "description": "The raw active-slot value stored behind the ten records. It keeps its sign: a negative value is reported as written."
          }
        },
        "required": [
          "saveSessionID",
          "characterID",
          "active",
          "items",
          "activeQuick"
        ]
      },
      "PouchItemSlot": {
        "type": "object",
        "description": "One raw Pouch record exactly as stored: the item field followed by the equip-index field, both little-endian uint32. Neither value is normalised, validated or resolved against GameCatalog.",
        "properties": {
          "itemID": {
            "type": "integer",
            "format": "int64"
          },
          "equipIndex": {
            "type": "integer",
            "format": "int64"
          }
        },
        "required": [
          "itemID",
          "equipIndex"
        ]
      },
      "CharacterPouchItems": {
        "type": "object",
        "description": "Raw Pouch state of one character slot: the six EquipItemData records that follow the ten Quick Items records and the active-slot value, in stored order. Nothing is normalised, validated or resolved against GameCatalog. An inactive slot reports active false and six zeroed records.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "active": {
            "type": "boolean"
          },
          "items": {
            "type": "array",
            "description": "The six raw Pouch records, in stored order. The values are returned without any normalisation.",
            "minItems": 6,
            "maxItems": 6,
            "items": {
              "$ref": "#/components/schemas/PouchItemSlot"
            }
          }
        },
        "required": [
          "saveSessionID",
          "characterID",
          "active",
          "items"
        ]
      },
      "CharacterPhysickMixture": {
        "type": "object",
        "description": "Raw Flask of Wondrous Physick mixture of one character slot: the two Crystal Tear identifiers at the start of EquipPhysicsData, in stored order. Nothing is normalised, validated or resolved against GameCatalog. An inactive slot reports active false and two zeros.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "active": {
            "type": "boolean"
          },
          "tears": {
            "type": "array",
            "description": "The two raw Crystal Tear identifiers, in stored order. The values are returned without any normalisation.",
            "minItems": 2,
            "maxItems": 2,
            "items": {
              "type": "integer",
              "format": "int64"
            }
          }
        },
        "required": [
          "saveSessionID",
          "characterID",
          "active",
          "tears"
        ]
      },
      "SetPhysickMixtureRequest": {
        "type": "object",
        "additionalProperties": false,
        "description": "Complete two-position Physick assignment and optimistic-concurrency revision. A null array entry clears that exact position.",
        "properties": {
          "crystalTearResources": {
            "type": "array",
            "minItems": 2,
            "maxItems": 2,
            "description": "Exactly two positions in stored order. Each non-null ResourceRef must resolve to an owned Crystal Tear; null is the native empty position.",
            "items": {
              "allOf": [
                {
                  "$ref": "#/components/schemas/ResourceRef"
                }
              ],
              "nullable": true
            }
          },
          "expectedRevision": {
            "type": "string",
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Canonical decimal revision that must equal the session's current revision."
          }
        },
        "required": ["crystalTearResources", "expectedRevision"]
      },
      "SetPhysickMixtureResult": {
        "type": "object",
        "description": "Receipt for one committed complete Physick assignment, expressed with public catalog identities.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "crystalTearResources": {
            "type": "array",
            "minItems": 2,
            "maxItems": 2,
            "items": {
              "allOf": [
                {
                  "$ref": "#/components/schemas/ResourceRef"
                }
              ],
              "nullable": true
            }
          }
        },
        "required": ["saveSessionID", "saveRevision", "characterID", "crystalTearResources"]
      },
      "SetQuickItemsRequest": {
        "type": "object",
        "additionalProperties": false,
        "description": "Complete ten-position Quick Items assignment and optimistic-concurrency revision. A null array entry clears that exact position.",
        "properties": {
          "slotAssignments": {
            "type": "array",
            "minItems": 10,
            "maxItems": 10,
            "description": "Exactly ten positions in stored order. Each position is null to clear the slot, or a public ownedItemID string pointing to an active common Inventory record.",
            "items": {
              "type": "string",
              "nullable": true
            }
          },
          "expectedRevision": {
            "type": "string",
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Canonical decimal revision that must equal the session's current revision."
          }
        },
        "required": ["slotAssignments", "expectedRevision"]
      },
      "SetQuickItemsResult": {
        "type": "object",
        "description": "Receipt for one committed complete Quick Items assignment, expressed with public catalog identities.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "slotAssignments": {
            "type": "array",
            "minItems": 10,
            "maxItems": 10,
            "items": {
              "allOf": [
                {
                  "$ref": "#/components/schemas/ResourceRef"
                }
              ],
              "nullable": true
            }
          }
        },
        "required": ["saveSessionID", "saveRevision", "characterID", "slotAssignments"]
      },
      "SetPouchItemsRequest": {
        "type": "object",
        "additionalProperties": false,
        "description": "Complete six-position Pouch assignment and optimistic-concurrency revision. A null array entry clears that exact position.",
        "properties": {
          "slotAssignments": {
            "type": "array",
            "minItems": 6,
            "maxItems": 6,
            "description": "Exactly six positions in stored order. Each position is null to clear the slot, or a public ownedItemID string pointing to an active Inventory record.",
            "items": {
              "type": "string",
              "nullable": true
            }
          },
          "expectedRevision": {
            "type": "string",
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Canonical decimal revision that must equal the session's current revision."
          }
        },
        "required": ["slotAssignments", "expectedRevision"]
      },
      "SetPouchItemsResult": {
        "type": "object",
        "description": "Receipt for one committed complete Pouch assignment, expressed with public catalog identities.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "slotAssignments": {
            "type": "array",
            "minItems": 6,
            "maxItems": 6,
            "items": {
              "allOf": [
                {
                  "$ref": "#/components/schemas/ResourceRef"
                }
              ],
              "nullable": true
            }
          }
        },
        "required": ["saveSessionID", "saveRevision", "characterID", "slotAssignments"]
      },
      "SetEquippedSpellsRequest": {
        "type": "object",
        "additionalProperties": false,
        "description": "Compact list of spell resources and optimistic-concurrency revision.",
        "properties": {
          "orderedResources": {
            "type": "array",
            "maxItems": 12,
            "description": "At most 12 spell resources in desired memory slot order.",
            "items": {
              "$ref": "#/components/schemas/ResourceRef"
            }
          },
          "expectedRevision": {
            "type": "string",
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Canonical decimal revision that must equal the session's current revision."
          }
        },
        "required": ["orderedResources", "expectedRevision"]
      },
      "SetEquippedSpellsResult": {
        "type": "object",
        "description": "Receipt for one committed equipped-spells update, expressed with public catalog identities.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "orderedResources": {
            "type": "array",
            "maxItems": 12,
            "items": {
              "$ref": "#/components/schemas/ResourceRef"
            }
          },
          "usedMemorySlots": {
            "type": "integer",
            "format": "int32"
          },
          "availableMemorySlots": {
            "type": "integer",
            "format": "int32"
          }
        },
        "required": [
          "saveSessionID",
          "saveRevision",
          "characterID",
          "orderedResources",
          "usedMemorySlots",
          "availableMemorySlots"
        ]
      },
      "SetEquippedTalismansRequest": {
        "type": "object",
        "additionalProperties": false,
        "description": "Compact ordered list of owned talisman instances and optimistic-concurrency revision.",
        "properties": {
          "orderedOwnedItemIDs": {
            "type": "array",
            "maxItems": 4,
            "description": "At most four ownedItemID tokens in desired talisman-slot order. The list must also fit the character's unlocked slot count.",
            "items": {
              "type": "string",
              "minLength": 1
            }
          },
          "expectedRevision": {
            "type": "string",
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Canonical decimal revision that must equal the session's current revision."
          }
        },
        "required": ["orderedOwnedItemIDs", "expectedRevision"]
      },
      "SetEquippedTalismansResult": {
        "type": "object",
        "description": "Receipt for one committed talisman loadout, expressed with public catalog identities.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "orderedResources": {
            "type": "array",
            "maxItems": 4,
            "items": {
              "$ref": "#/components/schemas/ResourceRef"
            }
          },
          "unlockedSlots": {
            "type": "integer",
            "format": "int32",
            "minimum": 1,
            "maximum": 4
          }
        },
        "required": [
          "saveSessionID",
          "saveRevision",
          "characterID",
          "orderedResources",
          "unlockedSlots"
        ]
      },
      "EquippedSpellSlot": {
        "type": "object",
        "description": "One physical EquippedSpells record: the raw MagicParam ID exactly as stored, and the GameCatalog resource key, name and Memory Slots cost of the spell it resolves to. The three resolved fields are empty or zero only for the native empty record, whose raw identifier is the sentinel 0xFFFFFFFF.",
        "properties": {
          "rawMagicParamID": {
            "type": "integer",
            "format": "int64"
          },
          "resourceKey": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "memorySlots": {
            "type": "integer",
            "format": "int32"
          }
        },
        "required": [
          "rawMagicParamID",
          "resourceKey",
          "name",
          "memorySlots"
        ]
      },
      "CharacterEquippedSpells": {
        "type": "object",
        "description": "Equipped spells of one character slot: the twelve public playable EquippedSpells records in stored order, the Memory Slots the occupied records consume, and the Memory Slots the character may fill. An inactive slot reports active false, twelve zero-value records and both counts zero.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "active": {
            "type": "boolean"
          },
          "spells": {
            "type": "array",
            "description": "The twelve public playable spell records, in stored order. The raw MagicParam identifier of every record is returned without any normalisation.",
            "minItems": 12,
            "maxItems": 12,
            "items": {
              "$ref": "#/components/schemas/EquippedSpellSlot"
            }
          },
          "usedMemorySlots": {
            "type": "integer",
            "format": "int32",
            "description": "Sum of the Memory Slots costs GameCatalog states for the occupied records."
          },
          "availableMemorySlots": {
            "type": "integer",
            "format": "int32",
            "description": "Memory Slots the character may fill: the base capacity plus the effective Memory Stones, capped by the game maximum, plus the Moon of Nokstella bonus while that talisman sits in an unlocked talisman field."
          }
        },
        "required": [
          "saveSessionID",
          "characterID",
          "active",
          "spells",
          "usedMemorySlots",
          "availableMemorySlots"
        ]
      },
      "InventoryRecord": {
        "type": "object",
        "description": "One non-empty physical InventoryHeld record, its opaque owned-item identity and its resolved ItemDocument identity. kind and key are the canonical catalog resource reference; gameID is the exact catalog game ID resolved from the save and selects a materialised variant where applicable. containerSection and physicalIndex identify the physical row it currently occupies and are deliberately not an item identity, because a row moves when the game rewrites the section. gaItemHandle and acquisitionIndex are raw stored values, while quantity has its high bit removed by the 0x7FFFFFFF mask.",
        "properties": {
          "ownedItemID": {
            "type": "string",
            "minLength": 1,
            "description": "Opaque identity of this physical record, valid only for the saveRevision of the enclosing result and only inside the save session that produced it. Compare it byte for byte; never parse, trim, normalise or persist it. It encodes no handle, no index and no slot address, and an Inventory record never shares it with a Storage record at the same coordinates."
          },
          "kind": {
            "type": "string",
            "enum": ["item"],
            "description": "The GameCatalog resource kind of the resolved item."
          },
          "key": {
            "type": "string",
            "minLength": 1,
            "description": "The canonical GameCatalog resource key of the resolved item."
          },
          "gameID": {
            "type": "integer",
            "format": "int64",
            "description": "The exact GameCatalog game ID resolved from the save. It selects an item variant where applicable."
          },
          "containerSection": {
            "type": "string",
            "enum": ["common", "key"],
            "description": "The physical section the record was read from."
          },
          "physicalIndex": {
            "type": "integer",
            "format": "int32",
            "description": "Position of the record inside its own section, counted from 0."
          },
          "gaItemHandle": {
            "type": "integer",
            "format": "int64",
            "description": "The raw stored GaItem handle. It is not normalised, validated or resolved against GameCatalog."
          },
          "quantity": {
            "type": "integer",
            "format": "int64",
            "description": "The stored quantity with the high bit masked off, because that bit is not part of the count."
          },
          "acquisitionIndex": {
            "type": "integer",
            "format": "int64",
            "description": "The raw stored acquisition index."
          }
        },
        "required": ["ownedItemID", "kind", "key", "gameID", "containerSection", "physicalIndex", "gaItemHandle", "quantity", "acquisitionIndex"]
      },
      "CharacterInventory": {
        "type": "object",
        "description": "One page of resolved InventoryHeld records of one character slot. Every active record carries an ItemDocument kind, canonical key and exact resolved gameID; the remaining physical fields and paging rules are preserved. An inactive or residual slot reports active false, total 0 and an empty array, mints no identity, and still reports the current saveRevision.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "The revision this result was read under, and the one every ownedItemID in it was minted under. A non-empty decimal string with no sign, prefix, padding or separator. It is deliberately not a JSON number, because a uint64 above 2^53-1 would round in a JavaScript client. Carry it back unchanged as expectedRevision and compare it byte for byte; never parse, increment or order it."
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "active": {
            "type": "boolean"
          },
          "records": {
            "type": "array",
            "description": "The requested page in physical native order, the common section first and then the key section. Empty, never null, when the slot is inactive or the page lies beyond the last one.",
            "items": {
              "$ref": "#/components/schemas/InventoryRecord"
            }
          },
          "total": {
            "type": "integer",
            "description": "Number of non-empty records that passed the section filter, counted before paging."
          },
          "page": {
            "type": "integer",
            "description": "The effective one-based page, so a requested 0 is reported as 1."
          },
          "pageSize": {
            "type": "integer",
            "description": "The effective page size, so a requested 0 is reported as 50."
          }
        },
        "required": ["saveSessionID", "saveRevision", "characterID", "active", "records", "total", "page", "pageSize"]
      },
      "CookbookEntry": {
        "type": "object",
        "description": "One cookbook GameCatalog declares, together with whether the character has unlocked it. kind and key are the public GameCatalog identity of the goods ItemDocument that declares the single item.unlocks entry of kind cookbook; that identity is unique across a result. The event flag identifier behind the state stays internal, and no provenance, source record, offset or raw save byte is exposed.",
        "properties": {
          "kind": {
            "type": "string",
            "description": "The GameCatalog resource kind. Always item for a cookbook."
          },
          "key": {
            "type": "string",
            "description": "The GameCatalog resource key of the declaring ItemDocument. One resource declares exactly one cookbook, so the key never repeats in a result."
          },
          "name": {
            "type": "string",
            "description": "The cookbook name stored in the unlock entry. Never empty; a missing name is a fail-closed error."
          },
          "category": {
            "type": "string",
            "description": "The cookbook series stored in the unlock entry. Never empty; a missing category is a fail-closed error."
          },
          "unlocked": {
            "type": "boolean",
            "description": "True when the event flag of this cookbook is set in the requested slot."
          }
        },
        "required": ["kind", "key", "name", "category", "unlocked"]
      },
      "GetCookbooksResult": {
        "type": "object",
        "description": "The cookbooks of one character slot that passed availabilityFilter, ordered by category, then name, then the key of the declaring resource. The order never depends on the unlock state. An inactive or residual slot reports active false and every cookbook locked, and its event flags are never read.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "active": {
            "type": "boolean",
            "description": "True only when the slot's activity flag is exactly 1."
          },
          "cookbooks": {
            "type": "array",
            "description": "The cookbooks that passed availabilityFilter, in the deterministic order. Empty, never null.",
            "items": {
              "$ref": "#/components/schemas/CookbookEntry"
            }
          }
        },
        "required": ["saveSessionID", "characterID", "active", "cookbooks"]
      },
      "BellBearingEntry": {
        "type": "object",
        "description": "One goods ItemDocument that declares exactly one bell_bearing unlock, together with its acquisition state. The raw event flag is not exposed.",
        "properties": {
          "kind": {
            "type": "string",
            "enum": ["item"]
          },
          "key": {
            "type": "string",
            "pattern": "^[0-9A-F]{8}$"
          },
          "name": {
            "type": "string",
            "minLength": 1
          },
          "category": {
            "type": "string",
            "minLength": 1
          },
          "unlocked": {
            "type": "boolean",
            "description": "True when the Bell Bearing acquisition flag is set."
          }
        },
        "required": ["kind", "key", "name", "category", "unlocked"]
      },
      "GetBellBearingsResult": {
        "type": "object",
        "description": "The Bell Bearings of one character slot that passed availabilityFilter, ordered by category, name and key. The array is empty rather than null.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32",
            "minimum": 0,
            "maximum": 9
          },
          "active": {
            "type": "boolean"
          },
          "bellBearings": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/BellBearingEntry"
            }
          }
        },
        "required": ["saveSessionID", "characterID", "active", "bellBearings"]
      },
      "SetBellBearingUnlockedRequest": {
        "type": "object",
        "description": "The request body for setting a Bell Bearing handed-in state.",
        "additionalProperties": false,
        "properties": {
          "bellBearingKind": {
            "type": "string",
            "enum": ["item"],
            "description": "The public GameCatalog resource kind."
          },
          "bellBearingKey": {
            "type": "string",
            "pattern": "^[0-9A-F]{8}$",
            "description": "The public 8-digit hex resource key of the Bell Bearing."
          },
          "unlocked": {
            "type": "boolean",
            "description": "True to mark the Bell Bearing as handed in and consume matching records; false to clear only the acquisition flag."
          },
          "expectedRevision": {
            "type": "string",
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "The canonical decimal save revision string expected by the caller."
          }
        },
        "required": ["bellBearingKind", "bellBearingKey", "unlocked", "expectedRevision"]
      },
      "SetBellBearingUnlockedResult": {
        "type": "object",
        "description": "The receipt returned after a successful Bell Bearing handed-in state mutation.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "bellBearingKind": {
            "type": "string"
          },
          "bellBearingKey": {
            "type": "string"
          },
          "unlocked": {
            "type": "boolean"
          }
        },
        "required": ["saveSessionID", "saveRevision", "characterID", "bellBearingKind", "bellBearingKey", "unlocked"]
      },
      "ColosseumEntry": {
        "type": "object",
        "description": "One colosseum ColosseumDocument reduced to its public identity together with its logical unlock state. The entry carries no unlock event flag; the full document does.",
        "properties": {
          "kind": {
            "type": "string",
            "enum": ["colosseum"]
          },
          "key": {
            "type": "string",
            "pattern": "^[a-z0-9_]+$"
          },
          "name": {
            "type": "string",
            "minLength": 1
          },
          "unlocked": {
            "type": "boolean",
            "description": "True when the declared unlock event flag is set in the character slot."
          }
        },
        "required": ["kind", "key", "name", "unlocked"]
      },
      "GetColosseumsResult": {
        "type": "object",
        "description": "The colosseums of one character slot, ordered by name and then key. The array is empty rather than null.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32",
            "minimum": 0,
            "maximum": 9
          },
          "active": {
            "type": "boolean"
          },
          "colosseums": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ColosseumEntry"
            }
          }
        },
        "required": ["saveSessionID", "characterID", "active", "colosseums"]
      },
      "RegionEntry": {
        "type": "object",
        "description": "One curated RegionDocument reduced to its public identity and its exact membership state in UnlockedRegions. The raw region ID stays private.",
        "properties": {
          "kind": {
            "type": "string",
            "enum": ["region"]
          },
          "key": {
            "type": "string",
            "pattern": "^[a-z0-9_]+$"
          },
          "name": {
            "type": "string",
            "minLength": 1
          },
          "area": {
            "type": "string",
            "minLength": 1
          },
          "unlocked": {
            "type": "boolean",
            "description": "True when the internal region ID is present in the character's UnlockedRegions list."
          }
        },
        "required": ["kind", "key", "name", "area", "unlocked"]
      },
      "GetRegionsResult": {
        "type": "object",
        "description": "The curated regions of one character slot, ordered by area, name and key. The array is empty rather than null.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32",
            "minimum": 0,
            "maximum": 9
          },
          "active": {
            "type": "boolean"
          },
          "regions": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RegionEntry"
            }
          }
        },
        "required": ["saveSessionID", "characterID", "active", "regions"]
      },
      "SummoningPoolEntry": {
        "type": "object",
        "description": "One curated SummoningPoolDocument reduced to its public identity, its curated region label and its activation state. The raw activation event flag ID stays private.",
        "properties": {
          "kind": {
            "type": "string",
            "enum": ["summoning_pool"]
          },
          "key": {
            "type": "string",
            "pattern": "^[a-z0-9_]+$"
          },
          "name": {
            "type": "string",
            "minLength": 1
          },
          "regionLabel": {
            "type": "string",
            "minLength": 1,
            "description": "Curated presentation and grouping label. It is not a resource reference and never resolves to a RegionDocument."
          },
          "activated": {
            "type": "boolean",
            "description": "True when the activation event flag of the pool is set in the character slot."
          }
        },
        "required": ["kind", "key", "name", "regionLabel", "activated"]
      },
      "GetSummoningPoolsResult": {
        "type": "object",
        "description": "The curated summoning pools of one character slot, ordered by regionLabel, name and key. The array is empty rather than null.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32",
            "minimum": 0,
            "maximum": 9
          },
          "active": {
            "type": "boolean"
          },
          "summoningPools": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SummoningPoolEntry"
            }
          }
        },
        "required": ["saveSessionID", "characterID", "active", "summoningPools"]
      },
      "WhetbladeEntry": {
        "type": "object",
        "description": "One goods ItemDocument that declares exactly one whetblade unlock, together with its logical unlock state. Raw event flag and goods identifiers are not exposed.",
        "properties": {
          "kind": {
            "type": "string",
            "enum": ["item"]
          },
          "key": {
            "type": "string",
            "pattern": "^[0-9A-F]{8}$"
          },
          "name": {
            "type": "string",
            "minLength": 1
          },
          "unlocked": {
            "type": "boolean",
            "description": "True when the main event flag is set or a positive-quantity copy exists in common or key InventoryHeld."
          }
        },
        "required": ["kind", "key", "name", "unlocked"]
      },
      "GetWhetbladesResult": {
        "type": "object",
        "description": "The Whetblades of one character slot that passed availabilityFilter, ordered by name and then key. The array is empty rather than null.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32",
            "minimum": 0,
            "maximum": 9
          },
          "active": {
            "type": "boolean"
          },
          "whetblades": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/WhetbladeEntry"
            }
          }
        },
        "required": ["saveSessionID", "characterID", "active", "whetblades"]
      },
      "SetWhetbladeUnlockedRequest": {
        "type": "object",
        "description": "The request body for setting one Whetblade state.",
        "additionalProperties": false,
        "properties": {
          "whetbladeKind": {
            "type": "string",
            "enum": ["item"],
            "description": "The public GameCatalog resource kind."
          },
          "whetbladeKey": {
            "type": "string",
            "pattern": "^[0-9A-F]{8}$",
            "description": "The public 8-digit hex resource key of the Whetblade."
          },
          "unlocked": {
            "type": "boolean",
            "description": "True to add and unlock the Whetblade; false to remove and lock it."
          },
          "expectedRevision": {
            "type": "string",
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "The canonical decimal save revision string expected by the caller."
          }
        },
        "required": ["whetbladeKind", "whetbladeKey", "unlocked", "expectedRevision"]
      },
      "SetWhetbladeUnlockedResult": {
        "type": "object",
        "description": "The receipt returned after a successful Whetblade state mutation.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "whetbladeKind": {
            "type": "string"
          },
          "whetbladeKey": {
            "type": "string"
          },
          "unlocked": {
            "type": "boolean"
          }
        },
        "required": ["saveSessionID", "saveRevision", "characterID", "whetbladeKind", "whetbladeKey", "unlocked"]
      },
      "SetCookbookUnlockedRequest": {
        "type": "object",
        "description": "The request body for setting a cookbook unlock state.",
        "additionalProperties": false,
        "properties": {
          "cookbookKind": {
            "type": "string",
            "enum": ["item"],
            "description": "The public GameCatalog resource kind (e.g. item)."
          },
          "cookbookKey": {
            "type": "string",
            "pattern": "^[0-9A-F]{8}$",
            "description": "The public 8-digit hex resource key of the cookbook."
          },
          "unlocked": {
            "type": "boolean",
            "description": "True to set the unlock bit, false to clear it."
          },
          "expectedRevision": {
            "type": "string",
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "The canonical decimal save revision string expected by the caller."
          }
        },
        "required": ["cookbookKind", "cookbookKey", "unlocked", "expectedRevision"]
      },
      "SetCookbookUnlockedResult": {
        "type": "object",
        "description": "The receipt returned upon successful cookbook unlock mutation.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "cookbookKind": {
            "type": "string"
          },
          "cookbookKey": {
            "type": "string"
          },
          "unlocked": {
            "type": "boolean"
          }
        },
        "required": ["saveSessionID", "saveRevision", "characterID", "cookbookKind", "cookbookKey", "unlocked"]
      },
      "GestureEntry": {
        "type": "object",
        "description": "One gesture GameCatalog knows, together with whether the character has it. kind and key are the public GameCatalog identity of the resource and slotID is the canonical save slot ID of this one gesture, because a resource may declare more than one gesture slot. No provenance, no source record, no icon and no raw save byte is exposed.",
        "properties": {
          "kind": {
            "type": "string",
            "description": "The GameCatalog resource kind. Always item for a gesture."
          },
          "key": {
            "type": "string",
            "description": "The GameCatalog resource key. It repeats when one resource declares more than one gesture slot."
          },
          "slotID": {
            "type": "integer",
            "format": "int64",
            "description": "The canonical save slot ID written into GestureGameData. It is the value the unlock decision matches exactly."
          },
          "name": {
            "type": "string",
            "description": "The gesture name stored in GameCatalog. Never empty; a missing name is a fail-closed error."
          },
          "category": {
            "type": "string",
            "description": "The gesture category stored in GameCatalog. Never empty; a missing category is a fail-closed error."
          },
          "unlocked": {
            "type": "boolean",
            "description": "True when this canonical slotID is present in the raw 64-record GestureGameData block of the requested slot."
          }
        },
        "required": ["kind", "key", "slotID", "name", "category", "unlocked"]
      },
      "GetGesturesResult": {
        "type": "object",
        "description": "The gestures of one character slot that passed availabilityFilter, ordered by category, then name, then slotID. The order never depends on the unlock state. An inactive or residual slot reports active false and every gesture locked, and its GestureGameData is never read.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "active": {
            "type": "boolean",
            "description": "True only when the slot's activity flag is exactly 1."
          },
          "gestures": {
            "type": "array",
            "description": "The gestures that passed availabilityFilter, in the deterministic order. Empty, never null.",
            "items": {
              "$ref": "#/components/schemas/GestureEntry"
            }
          }
        },
        "required": ["saveSessionID", "characterID", "active", "gestures"]
      },
      "SetGestureUnlockedRequest": {
        "type": "object",
        "description": "The exact public identity and desired state of one logical gesture resource.",
        "additionalProperties": false,
        "properties": {
          "gestureKind": {
            "type": "string",
            "enum": ["item"],
            "description": "The public GameCatalog resource kind."
          },
          "gestureKey": {
            "type": "string",
            "pattern": "^[0-9A-F]{8}$",
            "description": "The public 8-digit hexadecimal resource key of the gesture."
          },
          "unlocked": {
            "type": "boolean",
            "description": "The desired logical unlock state."
          },
          "expectedRevision": {
            "type": "string",
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "The canonical decimal save revision expected by the caller."
          }
        },
        "required": ["gestureKind", "gestureKey", "unlocked", "expectedRevision"]
      },
      "SetGestureUnlockedResult": {
        "type": "object",
        "description": "The committed logical gesture state and the new save revision. The physical slot ID remains internal.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string"
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "gestureKind": {
            "type": "string"
          },
          "gestureKey": {
            "type": "string"
          },
          "unlocked": {
            "type": "boolean"
          }
        },
        "required": ["saveSessionID", "saveRevision", "characterID", "gestureKind", "gestureKey", "unlocked"]
      },
      "StorageRecord": {
        "type": "object",
        "description": "One non-empty physical Storage Box record, its opaque owned-item identity and its resolved ItemDocument identity. kind and key are the canonical catalog resource reference; gameID is the exact catalog game ID resolved from the save and selects a materialised variant where applicable. containerSection and physicalIndex identify the physical row it currently occupies and are deliberately not an item identity, because a row moves when the game rewrites the section. gaItemHandle and acquisitionIndex are raw stored values, while quantity has its high bit removed by the 0x7FFFFFFF mask.",
        "properties": {
          "ownedItemID": {
            "type": "string",
            "minLength": 1,
            "description": "Opaque identity of this physical record, valid only for the saveRevision of the enclosing result and only inside the save session that produced it. Compare it byte for byte; never parse, trim, normalise or persist it. It encodes no handle, no index and no slot address, and a Storage record never shares it with an Inventory record at the same coordinates."
          },
          "kind": {
            "type": "string",
            "enum": ["item"],
            "description": "The GameCatalog resource kind of the resolved item."
          },
          "key": {
            "type": "string",
            "minLength": 1,
            "description": "The canonical GameCatalog resource key of the resolved item."
          },
          "gameID": {
            "type": "integer",
            "format": "int64",
            "description": "The exact GameCatalog game ID resolved from the save. It selects an item variant where applicable."
          },
          "containerSection": {
            "type": "string",
            "enum": ["common", "key"],
            "description": "The physical section the record was read from."
          },
          "physicalIndex": {
            "type": "integer",
            "format": "int32",
            "description": "Position of the record inside its own section, counted from 0."
          },
          "gaItemHandle": {
            "type": "integer",
            "format": "int64",
            "description": "The raw stored GaItem handle. It is not normalised, validated or resolved against GameCatalog."
          },
          "quantity": {
            "type": "integer",
            "format": "int64",
            "description": "The stored quantity with the high bit masked off, because that bit is not part of the count."
          },
          "acquisitionIndex": {
            "type": "integer",
            "format": "int64",
            "description": "The raw stored acquisition index."
          }
        },
        "required": ["ownedItemID", "kind", "key", "gameID", "containerSection", "physicalIndex", "gaItemHandle", "quantity", "acquisitionIndex"]
      },
      "CharacterStorage": {
        "type": "object",
        "description": "One page of resolved Storage Box records of one character slot. Every active record carries an ItemDocument kind, canonical key and exact resolved gameID; the remaining physical fields and paging rules are preserved. An inactive or residual slot reports active false, total 0 and an empty array, mints no identity, and still reports the current saveRevision.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "The revision this result was read under, and the one every ownedItemID in it was minted under. A non-empty decimal string with no sign, prefix, padding or separator. It is deliberately not a JSON number, because a uint64 above 2^53-1 would round in a JavaScript client. Carry it back unchanged as expectedRevision and compare it byte for byte; never parse, increment or order it."
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "active": {
            "type": "boolean"
          },
          "records": {
            "type": "array",
            "description": "The requested page in physical native order, the common section first and then the key section. Empty, never null, when the slot is inactive or the page lies beyond the last one.",
            "items": {
              "$ref": "#/components/schemas/StorageRecord"
            }
          },
          "total": {
            "type": "integer",
            "description": "Number of non-empty records that passed the section filter, counted before paging."
          },
          "page": {
            "type": "integer",
            "description": "The effective one-based page, so a requested 0 is reported as 1."
          },
          "pageSize": {
            "type": "integer",
            "description": "The effective page size, so a requested 0 is reported as 50."
          }
        },
        "required": ["saveSessionID", "saveRevision", "characterID", "active", "records", "total", "page", "pageSize"]
      },
      "OwnedItem": {
        "type": "object",
        "description": "One owned item instance addressed by its opaque ownedItemID, with its resolved ItemDocument identity. kind and key are the canonical catalog resource reference; gameID is the exact catalog game ID resolved from the save and selects a materialised variant where applicable. container, containerSection and physicalIndex describe the physical row the instance currently occupies and are deliberately not an item identity, because a row moves when the game rewrites the section. gaItemHandle and acquisitionIndex are raw stored values, while quantity has its high bit removed by the 0x7FFFFFFF mask.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "The revision this result was read under, and the one the ownedItemID is valid for. A non-empty decimal string with no sign, prefix, padding or separator. It is deliberately not a JSON number, because a uint64 above 2^53-1 would round in a JavaScript client. Carry it back unchanged as expectedRevision and compare it byte for byte; never parse, increment or order it."
          },
          "ownedItemID": {
            "type": "string",
            "minLength": 1,
            "description": "The requested identity, echoed back unchanged. It stays opaque and is scoped to this save session and this saveRevision."
          },
          "characterID": {
            "type": "integer",
            "format": "int32"
          },
          "kind": {
            "type": "string",
            "enum": ["item"],
            "description": "The GameCatalog resource kind of the resolved item."
          },
          "key": {
            "type": "string",
            "minLength": 1,
            "description": "The canonical GameCatalog resource key of the resolved item."
          },
          "gameID": {
            "type": "integer",
            "format": "int64",
            "description": "The exact GameCatalog game ID resolved from the save. It selects an item variant where applicable."
          },
          "container": {
            "type": "string",
            "enum": ["inventory", "storage"],
            "description": "The physical container the instance lives in. It is part of the identity: the same coordinates in the other container are a different record."
          },
          "containerSection": {
            "type": "string",
            "enum": ["common", "key"],
            "description": "The physical section the record was read from."
          },
          "physicalIndex": {
            "type": "integer",
            "format": "int32",
            "description": "Position of the record inside its own section, counted from 0."
          },
          "gaItemHandle": {
            "type": "integer",
            "format": "int64",
            "description": "The raw stored GaItem handle. It is not normalised, validated or resolved against GameCatalog."
          },
          "quantity": {
            "type": "integer",
            "format": "int64",
            "description": "The stored quantity with the high bit masked off, because that bit is not part of the count."
          },
          "acquisitionIndex": {
            "type": "integer",
            "format": "int64",
            "description": "The raw stored acquisition index."
          }
        },
        "required": ["saveSessionID", "saveRevision", "ownedItemID", "characterID", "kind", "key", "gameID", "container", "containerSection", "physicalIndex", "gaItemHandle", "quantity", "acquisitionIndex"]
      },
      "ItemCapacity": {
        "type": "object",
        "description": "Read-only item-add preflight. Empty limitingFactor means every checked budget fits; no capacity is reserved.",
        "properties": {
          "saveSessionID": {"type": "string", "minLength": 1},
          "saveRevision": {"type": "string", "pattern": "^(0|[1-9][0-9]*)$"},
          "characterID": {"type": "integer", "format": "int32", "minimum": 0, "maximum": 9},
          "active": {"type": "boolean"},
          "destination": {"type": "string", "enum": ["inventory", "storage"]},
          "kind": {"type": "string", "minLength": 1},
          "key": {"type": "string", "minLength": 1},
          "gameID": {"type": "integer", "format": "int64", "minimum": 0, "maximum": 4294967295},
          "quantity": {"type": "integer", "format": "int64", "minimum": 1, "maximum": 4294967295},
          "canFit": {"type": "boolean"},
          "limitingFactor": {
            "type": "string",
            "enum": ["", "per_record_quantity", "container_quantity", "physical_records", "acquisition_allocator", "gaitemdata_entries"]
          },
          "currentQuantity": {"type": "integer", "format": "int64", "minimum": 0},
          "quantityAfter": {"type": "integer", "format": "int64", "minimum": 0},
          "maxContainerQuantity": {"type": "integer", "format": "int64", "minimum": 1, "maximum": 4294967295},
          "freePhysicalRecords": {"type": "integer", "format": "int32", "minimum": 0},
          "physicalRecordsRequired": {"type": "integer", "format": "int32", "minimum": 0, "maximum": 1},
          "freeGaItemDataEntries": {"type": "integer", "format": "int32", "minimum": 0, "maximum": 7000},
          "gaItemDataEntriesRequired": {"type": "integer", "format": "int32", "minimum": 0, "maximum": 1}
        },
        "required": ["saveSessionID", "saveRevision", "characterID", "active", "destination", "kind", "key", "gameID", "quantity", "canFit", "limitingFactor", "currentQuantity", "quantityAfter", "maxContainerQuantity", "freePhysicalRecords", "physicalRecordsRequired", "freeGaItemDataEntries", "gaItemDataEntriesRequired"]
      },
      "AddItemToInventoryRequest": {
        "type": "object",
        "description": "Body of AddItemToInventory. Every value reaches the endpoint unchanged; unknown fields are rejected.",
        "additionalProperties": false,
        "properties": {
          "kind": {
            "type": "string",
            "minLength": 1,
            "description": "Resource kind, matched exactly and never normalised."
          },
          "key": {
            "type": "string",
            "minLength": 1,
            "description": "Resource key inside that kind, matched exactly and never normalised."
          },
          "variantID": {
            "type": "integer",
            "format": "int64",
            "minimum": 0,
            "maximum": 4294967295,
            "nullable": true,
            "description": "Optional variant game ID. Absent or null selects the base item document; a value selects exactly the stored variant of the same (kind, key) pair that declares it."
          },
          "quantity": {
            "type": "integer",
            "format": "int64",
            "minimum": 1,
            "maximum": 2147483647,
            "description": "Amount added, never a target total. It is never clamped; the resolved per-record and container limits may impose a lower maximum, and a separate-instances resource accepts 1 only."
          },
          "expectedRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Current canonical decimal saveRevision, compared exactly and never normalised."
          }
        },
        "required": ["kind", "key", "quantity", "expectedRevision"]
      },
      "AddItemToInventoryResult": {
        "type": "object",
        "description": "Receipt for one committed add. It carries no ownedItemID: the successful mutation advanced saveRevision and retired every identity of the previous one, so the row is addressed again by reading the container back.",
        "properties": {
          "saveSessionID": {
            "type": "string",
            "minLength": 1
          },
          "saveRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$"
          },
          "characterID": {
            "type": "integer",
            "format": "int32",
            "minimum": 0,
            "maximum": 9
          },
          "gameID": {
            "type": "integer",
            "format": "int64",
            "minimum": 0,
            "maximum": 4294967295,
            "description": "Save-side game ID of the resolved resource or variant."
          },
          "added": {
            "type": "integer",
            "format": "int64",
            "minimum": 1,
            "maximum": 2147483647,
            "description": "The delta that was applied."
          },
          "quantity": {
            "type": "integer",
            "format": "int64",
            "minimum": 1,
            "maximum": 2147483647,
            "description": "The value the written record stores now."
          },
          "createdRecord": {
            "type": "boolean",
            "description": "True when a new record was opened, false when an existing one was topped up."
          },
          "containerSection": {
            "type": "string",
            "enum": ["common"],
            "description": "Always the common section; this endpoint never writes the key section."
          },
          "physicalIndex": {
            "type": "integer",
            "format": "int32",
            "minimum": 0,
            "description": "Physical row of the written record inside the common section."
          }
        },
        "required": ["saveSessionID", "saveRevision", "characterID", "gameID", "added", "quantity", "createdRecord", "containerSection", "physicalIndex"]
      },
      "AddItemToStorageRequest": {
        "type": "object",
        "description": "Body of AddItemToStorage. Every value reaches the endpoint unchanged; unknown fields are rejected.",
        "additionalProperties": false,
        "properties": {
          "kind": {
            "type": "string",
            "minLength": 1,
            "description": "Resource kind, matched exactly and never normalised."
          },
          "key": {
            "type": "string",
            "minLength": 1,
            "description": "Resource key inside that kind, matched exactly and never normalised."
          },
          "variantID": {
            "type": "integer",
            "format": "int64",
            "minimum": 0,
            "maximum": 4294967295,
            "nullable": true,
            "description": "Optional variant game ID. Absent or null selects the base item document; a value selects exactly the stored variant of the same (kind, key) pair that declares it."
          },
          "quantity": {
            "type": "integer",
            "format": "int64",
            "minimum": 1,
            "maximum": 2147483647,
            "description": "Amount added, never a target total. It is never clamped; maxStorage may impose a lower maximum, and a separate-instances resource accepts 1 only."
          },
          "expectedRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Current canonical decimal saveRevision, compared exactly and never normalised."
          }
        },
        "required": ["kind", "key", "quantity", "expectedRevision"]
      },
      "AddItemToStorageResult": {
        "type": "object",
        "description": "Receipt for one committed common-Storage add. It carries physical coordinates rather than an ownedItemID because the mutation retires every identity of the previous revision.",
        "properties": {
          "saveSessionID": {
            "type": "string",
            "minLength": 1
          },
          "saveRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$"
          },
          "characterID": {
            "type": "integer",
            "format": "int32",
            "minimum": 0,
            "maximum": 9
          },
          "gameID": {
            "type": "integer",
            "format": "int64",
            "minimum": 0,
            "maximum": 4294967295
          },
          "added": {
            "type": "integer",
            "format": "int64",
            "minimum": 1,
            "maximum": 2147483647
          },
          "quantity": {
            "type": "integer",
            "format": "int64",
            "minimum": 1,
            "maximum": 2147483647
          },
          "createdRecord": {
            "type": "boolean"
          },
          "containerSection": {
            "type": "string",
            "enum": ["common"]
          },
          "physicalIndex": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          }
        },
        "required": ["saveSessionID", "saveRevision", "characterID", "gameID", "added", "quantity", "createdRecord", "containerSection", "physicalIndex"]
      },
      "RemoveOwnedItemRequest": {
        "type": "object",
        "description": "Body of RemoveOwnedItem. The single value reaches the endpoint unchanged; unknown fields are rejected.",
        "additionalProperties": false,
        "properties": {
          "expectedRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Current canonical decimal saveRevision, compared exactly and never normalised."
          }
        },
        "required": ["expectedRevision"]
      },
      "RemoveOwnedItemResult": {
        "type": "object",
        "description": "Receipt for one committed removal. The echoed ownedItemID is already stale: the successful mutation advanced saveRevision, and the record it addressed no longer exists.",
        "properties": {
          "saveSessionID": {
            "type": "string",
            "minLength": 1
          },
          "saveRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$"
          },
          "ownedItemID": {
            "type": "string",
            "minLength": 1,
            "description": "The identifier the removal was performed with, echoed unchanged and already stale."
          },
          "characterID": {
            "type": "integer",
            "format": "int32",
            "minimum": 0,
            "maximum": 9
          },
          "gameID": {
            "type": "integer",
            "format": "int64",
            "minimum": 0,
            "maximum": 4294967295,
            "description": "The save-side game ID the removed record's GaItem handle resolved to under the mutation lock."
          }
        },
        "required": ["saveSessionID", "saveRevision", "ownedItemID", "characterID", "gameID"]
      },
      "SetOwnedItemQuantityRequest": {
        "type": "object",
        "description": "Body of SetOwnedItemQuantity. Both values reach the endpoint unchanged; unknown fields are rejected.",
        "additionalProperties": false,
        "properties": {
          "quantity": {
            "type": "integer",
            "format": "int64",
            "minimum": 1,
            "maximum": 2147483647,
            "description": "Requested stored quantity. The record stores it in 31 bits, because the high bit 0x80000000 of the raw field is a preserved flag and not part of the count. It is never clamped; the resolved item and container limits may impose a lower maximum."
          },
          "expectedRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Current canonical decimal saveRevision, compared exactly and never normalised."
          }
        },
        "required": ["quantity", "expectedRevision"]
      },
      "SetOwnedItemQuantityResult": {
        "type": "object",
        "description": "Receipt for one committed quantity mutation. The echoed ownedItemID is already stale because the successful mutation advanced saveRevision.",
        "properties": {
          "saveSessionID": {
            "type": "string",
            "minLength": 1
          },
          "saveRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$"
          },
          "ownedItemID": {
            "type": "string",
            "minLength": 1,
            "description": "The identifier used for the mutation, echoed unchanged and already stale."
          },
          "characterID": {
            "type": "integer",
            "format": "int32",
            "minimum": 0,
            "maximum": 9
          },
          "quantity": {
            "type": "integer",
            "format": "int64",
            "minimum": 1,
            "maximum": 2147483647
          }
        },
        "required": ["saveSessionID", "saveRevision", "ownedItemID", "characterID", "quantity"]
      },
      "SetInventoryOrderRequest": {
        "type": "object",
        "description": "Complete permutation of the supported common Inventory instances and the optimistic-concurrency revision. Unknown fields are rejected.",
        "additionalProperties": false,
        "properties": {
          "orderedOwnedItemIDs": {
            "type": "array",
            "minItems": 1,
            "maxItems": 2688,
            "uniqueItems": true,
            "description": "Every current supported common Inventory ownedItemID exactly once, in desired logical order.",
            "items": {
              "type": "string",
              "minLength": 1
            }
          },
          "expectedRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Current canonical decimal saveRevision, compared exactly and never normalised."
          }
        },
        "required": ["orderedOwnedItemIDs", "expectedRevision"]
      },
      "SetInventoryOrderResult": {
        "type": "object",
        "description": "Receipt for one committed complete supported Inventory order. Every ownedItemID used by the request is stale after the revision advance.",
        "properties": {
          "saveSessionID": {
            "type": "string",
            "minLength": 1
          },
          "saveRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$"
          },
          "characterID": {
            "type": "integer",
            "format": "int32",
            "minimum": 0,
            "maximum": 9
          },
          "orderedResources": {
            "type": "array",
            "minItems": 1,
            "maxItems": 2688,
            "description": "Committed order expressed with stable GameCatalog resource identities.",
            "items": {
              "$ref": "#/components/schemas/ResourceRef"
            }
          },
          "acquisitionIndices": {
            "type": "array",
            "minItems": 1,
            "maxItems": 2688,
            "description": "Fresh native acquisition indices assigned in the same order as orderedResources.",
            "items": {
              "type": "integer",
              "format": "int64",
              "minimum": 434,
              "maximum": 9999
            }
          }
        },
        "required": [
          "saveSessionID",
          "saveRevision",
          "characterID",
          "orderedResources",
          "acquisitionIndices"
        ]
      },
      "SetStorageOrderRequest": {
        "type": "object",
        "description": "Complete permutation of the supported common Storage instances and the optimistic-concurrency revision. Unknown fields are rejected.",
        "additionalProperties": false,
        "properties": {
          "orderedOwnedItemIDs": {
            "type": "array",
            "minItems": 1,
            "maxItems": 1920,
            "uniqueItems": true,
            "description": "Every current supported common Storage ownedItemID exactly once, in desired logical order.",
            "items": {
              "type": "string",
              "minLength": 1
            }
          },
          "expectedRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Current canonical decimal saveRevision, compared exactly and never normalised."
          }
        },
        "required": ["orderedOwnedItemIDs", "expectedRevision"]
      },
      "SetStorageOrderResult": {
        "type": "object",
        "description": "Receipt for one committed complete supported Storage order. Every ownedItemID used by the request is stale after the revision advance.",
        "properties": {
          "saveSessionID": {
            "type": "string",
            "minLength": 1
          },
          "saveRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$"
          },
          "characterID": {
            "type": "integer",
            "format": "int32",
            "minimum": 0,
            "maximum": 9
          },
          "orderedResources": {
            "type": "array",
            "minItems": 1,
            "maxItems": 1920,
            "description": "Committed order expressed with stable GameCatalog resource identities.",
            "items": {
              "$ref": "#/components/schemas/ResourceRef"
            }
          },
          "acquisitionIndices": {
            "type": "array",
            "minItems": 1,
            "maxItems": 1920,
            "description": "Fresh native acquisition indices assigned in the same order as orderedResources.",
            "items": {
              "type": "integer",
              "format": "int64",
              "minimum": 434,
              "maximum": 9999
            }
          }
        },
        "required": [
          "saveSessionID",
          "saveRevision",
          "characterID",
          "orderedResources",
          "acquisitionIndices"
        ]
      },
      "MoveOwnedItemToInventoryRequest": {
        "type": "object",
        "description": "Body of MoveOwnedItemToInventory. Both values reach the endpoint unchanged; unknown fields are rejected.",
        "additionalProperties": false,
        "properties": {
          "targetPosition": {
            "type": "integer",
            "format": "int32",
            "minimum": 0,
            "maximum": 2688,
            "description": "Zero-based insertion position in the logical acquisition-index order of common Inventory. The current record count is the append position."
          },
          "expectedRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Current canonical decimal saveRevision, compared exactly and never normalised."
          }
        },
        "required": ["targetPosition", "expectedRevision"]
      },
      "MoveOwnedItemToInventoryResult": {
        "type": "object",
        "description": "Receipt for one committed Storage-to-Inventory move. The echoed ownedItemID is already stale because the successful mutation advanced saveRevision.",
        "properties": {
          "saveSessionID": {"type": "string", "minLength": 1},
          "saveRevision": {"type": "string", "minLength": 1, "pattern": "^(0|[1-9][0-9]*)$"},
          "ownedItemID": {"type": "string", "minLength": 1},
          "characterID": {"type": "integer", "format": "int32", "minimum": 0, "maximum": 9},
          "gameID": {"type": "integer", "format": "int64", "minimum": 0, "maximum": 4294967295},
          "quantity": {"type": "integer", "format": "int64", "minimum": 1, "maximum": 2147483647},
          "containerSection": {"type": "string", "enum": ["common"]},
          "targetPosition": {"type": "integer", "format": "int32", "minimum": 0, "maximum": 2688},
          "physicalIndex": {"type": "integer", "format": "int32", "minimum": 0, "maximum": 2687},
          "acquisitionIndex": {"type": "integer", "format": "int64", "minimum": 0, "maximum": 4294967295}
        },
        "required": ["saveSessionID", "saveRevision", "ownedItemID", "characterID", "gameID", "quantity", "containerSection", "targetPosition", "physicalIndex", "acquisitionIndex"]
      },
      "MoveOwnedItemToStorageRequest": {
        "type": "object",
        "description": "Body of MoveOwnedItemToStorage. Both values reach the endpoint unchanged; unknown fields are rejected.",
        "additionalProperties": false,
        "properties": {
          "targetPosition": {
            "type": "integer",
            "format": "int32",
            "minimum": 0,
            "maximum": 1920,
            "description": "Zero-based insertion position among the non-empty common Storage records. The current record count is the append position."
          },
          "expectedRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Current canonical decimal saveRevision, compared exactly and never normalised."
          }
        },
        "required": ["targetPosition", "expectedRevision"]
      },
      "MoveOwnedItemToStorageResult": {
        "type": "object",
        "description": "Receipt for one committed Inventory-to-Storage move. The echoed ownedItemID is already stale because the successful mutation advanced saveRevision.",
        "properties": {
          "saveSessionID": {"type": "string", "minLength": 1},
          "saveRevision": {"type": "string", "minLength": 1, "pattern": "^(0|[1-9][0-9]*)$"},
          "ownedItemID": {"type": "string", "minLength": 1},
          "characterID": {"type": "integer", "format": "int32", "minimum": 0, "maximum": 9},
          "gameID": {"type": "integer", "format": "int64", "minimum": 0, "maximum": 4294967295},
          "quantity": {"type": "integer", "format": "int64", "minimum": 1, "maximum": 2147483647},
          "containerSection": {"type": "string", "enum": ["common"]},
          "targetPosition": {"type": "integer", "format": "int32", "minimum": 0, "maximum": 1920},
          "physicalIndex": {"type": "integer", "format": "int32", "minimum": 0, "maximum": 1919},
          "acquisitionIndex": {"type": "integer", "format": "int64", "minimum": 0, "maximum": 4294967295}
        },
        "required": ["saveSessionID", "saveRevision", "ownedItemID", "characterID", "gameID", "quantity", "containerSection", "targetPosition", "physicalIndex", "acquisitionIndex"]
      },
      "SetWeaponUpgradeLevelRequest": {
        "type": "object",
        "description": "Body of SetWeaponUpgradeLevel. Both values reach the endpoint unchanged; unknown fields are rejected.",
        "additionalProperties": false,
        "properties": {
          "upgradeLevel": {
            "type": "integer",
            "format": "int32",
            "minimum": 0,
            "maximum": 25,
            "description": "Requested target level. The resolved weapon's catalog model may impose the lower somber maximum of 10."
          },
          "expectedRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Current canonical decimal saveRevision, compared exactly and never normalised."
          }
        },
        "required": ["upgradeLevel", "expectedRevision"]
      },
      "SetWeaponUpgradeLevelResult": {
        "type": "object",
        "description": "Receipt for one committed weapon upgrade-level mutation. The echoed ownedItemID is already stale because the successful mutation advanced saveRevision.",
        "properties": {
          "saveSessionID": {"type": "string", "minLength": 1},
          "saveRevision": {"type": "string", "minLength": 1, "pattern": "^(0|[1-9][0-9]*)$"},
          "ownedItemID": {"type": "string", "minLength": 1},
          "characterID": {"type": "integer", "format": "int32", "minimum": 0, "maximum": 9},
          "container": {"type": "string", "enum": ["inventory", "storage"]},
          "previousGameID": {"type": "integer", "format": "int64", "minimum": 0, "maximum": 268435455},
          "gameID": {"type": "integer", "format": "int64", "minimum": 0, "maximum": 268435455},
          "upgradeLevel": {"type": "integer", "format": "int32", "minimum": 0, "maximum": 25}
        },
        "required": ["saveSessionID", "saveRevision", "ownedItemID", "characterID", "container", "previousGameID", "gameID", "upgradeLevel"]
      },
      "SetSpiritAshUpgradeLevelRequest": {
        "type": "object",
        "description": "Body of SetSpiritAshUpgradeLevel. Both values reach the endpoint unchanged; unknown fields are rejected.",
        "additionalProperties": false,
        "properties": {
          "upgradeLevel": {
            "type": "integer",
            "format": "int32",
            "minimum": 0,
            "maximum": 10,
            "description": "Requested target level. Grave- and ghost-glovewort Spirit Ashes use the catalog maximum, currently 10."
          },
          "expectedRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Current canonical decimal saveRevision, compared exactly and never normalised."
          }
        },
        "required": ["upgradeLevel", "expectedRevision"]
      },
      "SetSpiritAshUpgradeLevelResult": {
        "type": "object",
        "description": "Receipt for one committed Spirit Ash upgrade-level mutation. The echoed ownedItemID is already stale because the successful mutation advanced saveRevision.",
        "properties": {
          "saveSessionID": {"type": "string", "minLength": 1},
          "saveRevision": {"type": "string", "minLength": 1, "pattern": "^(0|[1-9][0-9]*)$"},
          "ownedItemID": {"type": "string", "minLength": 1},
          "characterID": {"type": "integer", "format": "int32", "minimum": 0, "maximum": 9},
          "container": {"type": "string", "enum": ["inventory", "storage"]},
          "previousGameID": {"type": "integer", "format": "int64", "minimum": 1073741824, "maximum": 1342177279},
          "gameID": {"type": "integer", "format": "int64", "minimum": 1073741824, "maximum": 1342177279},
          "upgradeLevel": {"type": "integer", "format": "int32", "minimum": 0, "maximum": 10}
        },
        "required": ["saveSessionID", "saveRevision", "ownedItemID", "characterID", "container", "previousGameID", "gameID", "upgradeLevel"]
      },
      "SetWeaponInfusionRequest": {
        "type": "object",
        "description": "Body of SetWeaponInfusion. Both values reach the endpoint unchanged; unknown fields are rejected.",
        "additionalProperties": false,
        "properties": {
          "affinity": {
            "type": "string",
            "enum": ["standard", "heavy", "keen", "quality", "fire", "flame_art", "lightning", "sacred", "magic", "cold", "poison", "blood", "occult"],
            "description": "Requested target affinity. The resolved weapon's catalog capability may allow only a subset."
          },
          "expectedRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Current canonical decimal saveRevision, compared exactly and never normalised."
          }
        },
        "required": ["affinity", "expectedRevision"]
      },
      "SetWeaponInfusionResult": {
        "type": "object",
        "description": "Receipt for one committed weapon affinity mutation. The echoed ownedItemID is already stale because the successful mutation advanced saveRevision.",
        "properties": {
          "saveSessionID": {"type": "string", "minLength": 1},
          "saveRevision": {"type": "string", "minLength": 1, "pattern": "^(0|[1-9][0-9]*)$"},
          "ownedItemID": {"type": "string", "minLength": 1},
          "characterID": {"type": "integer", "format": "int32", "minimum": 0, "maximum": 9},
          "container": {"type": "string", "enum": ["inventory", "storage"]},
          "previousGameID": {"type": "integer", "format": "int64", "minimum": 0, "maximum": 268435455},
          "gameID": {"type": "integer", "format": "int64", "minimum": 0, "maximum": 268435455},
          "affinity": {"type": "string", "enum": ["standard", "heavy", "keen", "quality", "fire", "flame_art", "lightning", "sacred", "magic", "cold", "poison", "blood", "occult"]},
          "upgradeLevel": {"type": "integer", "format": "int32", "minimum": 0, "maximum": 25}
        },
        "required": ["saveSessionID", "saveRevision", "ownedItemID", "characterID", "container", "previousGameID", "gameID", "affinity", "upgradeLevel"]
      },
      "SetWeaponAshOfWarRequest": {
        "type": "object",
        "description": "Body of SetWeaponAshOfWar. Both selector fields are required. Two strings mount the exact resource; two nulls remove the custom Ash. Unknown fields and a string/null mismatch are rejected.",
        "additionalProperties": false,
        "properties": {
          "ashOfWarKind": {
            "type": "string",
            "nullable": true,
            "minLength": 1,
            "description": "Exact GameCatalog resource kind, or null for removal."
          },
          "ashOfWarKey": {
            "type": "string",
            "nullable": true,
            "minLength": 1,
            "description": "Exact GameCatalog key inside ashOfWarKind, or null for removal."
          },
          "expectedRevision": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(0|[1-9][0-9]*)$",
            "description": "Current canonical decimal saveRevision, compared exactly and never normalised."
          }
        },
        "required": ["ashOfWarKind", "ashOfWarKey", "expectedRevision"]
      },
      "SetWeaponAshOfWarResult": {
        "type": "object",
        "description": "Receipt for one committed weapon Ash of War mutation. Zero in either Ash game-ID field means no custom Ash. The echoed weaponOwnedItemID is already stale under the returned revision.",
        "properties": {
          "saveSessionID": {"type": "string", "minLength": 1},
          "saveRevision": {"type": "string", "minLength": 1, "pattern": "^(0|[1-9][0-9]*)$"},
          "weaponOwnedItemID": {"type": "string", "minLength": 1},
          "characterID": {"type": "integer", "format": "int32", "minimum": 0, "maximum": 9},
          "container": {"type": "string", "enum": ["inventory", "storage"]},
          "weaponGameID": {"type": "integer", "format": "int64", "minimum": 0, "maximum": 268435455},
          "previousAshOfWarGameID": {"type": "integer", "format": "int64", "minimum": 0, "maximum": 2415919103},
          "ashOfWarGameID": {"type": "integer", "format": "int64", "minimum": 0, "maximum": 2415919103}
        },
        "required": ["saveSessionID", "saveRevision", "weaponOwnedItemID", "characterID", "container", "weaponGameID", "previousAshOfWarGameID", "ashOfWarGameID"]
      },
      "Error": {
        "type": "object",
        "description": "Error envelope carrying the message returned by the endpoint.",
        "properties": {
          "error": {
            "type": "string"
          }
        },
        "required": ["error"]
      },
      "SupportedSchema": {
        "type": "object",
        "description": "One schema the backend can read and the version range it accepts for it.",
        "properties": {
          "name": {
            "type": "string"
          },
          "minimumVersion": {
            "type": "integer",
            "format": "int64"
          },
          "currentVersion": {
            "type": "integer",
            "format": "int64"
          }
        },
        "required": ["name", "minimumVersion", "currentVersion"]
      },
      "GetApplicationInfoResult": {
        "type": "object",
        "properties": {
          "applicationVersion": {
            "type": "string",
            "description": "Returned exactly as supplied by the backend caller."
          },
          "supportedSchemas": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SupportedSchema"
            }
          },
          "capabilities": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": ["applicationVersion", "supportedSchemas", "capabilities"]
      },
      "DataSource": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "kind": {
            "type": "string"
          },
          "location": {
            "type": "string"
          },
          "version": {
            "type": "string"
          },
          "evidence": {
            "type": "string"
          },
          "reviewed": {
            "type": "boolean"
          }
        },
        "required": ["id", "kind", "location", "version", "evidence", "reviewed"]
      },
      "GetCatalogInfoResult": {
        "type": "object",
        "properties": {
          "schemaVersion": {
            "type": "integer",
            "format": "int64"
          },
          "dataVersion": {
            "type": "string"
          },
          "gameVersion": {
            "type": "string"
          },
          "valid": {
            "type": "boolean",
            "description": "True only means schema.ValidateManifest accepted the manifest."
          },
          "sources": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DataSource"
            }
          }
        },
        "required": ["schemaVersion", "dataVersion", "gameVersion", "valid", "sources"]
      },
      "Resource": {
        "type": "object",
        "description": "schema.Resource identified by the (kind, key) pair. It is a union over the resource kinds: kind 'item' carries ItemDocument, kind 'colosseum' carries ColosseumDocument, kind 'region' carries RegionDocument and kind 'summoning_pool' carries SummoningPoolDocument. Documents of other kinds are absent, never null. The documents are passed through as stored in the catalog; the full shape of ItemDocument is owned by the catalog schema, not by this explorer.",
        "properties": {
          "key": {
            "type": "string"
          },
          "kind": {
            "type": "string",
            "enum": ["item", "colosseum", "region", "summoning_pool"]
          },
          "item": {
            "type": "object",
            "description": "The complete schema.ItemDocument as stored, with presentation, capabilities, safety, storage, acquisition, modifiers, links, variants, aliases, unlocks, related technical records, source records, the family block and full provenance. Present exactly when kind is 'item'.",
            "additionalProperties": true
          },
          "colosseum": {
            "$ref": "#/components/schemas/ColosseumDocument"
          },
          "region": {
            "$ref": "#/components/schemas/RegionDocument"
          },
          "summoningPool": {
            "$ref": "#/components/schemas/SummoningPoolDocument"
          }
        },
        "required": ["key", "kind"],
        "oneOf": [
          {
            "properties": {
              "kind": {
                "enum": ["item"]
              }
            },
            "required": ["item"],
            "not": {
              "anyOf": [
                {"required": ["colosseum"]},
                {"required": ["region"]},
                {"required": ["summoningPool"]}
              ]
            }
          },
          {
            "properties": {
              "kind": {
                "enum": ["colosseum"]
              }
            },
            "required": ["colosseum"],
            "not": {
              "anyOf": [
                {"required": ["item"]},
                {"required": ["region"]},
                {"required": ["summoningPool"]}
              ]
            }
          },
          {
            "properties": {
              "kind": {
                "enum": ["region"]
              }
            },
            "required": ["region"],
            "not": {
              "anyOf": [
                {"required": ["item"]},
                {"required": ["colosseum"]},
                {"required": ["summoningPool"]}
              ]
            }
          },
          {
            "properties": {
              "kind": {
                "enum": ["summoning_pool"]
              }
            },
            "required": ["summoningPool"],
            "not": {
              "anyOf": [
                {"required": ["item"]},
                {"required": ["colosseum"]},
                {"required": ["region"]}
              ]
            }
          }
        ]
      },
      "ColosseumDocument": {
        "type": "object",
        "description": "schema.ColosseumDocument. The arena name and the unlock event flag come from different sources, so each is a separate fact with its own provenance and neither is derived from the other.",
        "properties": {
          "name": {
            "type": "object",
            "description": "schema.Fact[string] holding the arena name the game itself shows.",
            "properties": {
              "known": {
                "type": "boolean",
                "enum": [true]
              },
              "value": {
                "type": "string",
                "minLength": 1
              },
              "provenance": {
                "type": "object",
                "additionalProperties": true
              }
            },
            "required": ["known", "value", "provenance"]
          },
          "unlockEventFlagID": {
            "type": "object",
            "description": "schema.Fact[uint32] holding the event flag whose set bit records the unlock in a save. GetColosseums does not report it; the full document does.",
            "properties": {
              "known": {
                "type": "boolean",
                "enum": [true]
              },
              "value": {
                "type": "integer",
                "format": "int64",
                "minimum": 1,
                "maximum": 4294967295
              },
              "provenance": {
                "type": "object",
                "additionalProperties": true
              }
            },
            "required": ["known", "value", "provenance"]
          }
        },
        "required": ["name", "unlockEventFlagID"]
      },
      "RegionDocument": {
        "type": "object",
        "description": "schema.RegionDocument. Region ID, name and area are separate known facts with provenance from the curated invasion and blue-summon allowlist.",
        "properties": {
          "regionID": {
            "type": "object",
            "description": "schema.Fact[uint32] holding the internal PlayRegionParam row ID stored in UnlockedRegions.",
            "properties": {
              "known": {"type": "boolean", "enum": [true]},
              "value": {"type": "integer", "format": "int64", "minimum": 1, "maximum": 4294967295},
              "provenance": {"type": "object", "additionalProperties": true}
            },
            "required": ["known", "value", "provenance"]
          },
          "name": {
            "type": "object",
            "properties": {
              "known": {"type": "boolean", "enum": [true]},
              "value": {"type": "string", "minLength": 1},
              "provenance": {"type": "object", "additionalProperties": true}
            },
            "required": ["known", "value", "provenance"]
          },
          "area": {
            "type": "object",
            "properties": {
              "known": {"type": "boolean", "enum": [true]},
              "value": {"type": "string", "minLength": 1},
              "provenance": {"type": "object", "additionalProperties": true}
            },
            "required": ["known", "value", "provenance"]
          }
        },
        "required": ["regionID", "name", "area"]
      },
      "SummoningPoolDocument": {
        "type": "object",
        "description": "schema.SummoningPoolDocument. Name, region label and activation event flag ID are separate known facts with provenance from the curated Martyr Effigy table. The region label is a presentation and grouping label only and is never a resource reference.",
        "properties": {
          "name": {
            "type": "object",
            "properties": {
              "known": {"type": "boolean", "enum": [true]},
              "value": {"type": "string", "minLength": 1},
              "provenance": {"type": "object", "additionalProperties": true}
            },
            "required": ["known", "value", "provenance"]
          },
          "regionLabel": {
            "type": "object",
            "properties": {
              "known": {"type": "boolean", "enum": [true]},
              "value": {"type": "string", "minLength": 1},
              "provenance": {"type": "object", "additionalProperties": true}
            },
            "required": ["known", "value", "provenance"]
          },
          "activationEventFlagID": {
            "type": "object",
            "description": "schema.Fact[uint32] holding the activation event flag of the pool, confirmed to lie inside block 670.",
            "properties": {
              "known": {"type": "boolean", "enum": [true]},
              "value": {"type": "integer", "format": "int64", "minimum": 670000, "maximum": 670999},
              "provenance": {"type": "object", "additionalProperties": true}
            },
            "required": ["known", "value", "provenance"]
          }
        },
        "required": ["name", "regionLabel", "activationEventFlagID"]
      },
      "ItemVariant": {
        "type": "object",
        "description": "schema.ItemVariant as stored in the catalog, with full provenance.",
        "additionalProperties": true
      },
      "GetResourceResult": {
        "type": "object",
        "properties": {
          "resource": {
            "$ref": "#/components/schemas/Resource"
          }
        },
        "required": ["resource"]
      },
      "ResourceRef": {
        "type": "object",
        "description": "schema.ResourceRef: the (kind, key) pair that identifies a resource. There is no numeric resource identifier.",
        "properties": {
          "kind": {
            "type": "string",
            "example": "item"
          },
          "key": {
            "type": "string",
            "example": "000F4240"
          }
        },
        "required": ["kind", "key"]
      },
      "Relation": {
        "type": "object",
        "description": "schema.Relation as derived and stored by the catalog. Both endpoints are ResourceRef objects, never numeric IDs.",
        "properties": {
          "from": {
            "$ref": "#/components/schemas/ResourceRef"
          },
          "to": {
            "$ref": "#/components/schemas/ResourceRef"
          },
          "kind": {
            "type": "string",
            "enum": ["compatible_with_aow", "requires_container"]
          },
          "provenance": {
            "type": "object",
            "description": "The evidence the catalog derived the relation from.",
            "additionalProperties": true
          }
        },
        "required": ["from", "to", "kind", "provenance"]
      },
      "GetResourceRelationsResult": {
        "type": "object",
        "properties": {
          "outgoing": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Relation"
            }
          },
          "incoming": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Relation"
            }
          }
        },
        "required": ["outgoing", "incoming"]
      },
      "ResourceListEntry": {
        "type": "object",
        "description": "Picker-sized projection of one resource. Relations, variants, provenance, capabilities and the full item document are deliberately absent; they belong to GetResource, GetResourceRelations and GetItemVariants.",
        "properties": {
          "kind": {
            "type": "string",
            "example": "item"
          },
          "key": {
            "type": "string",
            "example": "000F4240"
          },
          "family": {
            "type": "string",
            "description": "Empty when the resource carries no item document or its family is not known.",
            "example": "weapon"
          },
          "name": {
            "type": "string",
            "description": "The item, colosseum, region or summoning pool name. Empty when the name is not known; there is no fallback to the key or to a placeholder.",
            "example": "Dagger"
          }
        },
        "required": ["kind", "key", "family", "name"]
      },
      "GetResourcesResult": {
        "type": "object",
        "properties": {
          "resources": {
            "type": "array",
            "description": "The requested page in catalog order, kind first and only then key. Empty, never null, when the page lies beyond the last one.",
            "items": {
              "$ref": "#/components/schemas/ResourceListEntry"
            }
          },
          "total": {
            "type": "integer",
            "description": "Number of resources that passed the filters, counted before paging."
          },
          "page": {
            "type": "integer",
            "description": "The effective one-based page, so a requested 0 is reported as 1."
          },
          "pageSize": {
            "type": "integer",
            "description": "The effective page size, so a requested 0 is reported as 50."
          }
        },
        "required": ["resources", "total", "page", "pageSize"]
      },
      "GetItemVariantsResult": {
        "type": "object",
        "properties": {
          "variants": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ItemVariant"
            }
          }
        },
        "required": ["variants"]
      },
      "NetworkParamValues": {
        "type": "object",
        "description": "The tunable PvP/multiplayer parameters grouped by player role. The field set and JSON names are owned by GameCatalog.",
        "properties": {
          "maxBreakInTargetListCount": {
            "type": "integer",
            "format": "int32"
          },
          "breakInRequestIntervalTimeSec": {
            "type": "number",
            "format": "float"
          },
          "breakInRequestTimeOutSec": {
            "type": "number",
            "format": "float"
          },
          "breakInRequestAreaCount": {
            "type": "integer",
            "format": "int32"
          },
          "summonTimeoutTime": {
            "type": "number",
            "format": "float"
          },
          "reloadSignIntervalTime2": {
            "type": "number",
            "format": "float"
          },
          "reloadSignTotalCount": {
            "type": "integer",
            "format": "int32"
          },
          "reloadSignCellCount": {
            "type": "integer",
            "format": "int32"
          },
          "updateSignIntervalTime": {
            "type": "number",
            "format": "float"
          },
          "singGetMax": {
            "type": "integer",
            "format": "int32"
          },
          "signDownloadSpan": {
            "type": "number",
            "format": "float"
          },
          "signUpdateSpan": {
            "type": "number",
            "format": "float"
          },
          "reloadVisitListCoolTime": {
            "type": "number",
            "format": "float"
          },
          "maxCoopBlueSummonCount": {
            "type": "integer",
            "format": "int32"
          },
          "maxVisitListCount": {
            "type": "integer",
            "format": "int32"
          },
          "reloadSearchCoopBlueMin": {
            "type": "number",
            "format": "float"
          },
          "reloadSearchCoopBlueMax": {
            "type": "number",
            "format": "float"
          },
          "allAreaSearchRateCoopBlue": {
            "type": "integer",
            "format": "int32"
          },
          "allAreaSearchRateVsBlue": {
            "type": "integer",
            "format": "int32"
          },
          "visitorListMax": {
            "type": "integer",
            "format": "int32"
          },
          "visitorTimeOutTime": {
            "type": "number",
            "format": "float"
          },
          "visitorDownloadSpan": {
            "type": "number",
            "format": "float"
          }
        },
        "required": [
          "maxBreakInTargetListCount",
          "breakInRequestIntervalTimeSec",
          "breakInRequestTimeOutSec",
          "breakInRequestAreaCount",
          "summonTimeoutTime",
          "reloadSignIntervalTime2",
          "reloadSignTotalCount",
          "reloadSignCellCount",
          "updateSignIntervalTime",
          "singGetMax",
          "signDownloadSpan",
          "signUpdateSpan",
          "reloadVisitListCoolTime",
          "maxCoopBlueSummonCount",
          "maxVisitListCount",
          "reloadSearchCoopBlueMin",
          "reloadSearchCoopBlueMax",
          "allAreaSearchRateCoopBlue",
          "allAreaSearchRateVsBlue",
          "visitorListMax",
          "visitorTimeOutTime",
          "visitorDownloadSpan"
        ]
      },
      "NetworkPreset": {
        "type": "object",
        "description": "One backend preset: its stable identifier and the full parameter values it stands for.",
        "properties": {
          "id": {
            "type": "string"
          },
          "parameters": {
            "$ref": "#/components/schemas/NetworkParamValues"
          }
        },
        "required": ["id", "parameters"]
      },
      "GetNetworkPresetsResult": {
        "type": "object",
        "properties": {
          "presets": {
            "type": "array",
            "description": "Every backend preset in backend order, or exactly the one requested preset. Never null.",
            "items": {
              "$ref": "#/components/schemas/NetworkPreset"
            }
          }
        },
        "required": ["presets"]
      },
      "GetNetworkSettingsResult": {
        "type": "object",
        "description": "The network parameters stored in one save session. The parameter model is the same NetworkParamValues the presets use, so a stored set and a preset set are directly comparable; the endpoint itself compares nothing.",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "parameters": {
            "$ref": "#/components/schemas/NetworkParamValues"
          }
        },
        "required": ["saveSessionID", "parameters"]
      },
      "SetNetworkSettingsRequest": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "networkSettings": {
            "$ref": "#/components/schemas/NetworkParamValues"
          },
          "expectedRevision": {
            "type": "string",
            "pattern": "^(0|[1-9][0-9]*)$"
          }
        },
        "required": ["networkSettings", "expectedRevision"]
      },
      "SetNetworkSettingsResult": {
        "type": "object",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string"
          },
          "networkSettings": {
            "$ref": "#/components/schemas/NetworkParamValues"
          }
        },
        "required": ["saveSessionID", "saveRevision", "networkSettings"]
      },
      "ApplyNetworkPresetRequest": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "presetID": {
            "type": "string",
            "minLength": 1
          },
          "expectedRevision": {
            "type": "string",
            "pattern": "^(0|[1-9][0-9]*)$"
          }
        },
        "required": ["presetID", "expectedRevision"]
      },
      "ApplyNetworkPresetResult": {
        "type": "object",
        "properties": {
          "saveSessionID": {
            "type": "string"
          },
          "saveRevision": {
            "type": "string"
          },
          "presetID": {
            "type": "string"
          },
          "networkSettings": {
            "$ref": "#/components/schemas/NetworkParamValues"
          }
        },
        "required": ["saveSessionID", "saveRevision", "presetID", "networkSettings"]
      },
      "AppearancePresetSummary": {
        "type": "object",
        "description": "List metadata of one appearance preset. The full appearance configuration (voice type, model identifiers, faceShape, body and skin) stays inside GameCatalog and is not returned by this getter.",
        "properties": {
          "id": {
            "type": "string",
            "description": "Stable lowercase kebab-case identifier. It equals the image file name without the .jpg suffix."
          },
          "name": {
            "type": "string",
            "description": "Display name of the preset."
          },
          "image": {
            "type": "string",
            "description": "Asset file name inside backend/gamecatalog/data/assets/appearance. No route serves the image yet."
          },
          "bodyType": {
            "type": "string",
            "description": "Public label of the stored numeric body type: 1 is Type A, 0 is Type B.",
            "enum": ["Type A", "Type B"]
          },
          "tags": {
            "type": "array",
            "description": "Tags of the preset. Never null; empty for every stored preset today.",
            "items": {
              "type": "string"
            }
          }
        },
        "required": ["id", "name", "image", "bodyType", "tags"]
      },
      "GetAppearancePresetsResult": {
        "type": "object",
        "properties": {
          "presets": {
            "type": "array",
            "description": "The matching preset summaries in the order of presets/appearance.json. Never null; empty when nothing matches.",
            "items": {
              "$ref": "#/components/schemas/AppearancePresetSummary"
            }
          }
        },
        "required": ["presets"]
      }
    }
  }
}
